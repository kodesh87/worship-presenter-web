interface:
  display_name: "Close Spec"
  short_description: "Finalize accepted specs and close workflows"
  default_prompt: "Use $close-spek to close the workflow and return only a concise summary with artifact links."
