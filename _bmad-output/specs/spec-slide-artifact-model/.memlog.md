---
topic: Slide Artifact Model: rearchitect the slide plan from flat SlideKind types into template-based Artifacts with placeholder resolution
updated: 2026-07-31T23:16
---

- (direction) User wants to replace flat SlideKind taxonomy (text, divider, song-title, song-lyric, scripture, sermon, closing-prayer, family, image, body) with a template-based Artifact model where each rundown entry is an Artifact with a visual template, placeholders for dynamic content, and optional auto-inserted children (song lyrics).
- (direction) Each Artifact is a visual template (like a reusable slide layout). Fixed Artifacts have hardcoded content (e.g., Bible Verse Contemplation = John 4:23). Dynamic Artifacts resolve placeholders from form input (e.g., Verse Reading fills reference+text from form). Song-title and song-lyric are auto-inserted children based on hymn number from form.
- (direction) Sermon flyer is a special Artifact type: full-bleed uploaded image from form. Family & Youth (Slide 56) is a composite Artifact: 2 photos + structured text with artistic layout. User acknowledges this is tricky and has a specific reference layout.
- (direction) User envisions a future canvas editor (like Canva) for editing Artifacts visually online with WYSIWYG output. This is out of scope for this spec but shapes the architecture: Artifacts must be defined in a way that could later be rendered/edited on a canvas.
- (decision) CAP-1: Artifact Registry — a declarative catalog of all worship slide templates (Welcome, Bible Talk Sequence, Prayer Partners, Opening Song, Verse Reading, Bible Verse Contemplation, Sermon, Family & Youth, etc.) with their layout rules, placeholder schema, and content resolution strategy.
- (decision) CAP-2: Artifact-based Slide Plan — buildSlidePlan outputs Artifact instances (not flat SlideKind items) where each rundown entry maps to one Artifact, and songs expand into child slides automatically.
- (decision) CAP-3: Live Slide Preview uses Artifact types in the badge column (instead of raw SlideKind), with Artifact grouping visible in the rundown table.
- (decision) CAP-4: Unified rendering — each Artifact type carries enough information to render to PPTX (pptxgenjs), web SlideView (React), and web preview (list row) through a single source of truth. The Artifact definition encodes layout rules, not the renderer.
- (constraint) buildSlidePlan remains the single slide planner consumed by PPTX, web slideshow, and presenter mode. The Artifact model is a rearchitecture of what buildSlidePlan outputs, not a new parallel system.
- (constraint) The rundown order is the service order. The Artifact sequence must match the existing slide sequence produced by buildSlidePlan today. No slide reordering in this spec.
- (event) SPEC.md + artifact-catalog.md created. 6 capabilities, 5 constraints, 5 non-goals, 3 open questions. Coherence pass: all capabilities have intent+success pairs; constraints bend design; non-goals present. Preservation pass: all user direction claims (Artifact concept, song auto-insert, sermon flyer, family-youth composite, canvas editor non-goal, fixed Bible verse) landed in SPEC or companion.
- (decision) Canvas editor IS in scope. Purpose: allow a human to edit template layouts visually (one-time design changes), not weekly content. Few layers per slide, so WYSIWYG is achievable. This resolves Q1: layout stored as positioned-element JSON (x,y,w,h,fontSize,color) editable via canvas, rendered to both web and PPTX from the same data.
- (decision) Refined Artifact taxonomy to 7 base types: (1) General — canvas-editable, no weekly placeholders; (2) TextPlaceholder — canvas-editable + text slots that change weekly; (3) FullScreenImage — single full-bleed image placeholder, changes weekly; (4) ImagePlaceholder — canvas-editable + image placeholder slots, changes weekly; (5) MixPlaceholder — canvas-editable + image + text placeholders, changes weekly; (6) SongSet — auto-generated title + lyric slides; (7) Announcement — list-driven full-screen images.
- (decision) Q2 resolved: Family & Youth = MixPlaceholder (single artifact with image + text placeholders). Q3 resolved: Registry must be runtime-mutable (JSON-persisted) since canvas editor saves layout changes. Compile-time TypeScript constants replaced by JSON template definitions loaded at runtime.
- (event) SPEC.md v2 + artifact-catalog.md v2 created. 8 capabilities (added CAP-3 canvas editor, CAP-7 fixed content, CAP-8 dynamic placeholders). All 3 open questions resolved. 1 remaining open question (Q1: Fabric.js vs HTML divs). Coherence pass: all 8 caps have intent+success; all constraints bend design; non-goals present. Preservation pass: all user claims preserved — canvas editor in scope, 7-type taxonomy, Family&Youth as MixPlaceholder, weekly vs fixed distinction.
- (decision) Dynamic Artifacts (like TextPlaceholder) can have standing default content embedded in the template (e.g. default Bible verses), behaving like a General slide if not overridden by the user form.
- (decision) Aligned artifact-catalog.md notes with PPTX extracted JSON data (SSOT) to serve as initial content baseline.
- (event) Self-Validate pass complete. Coherence: passed (Rules 1-6, 8 met). Preservation: passed (all claims landed). 1 Open Question remains (Q1: Fabric.js vs HTML divs).
- (decision) Q1 Resolved: Canvas editor will be built with Fabric.js. It natively supports layering (z-index) which fits the use case.
- (decision) Adopted architecture spine (architecture-epic-16) as a companion to ensure downstream builders follow the defined invariants (SQLite registry, Fat Payload AST, Fabric.js Uncontrolled Wrapper).
- (event) Self-Validate pass complete. Coherence: passed. Preservation: passed (ARCHITECTURE-SPINE.md formally adopted as companion to preserve structural invariants).
- (decision) Confirmed Uncontrolled Wrapper architecture (manual save) is acceptable over auto-save to prioritize canvas performance.
- (decision) CAP-9 added: Implement strict server-side schema validation (e.g., Zod) on the Canvas JSON output to prevent SSRF and ensure required placeholders cannot be deleted or bypassed.
- (direction) User approved the complete Epic 16 / Story 16.1 default decision package on 2026-07-23 and requested bmad-spec followed by spek-to-coding.
- (decision) SQLite is the live Artifact Registry SSOT; data/default-registry.json is a startup seed that inserts missing template IDs only and never overwrites persisted administrator edits.
- (decision) Artifact templates are global across services; only a DB-revalidated admin may access the editor or registry management APIs, while later server-side planners and renderers read the registry internally.
- (decision) Story 16.1 includes registry persistence, seed behavior, strict validation, admin APIs, and the minimal canvas editor; buildSlidePlan migration, unified renderers, and semantic preview remain Stories 16.2 through 16.4.
- (decision) The admin editor route is /admin/artifacts; management APIs are /api/admin/artifacts, /api/admin/artifacts/[id], and /api/admin/artifacts/[id]/reset, all guarded by requireAdminSession.
- (decision) The Story 16.1 editor modifies existing seeded templates only: select, move, resize, edit fixed text/font/color, save, reload, and reset one template; it cannot create/delete templates or elements, and required elements cannot be removed.
- (decision) Fabric.js owns uncontrolled 16:9 canvas state; persisted coordinates use normalized 0-100 values and React reads serialized state only on explicit Save.
- (decision) BibleVerseContemplation is always TextPlaceholder with standing default reference/text values rather than changing base type at runtime.
- (decision) SongSet, FullScreenImage, and Announcement layouts remain registry-defined but are read-only in the Story 16.1 canvas; SongSet supplies separate title and lyric layout variants.
- (decision) Registry image references accept only valid local upload references, bundled public asset paths, or HTTP(S) URLs accepted by the existing image safety policy; unsafe and arbitrary references are rejected server-side.
- (decision) Reset restores one template from its shipped seed without touching other templates; required seed entries remain recoverable.
- (assumption) Registry writes use updatedAt optimistic concurrency and return HTTP 409 for stale saves or resets, following the repository existing stale-write safety pattern.
- (constraint) Story 16.1 must not change SlidePlanItem, SlideKind, buildSlidePlan output, slide order, PPTX rendering, web SlideView rendering, or preview badges; existing slide-plan behavior remains the regression baseline.
- (event) BMad Spec update started from approved defaults; prior file-based runtime persistence wording is superseded by SQLite-live plus JSON-seed semantics.
- (decision) The committed data/default-registry.json is the v0 extraction baseline, not the final v1 contract; Story 16.1 transforms it while preserving geometry and content evidence from data/raw-slides.json and slides-new reference images.
- (decision) Normalized coordinates are percentage units relative to the 16:9 canvas, but elements may extend beyond 0..100 to preserve intentional off-canvas clipping from the source deck; widths and heights remain positive and all values finite.
- (constraint) Every bundled asset reference in the shipped v1 seed must resolve to a committed runtime asset; the local untracked source PPTX may be used for extraction but must not be required at runtime, in tests, or by another host.
- (note) Preservation audit found the v0 seed has 25 templates, only five represented base types, separate song-title/song-lyric entries, off-canvas geometry, BibleVerseContemplation typed general, and references to absent public/assets files; Story 16.1 explicitly normalizes these gaps.
- (note) Wrapper-only content dropped from the product contract: BMad invocation ceremony, host transition mechanics, and chat wording remain operational workflow metadata rather than product requirements.
- (event) Self-Validate coherence pass: PASS. CAP-1 through CAP-9 retain stable unique IDs and intent/success pairs; constraints and non-goals bend scope; Story 16.1 and Epic 16 success signals are demonstrable; one explicit optimistic-concurrency assumption remains and no open question remains.
- (event) Self-Validate preservation pass: PASS. Approved storage, authorization, editor scope, taxonomy, image safety, reset, source-deck geometry, v0 seed transformation, runtime-asset, architecture, and no-renderer-drift claims all land in SPEC.md or its companions.
- (event) SPEC-slide-artifact-model update finalized for Story 16.1 handover with registry-contract.md added and artifact-catalog.md reconciled.
- (event by bmad-spec-validate) Validate Pass 1 (Coherence): FAIL on Spec Law 3. (a) UNSATISFIABLE CONSTRAINT: SPEC.md requires preserving geometry/content evidence from data/raw-slides.json and slides-new/*.jpg, and registry-contract.md names scripts/build-registry.mjs as documenting the mapping. None of the three exist in this repository, and slides-new/ is on the AGENTS.md never-commit list (git ls-files slides-new returns 0) - the contract sends an implementer to files the public-repo guard forbids. (b) FALSE PREMISE: 'the committed data/default-registry.json is a v0 extraction baseline' / 'the current 25-template v0 extraction' - the committed seed is 28 templates at schemaVersion 1 with the six live base types, i.e. the v1 contract this section says Story 16.1 must still produce. CAP-1..9 intent/success, non-goals, IDs and lean prose otherwise pass.
- (event by bmad-spec-validate) Validate Pass 2 (Preservation): PASS - artifact faithfully reflects this log, including the v0-seed audit note. The staleness is reality moving, not a derive failure; this spec was never re-derived after Epic 16 shipped.
- (note by bmad-spec-validate) ONE-DIRECTIONAL SUPERSESSION: spec-artifact-registry-authoring adopts this SPEC.md and registry-contract.md as companions and declares it wins on conflict, including the seven-base-type model. This spec carries no reciprocal notice, so CAP-2 and the registry-contract Base-Type Rules table read as current to anyone opening it directly - which stories 14-x and 16-x do.
- (note by bmad-spec-validate) UNLOGGED HAND-EDIT (commit c04fea0): companions: path repaired from architecture-epic-16/ARCHITECTURE-SPINE.md to architecture-bic-pptx-workflow-2026-07-10/ARCHITECTURE-SPINE.md after the spine fold-in. Recorded here so the next derive does not reinstate the dead path.
- (decision by bmad-spec-update) EXTRACTION-EVIDENCE CONSTRAINT RETIRED (validate finding a). data/raw-slides.json, slides-new/*.jpg and scripts/build-registry.mjs do not exist in this repository and slides-new/ is on the AGENTS.md never-commit list, so the clause requiring Story 16.1 to preserve geometry/content evidence from them was unsatisfiable here - it was written against the frozen bic-pptx-workflow working tree. The v0-to-v1 transformation it governed is complete. What survives as a live invariant, because a future seed edit can still break it: every bundled asset reference in the committed seed must resolve to a committed runtime file. Verified 2026-07-30 at 22 of 22 distinct refs resolving under public/.
- (decision by bmad-spec-update) V0 BASELINE CLAIM RETIRED (validate finding b). 'The committed data/default-registry.json is a v0 extraction baseline' and registry-contract.md's 'current 25-template v0 extraction' are both false as written: the committed seed is 28 templates at schemaVersion 1 carrying the six live base types, which is the v1 contract those sentences said Story 16.1 must still produce.
- (decision by bmad-spec-update) RECIPROCAL SUPERSESSION NOTICE ADDED (validate finding c). spec-artifact-registry-authoring adopts this SPEC.md and registry-contract.md as companions and wins on conflict, including collapsing the seven base types to three kinds and reversing the no-create/no-delete/no-reorder fences. That was visible only from its side. A banner here now names it, so CAP-2 and the registry-contract Base-Type Rules table cannot be read as current authority by stories that cite this spec directly. The seven-type table itself is NOT rewritten here - that is Epic 20 Story 20-2's delivery, and this spec is not its author.
- (decision by bmad-spec-update) DELIVERY STATUS RECORDED (validate finding d). Epic 16 is done (epics.md:230, retrospective 2026-07-26); Stories 16.1-16.5 shipped, 16.2-16.5 without story files with spec-16-2-artifact-pipeline-completion.md as their delivery contract. The Story-16.1-scoped constraints and non-goals are kept verbatim as the DELIVERED SCOPE RECORD rather than deleted - the banner now says so, which is what stops them reading as pending fences. Success signal restated from a thing to be demonstrated into the thing that was.
- (event by bmad-spec-update) Self-Validate Pass 1 (Coherence) after update: PASS. CAP-1..9 unchanged, IDs stable, none renumbered. Spec Law 3 now holds - the unsatisfiable extraction-evidence clause is replaced by the one invariant that can still be broken, and the story-scoped constraints no longer masquerade as live fences because the banner labels them the delivered scope record. Success signal restated as met plus the binding regression floor, still demonstrable.
- (event by bmad-spec-update) Self-Validate Pass 2 (Preservation) after update: PASS. All four logged decisions landed. Content deliberately dropped, logged rather than silent: the 25-template v0 premise and the extraction-input list. The three dead paths survive in exactly one place, named as nonexistent so no future reader goes looking. Nothing else was removed - the seven-type table, Base-Type Rules, and Story 16.1 editor boundary stand as Epic 16's delivered contract under a supersession banner. tests/public-repo-guard.test.mjs 4 of 4 pass.
- (event by bmad-spec-update) Update finalized: SPEC.md, registry-contract.md, artifact-catalog.md re-derived. Findings a, b, c, d from the 2026-07-30 validate sweep are closed.
- (event) (bmad-spec validate, re-sweep) Pass 1 Coherence: PASS. Pass 2 Preservation: PASS. Spot-verified factual claims against live repo: data/default-registry.json has 28 templates at schemaVersion 1 across six base types (matches registry-contract.md); all 22 distinct bundled asset references resolve under public/ (matches the 22-of-22 claim). No new findings.
- (event by bmad-spec-validate) Validate Pass 1 (Coherence): PASS - CAP-1..9 retain intent+success, non-goals (8) and constraints (each excludes something) present, IDs stable since prior settle, all companion/citation paths open (SPEC.md, artifact-catalog.md, registry-contract.md, spec-artifact-registry-authoring/SPEC.md, PRD FR-20 4.10, ARCHITECTURE-SPINE.md AD-11/12/13/14/16, project-context.md, epics.md Epic 16 done + Epic 20 backlog, spec-16-2-artifact-pipeline-completion.md). Validate Pass 2 (Preservation): PASS both directions - every memlog decision lands in SPEC/companions and every load-bearing claim traces back; spot-verified live facts unchanged: data/default-registry.json has 28 templates, schemaVersion 1, six distinct base types (text-placeholder 6, general 18, fullscreen-image 1, song-set 1, mix-placeholder 1, announcement 1); all 22 distinct bundled asset refs (backgroundImage/imageRef/src) resolve under public/. Superseded-in-part banner cross-checked against spec-artifact-registry-authoring/SPEC.md: seven-base-types-to-three-kinds collapse (Constraints: 'Slide kinds are exactly three') and no-create/delete/reorder becoming CAP-2 both confirmed current there; AD-14/AD-16 split (global-across-services reversal) confirmed in ARCHITECTURE-SPINE.md and matches epics.md Epic 20 section. No drift found; no new findings.
- (event) (bmad-spec validate, re-sweep 2026-07-30) Pass 1 Coherence: PASS. Pass 2 Preservation: PASS. No new findings. Live facts re-verified independently: data/default-registry.json is a bare 28-element array, every row carries schemaVersion 1, six distinct base types (general 18, text-placeholder 6, fullscreen-image 1, song-set 1, mix-placeholder 1, announcement 1) - matches registry-contract.md:76; all 22 distinct backgroundImage/imageRef/src references resolve under public/ (0 unresolved) - matches the 22-of-22 claim; layout keys in use are default/title/lyric and the song-set row carries exactly title+lyric, matching the Base-Type Rules table; spec-16-2-artifact-pipeline-completion.md exists as the 16.2-16.5 delivery contract; PRD FR-20 at prd.md:368 under section 4.10 at :362. Epic 16 done at epics.md:229. Supersession banner still matches spec-artifact-registry-authoring, which now additionally adopts the spine directly rather than reaching it through this file.
- (decision) (by bmad-spec-update, Gap 4b) F-4 CLOSED. SPEC.md:64 (seed inserts missing template IDs only) and :65 (templates are global across services) are not Story-16.1-scoped, so the delivered-scope-record caveat never covered them; both are now marked superseded IN PLACE — :64 by AD-17 (the seed is a bootstrap, not a correction channel; a missing-ID gap-filler resurrects a deleted row on every boot) and :65 by AD-16 (a service binds a registry snapshot at creation; only Sync Artifact refreshes it; the admin-only clause of the same bullet stands, AD-16 supersedes only the global clause of AD-14). The :16 supersession banner extends from two enumerated reversals to four — the two-item list affirmatively implied everything else still binds, which was the trap.
- (decision) (by bmad-spec-update, Gap 4b) F-10 CLOSED. The :16 banner named CAP-2 as the seven-base-type casualty and not CAP-8, so CAP-8's success clause ('BibleVerseContemplation remains a TextPlaceholder') read as surviving a kind AD-19 abolished (gone rather than renamed). The banner now names CAP-8 and states the successor: a General carrying a Placeholder Catalog text element, standing defaults intact. CAP-8's text itself stands unchanged under the banner, same treatment as CAP-2 — rewriting it is Epic 20 delivery, not this spec's to author. deferred-work.md:83 compounds the same misreading (records welcome's general-to-text-placeholder change as current state) but is an implementation artifact outside this spec's write surface; noted for the maintainer, not edited here.
- (decision) (by bmad-spec-update, Gap 4b) ONE STEP BEYOND THE NAMED FIX, for coherence: registry-contract.md:5 carries its own supersession banner enumerating the same two reversals, and its lines 11/22/25/143 restate the seed-missing-only and global-across-services clauses — the identical affirmative-implication trap for anyone opening that companion directly, which spec-artifact-registry-authoring's companions: list invites. Its banner is extended to the same four reversals. artifact-catalog.md checked and needs nothing: its banner already names the TextPlaceholder retirement and the General-plus-catalog successor, and it makes no seed or scoping claims.
- (event) (by bmad-spec-update) Self-Validate Pass 1 (Coherence) after Gap 4b: PASS. CAP-1..9 unchanged, IDs stable, nothing renumbered. Spec Law 3 restored on the two Constraints bullets: the reversed clauses no longer masquerade as live — each carries its superseding AD in place while the clauses that still bind (SQLite SSOT, admin-only + DB role re-check) stand unmarked. The banner's four-item enumeration closes the affirmative-implication gap that a two-item list created. Lean check: the banner grew 1 line to 9, every line names a reversal, its AD, and the consequence a downstream reader needs.
- (event) (by bmad-spec-update) Self-Validate Pass 2 (Preservation) after Gap 4b: PASS. F-4 landed (banner two-to-four, :64/:65 marked in place; the bullets moved to ~:72/:73 after the banner expansion — the only line-number citations to the old positions live in review records, verified by grep, so nothing to repair); F-10 landed (banner names CAP-8 and its successor, a General carrying a Placeholder Catalog text element; CAP-8 text stands under the banner, same treatment as CAP-2); registry-contract.md:5 banner matched to the same four reversals. Nothing deleted — the delivered scope record remains verbatim. Out of scope and logged, not silent: deferred-work.md:83.
- (event) (by bmad-spec-update) Gap 4b update finalized: SPEC.md and registry-contract.md re-derived. Review findings F-4 and F-10 (review-cross-document-coverage-2026-07-30-validate-b.md) are closed; sprint-status.yaml Gap 4b action item to be marked done in the same change set.
