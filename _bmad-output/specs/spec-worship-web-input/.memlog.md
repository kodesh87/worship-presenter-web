---
topic: Web boundary for creating and editing worship service records via the existing Next.js web application
updated: 2026-07-30T18:23
---

- (direction) Source: brainstorm-intent.md from brainstorm-worship-web-input-2026-07-19 session
- (direction) Intent: Web-based input channel for operators to create/edit worship service records independently of Telegram/PicoClaw webhook
- (capability) CAP-1: Operator can create a new worship service via structured web form at /services/new, producing ParsedRundown JSON stored in parsed_data
- (capability) CAP-2: Operator can edit an existing service using structured form fields alongside the existing raw-text mode on /services/[id]
- (capability) CAP-3: Hymn input supports dynamic-length list with autocomplete search by SDAH number or title from hymns table
- (capability) CAP-4: Date collision detection warns operator when a service already exists for the chosen date and offers to edit that service instead
- (capability) CAP-5: Form provides live preview using slide-plan rendering so operator sees projected output before saving
- (capability) CAP-6: Scripture fields (themeVerse, verseReading) accept reference strings with optional inline KJV text resolution
- (constraint) Web-created services write parsed_data directly (ParsedRundown shape); raw_payload is set to a marker string, not synthetic telegram text
- (constraint) Downstream consumers (slide-plan, PPTX, presenter) must remain untouched — web form produces identical ParsedRundown shape
- (constraint) Optimistic concurrency via updated_at (409 on stale write) must be honored in both create collision check and edit save
- (constraint) Auth: existing session cookie auth (operator+admin roles) — no new role needed
- (constraint) No new database tables — services table is sufficient; POST /api/services needs a new handler or extend webhook logic
- (decision) POST /api/services route handler to be created (currently only webhook creates services) — accepts structured JSON with source:web
- (decision) PUT /api/services/[id] already supports structured field patches via coerceStructuredFields — no backend change needed for edit
- (decision) Hymn search needs new API endpoint GET /api/hymns?q= to query hymns table for autocomplete
- (assumption) Assumed hymns table is fully populated (695 SDAH entries) and does not need import as part of this feature
- (assumption) Assumed existing EditForm raw textarea mode remains available as fallback — structured mode is additive
- (note) Non-goals: Telegram/PicoClaw changes, collaborative editing, draft staging table, mobile-specific UI, announcement management within worship form
- (event) Self-validate Pass 1 (Coherence): All 6 capabilities have both intent and success. Intents describe WHAT not HOW. All 5 constraints bend design decisions (no decoration). 6 non-goals are explicit. Success signal is concrete and demonstrable. Capability IDs CAP-1 through CAP-6 are stable and unique. Prose is lean. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation): Walked brainstorm-intent.md claim by claim. All load-bearing claims landed in SPEC.md or form-fields.md companion. Dropped: dreamer-phase ideas (liturgy builder, AI-assisted, collaborative editing) — correctly routed to non-goals. Dropped: morphological parameter analysis wrapper — process not content. Dropped: BroadcastChannel live-preview idea — design detail beyond spec scope. VERDICT: PASS
- (event) Spec finalized: SPEC.md + form-fields.md companion. 6 capabilities, 5 constraints, 6 non-goals, 3 assumptions, 3 open questions.
- (decision) Aligned fields mapping with the codebase hybrid design: raw rundown text is required for parsing, structured fields are overlays
- (event) Self-validate Pass 1 (Coherence) after alignment: CAP-1 through CAP-6 have clear intent/success. Constraints updated for raw_payload. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation) after alignment: Hybrid form fields mapped accurately to EditForm.tsx and data-models-monolith.md. VERDICT: PASS
- (event) Spec finalized after alignment. 6 capabilities, 5 constraints, 6 non-goals, 3 assumptions, 3 open questions.
- (decision) Explicitly added sermonGraphicUrl and familyPhotoUrl fields to images_payload object matching PRD addendum slides 41 and 56
- (decision) Mark theme verse slide 26 as fixed John 4:23 template slide and remove it from variable payload
- (decision) Remove sermon title from payload, leaving only sermon speaker name on Slide 40
- (decision) Derived closing prayer person name on Slide 50 from sermon speaker name on Slide 40
- (decision) Split family & youth week payload on Slide 56 into separate photo URLs and text fields
- (decision) Define announcements list payload structure with isOneOff flag for master vs week-specific options
- (event) Self-validate Pass 1 (Coherence) after payload alignment: All 6 capabilities have intent/success. Constraints updated. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation) after payload alignment: Form fields and deck blueprint mapped verbatim to addendum.md and prd.md. VERDICT: PASS
- (event) Spec finalized. 6 capabilities, 5 constraints, 6 non-goals, 3 assumptions, 4 open questions.
- (decision by user) Resolve copy-from-previous: rejected, the service loads from standard active template skeleton
- (decision) Resolve hymn search: preloaded client-side search index is preferred since the dataset is only 695 rows
- (decision) Resolve edit page layout: structured form is the default view with raw text editor as a collapsible section
- (decision) Resolve image sync: no extra sync needed as uploaded images are saved in UPLOADS_DIR and URLs stored in DB
- (event) Self-validate Pass 1 (Coherence) after resolving open questions: SPEC.md updated, all CAPs and Constraints coherent. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation) after resolving open questions: Checked user decisions against SPEC.md. VERDICT: PASS
- (event) Spec finalized. 6 capabilities, 5 constraints, 7 non-goals, 3 assumptions, 0 open questions.
- (decision) Raw participant list is stored as-is for reference; no smart parsing/role extraction is done by the Web Hub (handled on Telegram/picoclaw side).
- (event) Self-validate Pass 1 (Coherence) after participant raw layout update: All CAPs and constraints validated. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation) after participant raw layout update: Checked changes against user instructions. VERDICT: PASS
- (event) Spec finalized. 6 capabilities, 5 constraints, 7 non-goals, 3 assumptions, 0 open questions.
- (direction) User feedback on UI: Combine raw rundown and participant list. Add [Parse] button to manually extract structured fields like Song Leaders and Sermon Speaker. Change hymn inputs to autocomplete dropdowns. Group UI fields explicitly into Bible Talk, Divine Worship, Sermon, Family Prayer, and Youth Prayer.
- (decision) Remove separate raw participants list field. The Raw Rundown Text field is the sole raw input and acts as both rundown and participant list.
- (decision) Add a [Parse] button that manually extracts roles, hymns, and dates from the Raw Rundown Text and populates the Structured Overlays.
- (decision) Change hymn input fields to autocomplete dropdowns that search the hymn index, eliminating the need for a separate side reference helper.
- (decision) Group Structured Overlays distinctly into: Bible Talk, Divine Worship, Sermon, Family Prayer, and Youth Prayer sections for better UI organization.
- (event) Self-validate Pass 1 & 2: Updates reflect unified raw input, manual parse trigger, explicit field groupings, and autocomplete hymns. VERDICT: PASS. Spec updated.
- (direction) User tested /services/2: edit form should match create form; keep Preview/Present/Delete/Download PPTX and Announcements Manage list; remove Order of Service (covered by raw text + live preview); stabilize header/content width across dashboard, /services/new, and /services/[id] (new and [id] share same shell).
- (decision) Edit page chrome keeps service actions Preview, Present, Delete Service, Download PPTX; Announcement flyers retains Manage list link to /announcements.
- (decision) Remove read-only Order of Service card from /services/[id]; raw rundown text + Live Slide Preview are the operator-facing order representation.
- (capability) CAP-7: Create and edit worship forms present the same field set, grouping, labels, and controls; edit page may add service-scoped chrome only.
- (capability) CAP-8: App shell header and main content column width stay visually stable when navigating among /, /services/new, and /services/[id]; create and edit share the same shell.
- (constraint) CreateForm and EditForm structured overlays must stay in lockstep (labels, section nesting, Parse placement); drift is a defect.
- (assumption) Assumed Service Highlights and Unmapped Content cards on edit page remain unless later directed; only Order of Service was called out for removal.
- (question) Should the read-only Announcement flyers strip above the edit form stay as a summary, or is Manage list + in-form announcement editor enough?
- (event) Self-validate Pass 1 (Coherence) after /services/2 UX feedback: CAP-1..CAP-8 each have intent+success; intents are WHAT not HOW; constraints bend decisions (form lockstep, chrome keep/remove, planner/auth/DB); non-goals include Order of Service restore; success signal testable; IDs stable (CAP-7/8 new, no reuse); prose lean. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation) after /services/2 UX feedback: Form match create->CAP-7+form-fields; keep Preview/Present/Delete/Download/Manage list->edit-page-chrome+constraint; remove Order of Service->decision+non-goal+companion; header/width stability new=[id]->CAP-8. Wrapper-only: localhost URL path. VERDICT: PASS
- (note) Wrapper-only content dropped: concrete localhost:3000/services/2 URL (route pattern /services/[id] retained).
- (direction) User clarification: current /services/[id] is a show/run-sheet surface; it must follow create form. Edit form should exist (may be missing or incomplete today). Announcement flyers read-only strip: leave unchanged for now.
- (decision) Open question resolved: keep read-only Announcement flyers strip above edit/show as-is; no change this pass.
- (decision) CAP-7 reframed: /services/[id] show chrome + form body must mirror /services/new create form; a working edit form is required (not optional). Show-only run-sheet that diverges from create is a defect.
- (assumption) Assumed EditForm may already exist in codebase but operator-facing surface still reads as show/run-sheet; contract requires create-parity form experience on /services/[id] regardless.
- (event) Self-validate Pass 1 (Coherence) after show/edit clarification: CAP-2 requires working edit form; CAP-7 reframes show->create-parity; open questions cleared; announcement strip keep in constraint+non-goal; IDs stable. VERDICT: PASS
- (event) Self-validate Pass 2 (Preservation): show must mirror create; edit form required; announcement strip unchanged. VERDICT: PASS
- (decision) Sermon is its own Card section after Divine Worship (not nested inside); Special Song stays in Divine Worship; Family/Youth/Announcements order unchanged
- (note) CAP-6 Resolve KJV requires imported bible_verses (npm run import:kjv); empty corpus must surface actionable error not generic not-found
- (event) Self-validate: CAP-7 Sermon Card split + CAP-6 empty-corpus/ops note preserved; form-fields order synced
- (decision) Remove 'Service Highlights' section from the top of the edit form; operator relies only on 'Raw Rundown Text'.
- (decision) Display hymn title alongside hymn number in text fields for all hymn inputs (Bible Talk, Divine Worship, Special Song).
- (direction) Improve UX/explanation for the Announcement Flyers section usage (e.g. place instructions at the bottom of the section).
- (event by bmad-spec-validate) Validate Pass 1 (Coherence): PASS. CAP-1..8 all carry intent+success; 8 constraints each rule something out; 8 non-goals; success signal is demonstrable (under 3 minutes, create-parity on /services/[id]); IDs stable. One misfile: SPEC.md Assumptions 'Service Highlights and Order of Service cards are removed' is a DECISION (this log, remove-Highlights entry) not an inferred assumption, and it now duplicates the binding Remove table in edit-page-chrome.md. The 'EditForm may already exist' assumption is stale - 14-4 is done.
- (event by bmad-spec-validate) Validate Pass 2 (Preservation): PASS. The three late decisions all landed: remove Service Highlights -> edit-page-chrome.md Remove table; hymn title alongside number -> form-fields.md Raw Rundown Ingestion; Announcement Flyers helper UX -> form-fields.md Announcements. Story 14-6 cites all three. Gap: no self-validate or finalize event was recorded after those three entries.
- (decision by bmad-spec-update) CLEANUP of misfiled Assumptions (validate finding C). The 'Service Highlights and Order of Service cards are removed' clause leaves Assumptions - it is a user DECISION, not an inferred call, and it is already binding in edit-page-chrome.md's Remove table. But the sentence is SPLIT rather than deleted: 'Unmapped Content remains unless later directed' is a genuine still-live assumption and is covered nowhere else in the spec, the companions, or the code, so it stays as an assumption in its own right.
- (decision by bmad-spec-update) The assumption 'EditForm may already exist in the codebase but the surface still reads as a show/run-sheet; contract requires create-parity regardless' is DROPPED as spent. Story 14-4 is done, so the condition it hedged against no longer exists. CAP-2 and CAP-7 carry the create-parity requirement on their own.
- (event by bmad-spec-update) Self-Validate Pass 1 (Coherence): PASS. CAP-1..8 untouched. Assumptions now hold only genuine inferred calls; the Service Highlights / Order of Service decision moved out and its binding home is edit-page-chrome.md's Remove table. Pass 2 (Preservation): PASS - the 'Unmapped Content remains unless later directed' clause was split off and kept because it is covered nowhere else in the spec, companions or code. The spent EditForm hedge was dropped deliberately, logged not silent. Closes validate finding C.
- (event) (bmad-spec validate, re-sweep) Pass 1 Coherence: PASS. Pass 2 Preservation: PASS. Spot-verified data/hymns.json has 695 entries (matches Assumptions). Prior validate finding C (misfiled Assumptions, stale EditForm hedge) confirmed already closed in current SPEC.md.
- (event by bmad-spec-validate) Validate Pass 1 (Coherence) finding: FAIL on 2 stale Assumptions in SPEC.md, code-verified against src/. (A) 'Structured mode is the default form view, with the raw text editor placed in a collapsible section' is stale from the pre-unification layout decision (this log, resolve-edit-page-layout entry) and was superseded by the later 'combine raw rundown and participant list' decision (this log, UI feedback entry) without the Assumptions bullet being updated; CreateForm.tsx and EditForm.tsx render Raw Rundown Text as an always-visible top Card, not a collapsible section, and form-fields.md already states it correctly as the single primary input. (B) 'Hymn searching/autocomplete in the form is performed client-side using a preloaded index of the 695 SDAH entries' contradicts src/components/HymnNumberAutocomplete.tsx and src/app/api/hymns/route.ts: the component debounce-queries GET /api/hymns?q= per keystroke with a growing client cache, and pages seed only the hymns already referenced by a service's initial values (see src/app/services/[id]/page.tsx seedNumbers) rather than preloading the full 695-entry index.
- (decision by bmad-spec-validate) DROP the 'Structured mode is the default form view, with the raw text editor placed in a collapsible section' assumption as spent. It described the pre-unification layout (structured overlays as the default view, raw text a collapsible fallback), a design later replaced by the unified single Raw Rundown Text input (this log, 'combine raw rundown and participant list' decision) that form-fields.md already documents as the sole primary input with structured overlays populated via Parse. No replacement bullet is needed; the current behavior is already binding contract text in form-fields.md's Form Field Mapping and Structured Fields Overlays sections.
- (decision by bmad-spec-validate) CORRECT the hymn-search Assumption to match the built implementation instead of the earlier 'preloaded client-side index' preference (this log, resolve-hymn-search entry). Actual behavior, verified in src/components/HymnNumberAutocomplete.tsx and src/app/api/hymns/route.ts: the input debounces 180ms then queries GET /api/hymns?q= (capped at 40 rows) as the operator types, batches numeric lookups via GET /api/hymns?numbers=, and keeps a module-level cache of settled queries and known-by-number rows so repeats and Parse-hydrated fields cost no extra round trip; the server seeds only the hymns an existing service's initial values already reference (src/app/services/[id]/page.tsx seedNumbers), not the full 695-row hymnal. No full client-side preload of the hymnal exists.
- (event by bmad-spec-validate) Fix applied: SPEC.md Assumptions edited — dropped the spent 'structured default view / collapsible raw text' bullet (already covered as binding text in form-fields.md) and rewrote the hymn-search bullet to describe the debounced GET /api/hymns?q= + numbers= batching + client cache actually implemented in HymnNumberAutocomplete.tsx, replacing the stale 'preloaded full index' claim. Re-validate Pass 1 (Coherence): PASS — CAP-1..8 each carry intent+success unchanged; 6 constraints each rule something out; 8 non-goals; success signal concrete (under 3 minutes, create-parity, 409 on stale write); CAP IDs stable across full memlog history, none renumbered; prose lean; the two corrected Assumptions now match source (verified against src/app/api/services/route.ts POST, src/app/api/services/[id]/route.ts PUT 409/updated_at, src/app/api/hymns/route.ts, src/components/HymnNumberAutocomplete.tsx, src/app/services/new/CreateForm.tsx, src/app/services/[id]/EditForm.tsx and page.tsx, src/app/services/new/page.tsx, edit-page-chrome.md). Validate Pass 2 (Preservation): PASS both directions — (a) walked memlog entries 1-84, every load-bearing item lands in SPEC.md/form-fields.md/edit-page-chrome.md, including the two corrections just logged; (b) walked SPEC.md/companions claim by claim, every load-bearing claim traces to a memlog entry, no unlogged content found. Verified: POST /api/services and PUT /api/services/[id] (409 + updated_at) exist as specified; GET /api/hymns and GET /api/scripture (503 actionable error on empty KJV corpus) exist as specified; CreateForm.tsx and EditForm.tsx share identical section order (Raw Rundown Text+Parse -> Bible Talk -> Divine Worship -> Sermon (own Card) -> Family of the Week -> Youth of the Week -> Announcement Flyers with bottom-of-section usage note); /services/[id] page.tsx has no Order of Service or Service Highlights card and keeps Preview/Present/Delete/Download PPTX/Manage list plus the read-only announcement strip above the form; /, /services/new, and /services/[id] all wrap content in the same max-w-5xl shell.
- (event) (bmad-spec validate, re-sweep 2026-07-30) Pass 1 Coherence: PASS. Pass 2 Preservation: PASS. No new findings. Load-bearing claims re-verified against live source: data/hymns.json has 695 entries (matches Assumptions); POST exists at src/app/api/services/route.ts:28 alongside GET at :11; src/app/api/hymns/route.ts has MAX_LIMIT 40 at :9 and the numbers= batch branch at :76-85, matching the corrected hymn-search Assumption; src/app/api/scripture/route.ts returns 503 at :24 for the unimported KJV corpus, matching CAP-6's actionable-error clause; /, /services/new and /services/[id] all wrap content in max-w-5xl (page.tsx:43, new/page.tsx:44, [id]/page.tsx:154), matching CAP-8 shell parity.
