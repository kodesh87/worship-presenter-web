---
topic: "Lyric formatting, chorus injection, and flow skip rules"
updated: 2026-07-30T15:23
---

- **decision:** Implement continuous text formatting for verse lyrics. Replace `\n` with `; ` unless preceded by punctuation (where it becomes ` `).
- **decision:** Chorus must be injected after every verse for all songs, regardless of how it is stored in the database.
- **decision:** Skip song title slides for specific transitional parts (e.g. after intercessory prayer, "We Have This Hope").
- **capability (CAP-1):** Format verse lyrics into continuous paragraphs based on punctuation presence at line ends.
  - intent: Consolidate vertical space and improve readability of lyrics.
  - success: Lyrics like `Verse 1\nLine 1\nLine 2` become `Line 1; Line 2` (or `Line 1. Line 2` if punctuation exists).
- **capability (CAP-2):** Inject Chorus after every mapped Verse during slide generation.
  - intent: Automate chorus repetition so operators don't have to manually ensure the chorus is sung after every verse.
  - success: The slide plan sequence for any song with a Chorus becomes `Verse 1 -> Chorus -> Verse 2 -> Chorus` etc.
- **capability (CAP-3):** Skip specific song title slides during the intercessory prayer flow.
  - intent: Remove unnecessary transitional slides that disrupt the flow of the worship service.
  - success: Song title slides for "Now Dear Lord As We Pray", "Hear Our Prayer, O Lord", and "We Have This Hope" are explicitly filtered out from the slide plan.
- **constraint:** Long continuous verses should still split across multiple slides if they exceed optimal character limits, maintaining a readable presentation.
- **constraint:** The skip rules apply only to the generated slide plan (PPTX and Preview), not to the parsed rundown data structure.
- **event:** Self-validation completed.
- **event:** Spec finalized.
- (decision) Format 'We Have This Hope' (SDAH #214 fallback) into exactly 2 fixed slides with original line breaks preserved (no continuous ';' joining).
- (capability) CAP-4: Format 'We Have This Hope' lyrics as 2 fixed slides. Intent: Maintain traditional poetic stanza pacing for the closing prayer song. Success: 'We Have This Hope' yields exactly 2 slides preserving manual line breaks, exempt from continuous prose joining.
- (event by bmad-spec-validate) Validate Pass 1 (Coherence): PASS on Spec Law 1-6,8. Deviations from spec-template, not Spec Law: no id: in frontmatter (other three specs carry id: SPEC-<slug>), no canonical-contract banner, '## Success Signal' vs '## Success signal', '**Intent**:/**Success**:' vs '**intent:/success:**', CAP headings carry titles instead of bare CAP-n.
- (event by bmad-spec-validate) Validate Pass 2 (Preservation): FAIL. Owner decision 2026-07-30 (epics.md:325, Epic 19 RETIRED; spine AD-20 + ARCHITECTURE-SPINE.md:307) makes hymns 671, 684 and We Have This Hope General registry entries and REMOVES skipTitle rather than migrating it. CAP-3 success still says title slides are 'explicitly filtered out' - under that decision there is nothing to filter. CAP-4's fixed 2-slide line breaks become canvas text and stop passing the FR-5 verse/Reff splitter and data/hymns.json. Neither this log nor SPEC.md records it. Three skipTitle sites still live at src/lib/slide-plan.ts:438,460,550.
- (note by bmad-spec-validate) No self-validate or finalize event was recorded after CAP-4 was appended; the last derive went unverified until this sweep.
- (decision by owner) CAP-3 and CAP-4 RETIRED. Reason: the three songs they govern - SDAH 671 'Now, Dear Lord, as we pray', 684 'Hear our prayer, O Lord', 214 'We have this hope' - will be set MANUALLY as General registry entries on free canvas. On a General slide the title display, the number of slides, the line breaks and the text position are all authored by hand on the canvas. So there is no generated title left to filter (CAP-3) and no fixed 2-slide splitting rule left to enforce (CAP-4). Both capabilities dissolve into canvas authoring rather than being reimplemented anywhere.
- (decision by owner) Retirement takes effect on delivery of Story 20-1 (currently backlog), not today. As of 2026-07-30 the shipped behavior is still the skipTitle path at src/lib/slide-plan.ts:438 (671), :460 (684), :550 (214) plus weHaveThisHopeFixed at :165 via src/lib/lyrics.ts:109, covered by two tests named 'CAP-4:' at tests/lyrics.test.mjs:137 and :169. The retired entries therefore stay visible in the spec as the description of live behavior and as the record of what Story 20.1's General seed must reproduce on canvas - epics.md:332 states this is the only record of those sites and their liturgical reasons.
- (decision by bmad-spec-update) IDs CAP-3 and CAP-4 are burned per Spec Law 6 - never reused, never renumbered. The next new capability in this spec takes CAP-5. CAP-1 (continuous lyric formatting) and CAP-2 (chorus injection after every verse) are UNAFFECTED: they govern ordinary hymns, which remain SongSet under the Epic 20 three-kind model.
- (decision by bmad-spec-update) The two constraints that served only the retired pair follow them into retirement: 'skip rules apply only to the generated slide plan, not the parsed rundown' and 'We Have This Hope must be exempt from continuous line formatting'. Success signal restated so it no longer asserts a filter or a fixed 2-slide count as the contract.
- (event by bmad-spec-update) Self-Validate Pass 1 (Coherence): PASS. Live capabilities are CAP-1 and CAP-2, both with intent+success. The one surviving constraint (long verses still split at character limits) still rules something out. Non-goals unchanged. Success signal reduced to what this spec can still be tested against - continuous chunking plus chorus repetition - with the two retired clauses relocated rather than deleted. IDs CAP-3/CAP-4 burned, nothing renumbered.
- (event by bmad-spec-update) Self-Validate Pass 2 (Preservation): PASS. Nothing was deleted. CAP-3, CAP-4 and their two constraints are kept verbatim under Retired capabilities with the reason (manual General free-canvas authoring), the three hymn numbers and titles, the effective condition (Story 20-1 delivery), and the live code and test sites they still describe. The Why paragraph now names the third original force and says it is no longer answered here rather than dropping it. This closes validate finding 4 from the 2026-07-30 sweep.
- (decision by bmad-spec-update) SHAPE NORMALIZED to spec-template.md and the three sibling specs (validate finding D). Added the id: SPEC-lyrics-and-flow frontmatter field, which this spec never had - it is what stories cite a spec by, so the file previously had no formal name. Added the canonical-contract banner. Fixed '## Success Signal' to '## Success signal'. Changed '**Intent**:'/'**Success**:' to the template's '**intent:**'/'**success:**' - downstream BMad skills match on that form. Title heading '# SPEC: Lyric Formatting and Flow Rules' shortened to '# Lyric Formatting and Flow Rules' to match the siblings, which carry no SPEC: prefix.
- (decision by bmad-spec-update) DELIBERATE DEVIATION KEPT: the descriptive CAP titles ('**CAP-1: Continuous Lyric Formatting**') stay, though the template specifies bare '**CAP-1**'. They aid reading and no consumer breaks on the extra text. Recorded so a later run does not read it as drift and strip them.
- (event by bmad-spec-update) Self-Validate after shape normalization: Pass 1 PASS, Pass 2 PASS. No contract content changed - only frontmatter id, the canonical-contract banner, heading case, title prefix, and intent/success markup. CAP-1/CAP-2 semantics and the Retired capabilities section are byte-identical in meaning. Closes validate finding D.
- (event) (bmad-spec validate, re-sweep) Pass 1 Coherence: PASS. Pass 2 Preservation: PASS. No changes from prior finalized state — CAP-1/CAP-2 live, CAP-3/CAP-4 retired verbatim with reason and effective condition, IDs burned not reused.
