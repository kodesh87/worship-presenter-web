# generated: 2026-07-10
# last_updated: 2026-08-03 (Story 23.1 context created and ready for development;
#   Epic 23 is active. Story 23.2 remains backlog, waiting on Stories 23.1 and 22.3.)
# project: BIC Worship Presentation Automation
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#   - retired: Deliberately not implemented; superseded by a decision recorded elsewhere.
#     Distinct from `done` (nothing shipped) and from `backlog` (nothing is waiting).
#     Added 2026-07-30 for Epic 19, whose goal architecture AD-20 met by another route.
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review
#   - done: Story completed
#   - retired: Deliberately not implemented (see the epic's note for what replaced it)
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#   - n/a: Not applicable — the epic was retired, so there is nothing to retro

generated: 2026-07-10
last_updated: 2026-08-03 # Story 23.1 code review closed; status moved to done.
project: BIC Worship Presentation Automation
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # --- Epics 1–5: shipped story ACs (vertical slice on main) ---
  epic-1: done
  1-1-next-js-foundation-and-monorepo-setup: done
  1-2-basic-authentication-and-roles: done
  epic-1-retrospective: optional
  epic-2: done
  2-1-telegram-webhook-endpoint: done
  2-2-rundown-parser-and-hymnal-db-integration: done
  epic-2-retrospective: optional
  epic-3: done
  3-1-core-presentation-generation-pptx: done
  epic-3-retrospective: optional
  epic-4: done
  4-1-web-hub-service-list-and-run-sheet: done
  epic-4-retrospective: optional
  epic-5: done
  5-1-parser-robustness: done
  5-2-delete-service: done
  5-3-edit-and-regenerate: done
  5-4-image-support: done
  epic-5-retrospective: optional

  # --- Epic 6: Phase 1 gap closure (PRD-complete MVP) ---
  epic-6: done
  6-1-persistent-announcement-list: done
  6-2-per-person-admin-operator-auth: done
  6-3-deck-blueprint-fidelity: done
  6-4-section-aware-hymn-mapping: done
  6-5-picoclaw-intake-readback: done
  6-6-automated-tests-parser-auth-webhook: done
  6-7-image-url-allowlist-ssrf: done
  6-8-deploy-sqlite-hardening: done
  epic-6-retrospective: optional

  # --- Epic 7: Phase 1 residuals ---
  epic-7: done
  7-1-services-list-search-api: done
  7-2-run-sheet-timings: done
  7-3-part-c-standing-slides: done
  7-4-font-deploy-note: done
  epic-7-retrospective: optional

  # --- Epic 8: Phase 2 Web Slideshow (FR-9/15) ---
  epic-8: done
  8-1-web-slideshow-slide-plan: done
  epic-8-retrospective: optional

  # --- Epic 9: Phase 3 Telegram corrections (FR-12/13b) ---
  epic-9: done
  9-1-telegram-correction-concurrency: done
  epic-9-retrospective: optional

  # --- Epic 10: Phase 4 PPTX retention (FR-10b) ---
  epic-10: done
  10-1-pptx-retention-cache: done
  epic-10-retrospective: optional

  # --- Epic 11: Phase 5 Presenter Mode (FR-16) ---
  epic-11: done
  11-1-presenter-mode: done
  epic-11-retrospective: optional

  # --- Epic 12: Phase 6 KJV scripture (FR-19) ---
  epic-12: done
  12-1-kjv-scripture-display: done
  epic-12-retrospective: optional

  # --- Epic 13: Hub UX + LiveServer gap (post-acad206 vibe → BMAD) ---
  epic-13: done
  13-1-liveserver-docker-deploy: done
  13-2-hub-shell-header-profile: done
  13-3-local-announcement-uploads: done
  epic-13-retrospective: optional

  # --- Epic 14: Worship Web Input Boundary (post-b679ff7 vibe → BMAD) ---
  # Closed 2026-07-29 by Correct Course: all six stories were already `done`, so
  # `in-progress` contradicted this file's own definition ("done: All stories in
  # epic completed"). Realizes FR-11 and FR-11b.
  epic-14: done
  14-1-worship-web-input-boundary: done
  14-2-worship-web-input-ux-refinements: done
  14-3-worship-web-input-ui-tweaks: done
  14-4-service-page-create-parity-shell: done
  14-5-sermon-section-kjv-resolve: done
  14-6-worship-form-ux-polish: done
  epic-14-retrospective: optional

  # --- Epic 15: Parser & Rendering Refinements (Phase 2) ---
  epic-15: done
  15-1-lyric-formatting-and-flow-skips: done
  epic-15-retrospective: done

  # --- Epic 16: Slide Artifact Model Refactoring ---
  epic-16: done
  16-1-artifact-registry-canvas-editor-foundation: done
  # Stories 16.2-16.5 shipped without story files, so no acceptance criteria were
  # ever authored for them. The four keys are retired here rather than backfilled
  # with AC written to match already-shipped code (AC that cannot fail is not a
  # verification instrument). Delivery contract:
  #   spec-16-2-artifact-pipeline-completion.md (this folder)
  # Scope narrative stays in ../planning-artifacts/epics.md "Epic 16".
  # Decision: ../planning-artifacts/sprint-change-proposal-2026-07-29.md
  epic-16-retrospective: done

  # --- Epic 17: An operator surface that is readable and honest ---
  # Created 2026-07-29 from the readiness assessment's product defects, through
  # Epic → Story rather than inline patching. Stories 17.1 and 17.8 are done;
  # 17.2 is `ready-for-dev`; 17.3–17.7 remain `backlog`.
  epic-17: in-progress
  17-1-reachable-dark-mode: done # 2026-08-01 CLOSED by the owner. Four review rounds, 15+19+25+12 patch items, and the one blocking decision item all closed; AC-4 scoped in writing to the token guarantee with its shell half owned by Story 17.7. The four guard narrownesses that had no owner are now Story 17.8, so deferred-work.md carries ZERO `owner: unassigned` entries — that, not the absence of latent holes, was the condition for closing. PRIOR ENTRY, at review # 2026-08-01 LATEST, bmad-architecture Update run (AD-24 closure-gate ceiling repair): **the round-4 decision item is CLOSED, so the stated blocker on `done` is gone — but the `done` call is the owner's, and this row does not make it.** The stale ceiling bullet and sub-bullet (ii) are rewritten, AD-24's rule clause is amended IN PLACE from "two sets" to the FOUR hardcoded lists the gate actually has, and the `projector/page.tsx:71` citation is repaired to `:85` at both sites. Nothing renumbered, no AD added; 24 AD headings before and after; lint_spine 0 findings; theme-chrome 48/48; tree clean (one measurement probe applied and reverted). **THREE OF THE FOUR THINGS THE OWNER'S SCOPE NOTE TOLD THIS RUN TO PRESERVE HAD GONE STALE** between the note being written and round 4's patches landing, so following it literally would have written two fresh falsehoods into the authority artifact: the `exportedProps` clause needed RETRACTING at the composed-type spelling rather than narrowing (P3 closed it); P5 closed the focusable guard's listed-files narrowing the same day; and (ii)'s arithmetic was wrong IN KIND — `FULL_SCREEN` feeds two loops, so the MEASURED result of adding both Server-Component shells is **52 tests / 4 failures**, not two, the extra pair being `exportedProps` (:927) refusing to read an `export default async function` at all. **The Reviewer Gate opened new findings for the FOURTH consecutive run, and for the second run running the headline findings were against the amendment ITSELF.** Three lenses converged on one: the spine had come to FORBID AND MANDATE THE SAME CHANGE — the new text called widening `FULL_SCREEN` "the widening to refuse" while AD-24:218 records the owner's ratified design as "`FULL_SCREEN` widened to it". Resolved by stating the implicit distinction: a route-group SEGMENT is the encoding (every page inherits, no registration step to forget); what must be refused is another LEAF FILE. **The gate also refuted this run's own headline claim** that round 4 broke the widen-the-list pattern. It did not: round 4 widened `LITERAL_OUTLINE_COLOUR` (:697) from four excluded spellings to nine after round 3 had named a positive colour vocabulary as the alternative — and that widened list is ALREADY DEFEATED in the same direction, reproduced against the shipped regex, `focus-visible:outline-[transparent]` / `outline-[inherit]` / `outline-[color:inherit]` accepted, none carrying a token name so no guard sees them. The pattern stays on the books in its general form. **FIVE code-owned findings filed in `deferred-work.md` under a new 2026-08-01 heading, not patched — an architecture Update run does not touch production code:** that focus-ring bypass; the `className` guard's index-signature hole plus its `.tsx`-only call-site belt (`React.createElement` from a `.ts` module is invisible); the EDGE-WIDTH GUARD NEVER RECEIVING THE TRANSITIVE SWEEP the token and focusable guards got (`EDGE_UTILITY` consumed at :631 and nowhere else, so a reachable `.tsx` is token- and focus-scanned but not edge-scanned); the four hardcoded room-facing lists where AD-24 claimed two; and the async default-export ceiling. **Three of those five are live narrowings of AC-4's own guard, all latent today** — same disposition round 4 gave DARK_VARIANT — which is the material argument for a round 5 and is the owner's call, not this run's. NOT DONE, reported rather than acted on: AD-24 grew again and the AD-25 split the 2026-07-31 rubric walker proposed is still unmade; the adversarial lens's proposal that AD-24 assert the PROPERTY (every room-facing URL resolves through a segment emitting the reset before first paint) rather than the mechanism spelling is a new invariant reaching into 17.7's design, so it is surfaced in the spine and left to the owner; and no story owns `PROJECTED`'s criterion. **The version/reality-check lens returned this run's own two passages CITATION-CLEAN** (all 11 line refs exact, 48-test baseline independently reproduced) and found the rot everywhere else — six spine-internal repairs applied, all verified by the parent first. **Two are claims about sibling artifacts that HAVE SINCE BEEN REPAIRED**, which is citation rot running in the direction that wastes work rather than hides it: the spine said the `EXPERIENCE.md` bare-`AD-4` repair *"is tracked nowhere"* while citing a BLANK LINE (`EXPERIENCE.md:153`) — `bmad-ux` had already made it, at `:185`, in its own voice; and a Deferred entry claimed two affordance handoffs to `EXPERIENCE.md` had *"not been received"* when both were received WITH OWNERS (Open Item 6 / Story 20.8, Open Item 7 / Story 20.3), the latter having found a second face the spine never named. Four more: `AD-23` cited `prd.md:305` (an offline-cache assumption) for FR-7, which is at `:179-180` with the cross-surface obligation verbatim at `:304`; NFR-3's binding cited `prd.md:550`, the Privacy paragraph, not `:563`; `SPEC.md:88`→`:89`; and **the Next security bullet's non-applicability argument was HALF WRONG** — Turbopack is the Next 16 default for `next dev`/`next build` and `next.config.ts` carries no opt-out, so this project DOES have that half and the CVE-2026-64642 dismissal now rests on the i18n leg alone. The conclusion survives, the stated reason does not — the third time this file has been bitten by a refutable reason, and it mattered because it handed a reader grounds to dismiss the bullet that also carries the SSRF bearing on AD-8 and `safeNextPath`. **Stack drift vs `package.json`: zero, row by row**; one undercount fixed (TypeScript is two majors behind, not one). PRIOR ENTRY, bmad-dev-story round-3 remediation: **all 18 remaining round-3 items closed; back to `review`.** The 19th patch item and the blocking decision were already closed 2026-07-31 by the bmad-architecture Update run, which is the channel AGENTS.md requires and not this workflow's to substitute for. Round 3 named its own shape — a rule applied too narrowly gets closed by widening the list instead of encoding the rule — so the remediation was written against the shape: three guard findings collapsed into ONE JSX walk (`walkJsx`/`openingTag`/`enclosingTag`) plus ONE shared lexical rule (`expressionPosition`, which answers the same question for `/` in `stripComments` and `<` in the walk), and the focusables, branch roots, header row and failure branches all ask that walk now instead of each carrying its own narrower regex. The badge guard's POLARITY IS INVERTED: universal over every `.tsx` and `.ts` under `src/`, per class value and per site, every chromatic hue at any shade, with 18 pinned exceptions each carrying a source citation and an owning key or AC, compared as a multiset in BOTH directions so an unlisted offender fails and a fixed-but-still-listed entry fails too. The closure walk enqueues every extension, reads `export … from` and bare side-effect imports, and asserts a floor of 27 reached modules against the 12 it was scanning. Two guards ended stricter than their findings asked (`fixed inset-0` required on the full-screen root; the reach floor). Code: the projector failure branch scrolls — MEASURED, the old shape hid 112px of error detail with no scrollbar and `scrollTop` pinned at 0; `header-chrome` splits into box + tone so the profile trigger stops hand-rolling the third copy, verified as NO VISUAL CHANGE by computed-style equality in both themes and by running the real `cn()` over the ThemeToggle merge path; the shell claim gains a floor and a `generation` token. Record: this file's superseded VERIFICATION GAP paragraph is DELETED and replaced by a record of what it claimed and which clauses retract it (that was round 3's only `high`, and it was this row); `epics.md` moved to `review`; `DESIGN.md`'s LogoutButton rows rewritten to the shipped `text-destructive`; `EXPERIENCE.md` scoped to the shells' own elements with all FOUR holes named; the story's own `layout.tsx:26` citation repaired. **The rules found three things the lists had not:** an unclassed context-provider root in `SlideGridDialog` (correcting AC-3's rule from *root* to *outermost classed*), 15 unwalked modules rather than the estimated 14, and a FOURTH copy of the header box at `AnnouncementsManager.tsx:275` — deliberately not absorbed into 17.1, because it is a page action button and not header chrome. Verification: full suite **386 tests / 385 pass / 0 fail / 1 skipped**; theme-chrome **47/47**; public-repo guard **4/4**; `tsc --noEmit` clean; `eslint src tests` **31 problems**, identical to HEAD measured by stashing this change set and re-running, so ZERO introduced; **24 defect injections, 23 react** — a property of those 24, NOT a coverage claim (round 4 found nine further defects that stay green, seven of them in guards this pass rewrote); the one that does not react is the shell-claim floor, which has no public path to reach once the generation token exists, stated in the source rather than covered by a source-text assertion. NOT VERIFIED IN THE RUNNING APP, and it is a real gap: the header row and the projector failure branch were never seen rendered — both are behind the session gate, the latter also needs a forced registry failure, and this workflow does not sign in; the two measurements above are of the CSS against the real compiled stylesheet, not of the rendered pages. PRIOR ENTRY, 2026-07-31, bmad-architecture Update run: **the BLOCKING prerequisite is now MET.** Both inline `ARCHITECTURE-SPINE.md` edits (`:218`, `:393`) are RATIFIED as amendments of that file rather than accepted as citation repairs — verified before ratifying, not rubber-stamped — and the stale `@/lib`/`componentImports` bullet at `:392` is repaired by the same hand, with the `.ts`-ceiling nuance the review asked to be carried. 24 AD headings before and after; nothing renumbered, no AD added; lint_spine 0 findings; theme-chrome 43/43. **The gate opened findings for a third consecutive time, and this time against the amendments themselves — three corrections landed on the run's own text:** (1) it replaced a refutable reason with a new refutable ABSOLUTE ("no browser-side mechanism can close the shell gap") which Next's own docs refute with a pre-paint inline `<script>` — the mechanism `next-themes` uses here, so the set handed to Story 17.7 had excluded the framework's answer; (2) the standing instruction "`FULL_SCREEN` gains both shells in the same change set" was UNSATISFIABLE — that set asserts every member calls `useProjectedShell()` and neither Server-Component shell can, so following it turns 43/43 into 2 failures; the closing change set must rewrite the assertion, not extend the list; (3) a route-segment stylesheet — which the earlier text MANDATED — has no release (Next does not remove stylesheets across client navigation), so a CSS-only closure strands the operator's whole hub at black with no gutter after Exit, failing on the opposite screen from the one being protected. AD-24's gap clause now states the constraint as a TIMING one with three pre-paint mechanisms separated by whether they release, and flags that a server-set class can only be encoded at the root layout, which couples AD-24's paint closure to AD-5's matcher. Also corrected while the spine was open, both pre-existing and both false: AD-23 still accused `docs/architecture.md:61` of hardcoding a crossfade after the 2026-07-30 reconciliation fixed it, and the Next security bullet claimed "Next has stated that earlier minors are not back-patched" — refuted, 15.5.21 was patched the same day. Retraction residue swept from three downstream projections (Structural Seed, the Client-state convention row, the mermaid edge). New at spine altitude: `projected-shell.ts:34-36`'s header comment tells a 17.7 implementer a Server Component can reach the reset without a hook — FALSE, filed in `deferred-work.md` under 17.7, not patched here; plus two Deferred entries (the shared reset's single colour is wrong for `PresenterOperator` as its queued third consumer; CI/release is the one dimension AD-21 binds by name and nothing owns). NOT done, reported to the owner instead: AD-24 is now ~2000 words and the rubric walker named a safe split into a new AD-25 — that is a restructuring pass the owner has not asked for and it moves a heavily-cited AD. PRIOR ENTRY, 2026-07-31, bmad-code-review round 3: back to `in-progress` with **19 patch action items**, 1 owner-resolved decision that BLOCKS closing, 1 deferred, 6 dismissed. Round 2's 25 items ARE closed — verified item by item against the code, none is a checkbox over nothing. The 19 are one pattern, now in its third round: a rule applied too narrowly gets closed by widening the list instead of encoding the rule. Nine of them are the AC-4 guard AD-24 names as its closure gate, all latent and all reproduced by injecting a defect and watching 43/43 stay green: the closure walk stops at the first `.ts` hop (so the removed `@/lib` directory exemption is now an extension exemption over the same directory) and `moduleImports` cannot see `export … from`, already spelled at `src/lib/parsed-fields.ts:432`; the `text-white` root guard reads `jsxReturnBranches(...)[0]`, the exact defect that function's own comment says it was written to remove, while the AC-3 guard 130 lines below iterates every branch; `jsxReturnBranches` matches only parenthesised returns though `SlideView.tsx:19` is paren-less; the focus-ring guard's `/focus-visible:outline-\w/` accepts `outline-none`, which deletes the ring it protects; "every projected focusable" is two hardcoded files and `<Link>` only, with `ProjectorClient` absent; the badge guard is a 4-file x 3-hue table satisfied file-wide by one match; `stripComments` has no regex-literal state and `\//` deletes the rest of a line from the scan (the false-pass direction); `exportedProps` is defeated by a named props type, which is house style at `Header.tsx:9`; `EDGE_END` misses `border-2${x}` and `border-2!`. Four code items: the projector's failure branch still cannot scroll after the same commit declared it and the slideshow's to be one failure and fixed one (`projector/page.tsx:71`); the `flex-wrap` assertion checks `<nav>` instead of the six-control row at `Header.tsx:79`; `header-chrome.ts`'s doc claims "every inactive control" while the profile button still hand-rolls the box; the shell claim counter has no floor and the exported test seam can drive it to -1. **The one `high` WAS this entry itself — see the `last_updated` note — and it was deliberately left unfixed by that round so the finding stayed visible. CLOSED 2026-08-01: the superseded paragraph is deleted and replaced by a record of what it claimed, further down this same row.** **BLOCKING: the owner ruled that the inline `ARCHITECTURE-SPINE.md` edit (`:218`, `:393`) goes through a `bmad-architecture` Update run before 17.1 can be `done`, and the stale `@/lib`/`componentImports` bullet at `:392` should be repaired by that same run rather than a different hand.** Patch items left as action items at the owner's direction, as in rounds 1 and 2. PRIOR ENTRY, 2026-07-31, bmad-dev-story round-2 remediation: **all 25 open review items addressed and verification is COMPLETE.** `npm run build` succeeds; full suite **380 tests / 379 pass / 0 fail / 1 skipped**; `tsc --noEmit` clean; theme-chrome 43/43; public-repo guard 4/4; `npm run lint` 31 problems against 32 before (the difference is the unused import this round deleted), with 4 in the 15 touched files, ALL pre-existing and verified individually against HEAD — zero introduced. Only a browser pass over the four new `dark:` sites is outstanding, and no new colour pair enters the product. **`tsc` caught one real defect that 43 green tests did not** — `ShellDocument` typed the claimed style as `Record<string, string>`, which a real `CSSStyleDeclaration` cannot satisfy, so `claimProjectedShell(document)` failed to compile while every stub-based behaviour test passed; fixed to a mapped type. **SEPARATE DEFECT FOUND AND REPAIRED, NOT THIS STORY'S: `npm ci` has been broken repo-wide since 2026-07-29.** `package-lock.json`'s `integrity` for `set-function-name@2.0.2` disagreed with the registry in one four-character substring — collateral damage from this project's own PII remediation, which replaced real names with invented ones across the whole tracked tree and rewrote one inside a base64 sha512. Nothing noticed for two days because npm is the only thing here that verifies a checksum. All 819 integrity values checked by base64 length (sha512 must be 88 chars; the corrupt one was 89): **exactly one affected** — but that detector CANNOT see a length-preserving rewrite, so the open question is what else that pass rewrote in non-prose data (asset-map, fixtures, any derived value). Repaired to the registry's authentic value; guard still 4/4; integrity checking never disabled. **This needs its own audit entry rather than attribution to Story 17.1.** NOTE, an earlier diagnosis recorded in the story was WRONG and is corrected there: the failure was first read as a live text substitution outside the repository; installing with a cache outside the default disproved that, because a freshly downloaded and hashed tarball produced the same digest — the stored expectation was the corrupt half. AC-4 is rescoped in the AC itself on two axes at the owner's direction (token guarantee = 17.1, shell guarantee = Story 17.7, and the word *previewed* removed because the Live Slide Preview is hub chrome and themes deliberately) — amending an AC is outside this workflow's normal edit surface and is flagged as owner-directed in both the AC and the Change Log. Landed: the four AC-6 sites the round-1 sweep missed; `text-destructive` replacing the hand-rolled red pair in two files (byte-identical to `red-600`/`red-400` in both themes) plus the unused `Button` import deleted; `ArtifactSlide`'s dead `className` parameter deleted so the AC-4 invariant is a compile error; `focus-visible:outline-white` on all three projected focusables (the ring painted from `--ring`, and the clearing measurement had been taken with nothing focused); a scrollable slideshow failure branch with the projector's headline; `ThemeToggle` on a shared `HEADER_CONTROL_BOX` (new `components/header-chrome.ts`) with `cursor-pointer` and `aria-disabled:pointer-events-none`, moved out of the `<nav>` landmark onto a `flex-wrap` row; `disableTransitionOnChange`; a **reference-counted** shell claim extracted to `src/lib/projected-shell.ts` and the cycle to `src/lib/theme-cycle.ts`, both so the restore path and the modulo are exercised as behaviour; the guard file rebuilt (scanner comment stripping, brace-balanced extraction, Tailwind 4 `bg-(--card)` shorthand, arbitrary + inline edge widths, per-branch AC-3, transitive closure with no exempt directory) — **28 -> 43 tests with 18 injected defects each confirmed to make the suite react**; `DESIGN.md` Open Items 4 and 6 repaired and the preview-list Components row corrected; `ARCHITECTURE-SPINE.md` naming **Story 17.7** as the key that closes AD-24's `[ADOPTED, partial]` tag and widening the gap from one hole to four. (A superseded VERIFICATION GAP paragraph was DELETED from this entry on 2026-08-01 — round 3 raised it as this story-round's only `high`. It claimed `npm test`, `tsc --noEmit` and `npm run lint` could not run, that the lockfile was intact, and that the cause was an environment fault for the maintainer. All three are retracted by the rest of this same entry: the runs completed, the lockfile was the corrupt half, and the fault was in the repository. It was a draft left inside the entry that replaced it, in this file, on the row that gates closing the story.) **TWO CLAIMS IN THE ENTRY BELOW WERE ALREADY STALE when this round started and are corrected here:** the "25-file working tree is STILL UNCOMMITTED" — it was committed as `3f210c7`, `git status` was clean; and "17.7 is NOT yet registered in this file or epics.md" — it was, by `116ba3d`, so patch item (b) was half-closed before this pass. PRIOR ENTRY, 2026-07-31, bmad-code-review round 2 (three parallel layers again, launched blind): back to `in-progress` with **24 patch action items left in the story**, 3 deferred, 2 dismissed. The owner resolved all 3 decision-needed items the same day: (1) the AC-4 shell holes are FOUR, not one — first paint on every projected load (a `useEffect` cannot fix it; the leaked paint is the server's), the two Server-Component error branches, `notFound()` at six reachable sites with NO not-found.tsx/error.tsx anywhere in `src/`, and `PROJECTED` never being closed upward — and they close with ONE route-group layout owning every room-facing URL, which is the scope of **new Story 17.7**, not a patch here; (2) 17.1 CLOSES once its 24 patches land, with AC-4 scoped in writing to the token guarantee and the shell guarantee spun out to 17.7 — **17.7 is NOT yet registered in this file or `epics.md`; that registration is patch item (b) and is the single most important one, because a decision recorded without a key is how this project has repeatedly lost findings**; (3) AC-4's contract sentence is amended to exclude operator-side preview, which `SlidePreviewList` now deliberately themes. Verified at review time: tsc clean, theme-chrome 28/28, suite 365/364/1-skipped, exactly one lint problem in any of the 13 changed files (pre-existing). The story's own `baseline_commit: acc8df0` was checked against HEAD — 16 commits apart, all `docs:`, none touching `src/`/`tests/`/`package.json` — so the uncommitted working tree IS 17.1's complete code delta; it is still uncommitted. PRIOR, 2026-07-31 bmad-architecture Update: AD-24 is IN THE SPINE, so all 12 patch findings are closed. Tagged [ADOPTED, partial], because that run's Reviewer Gate opened ONE NEW HIGH finding against this story's change set: the AC-4 shell reset reaches the two full-screen Clients and cannot reach the two room-facing route shells (projector/page.tsx:71, slideshow/page.tsx:76), which are Server Components — so html/body keep the themed background and the reserved scrollbar gutter on the branch a buildSlidePlan throw renders. Filed in the story's Review Findings with NO OWNER; needs a story key before Epic 17 closes. AC-4's token guarantee holds; its shell guarantee does not. PRIOR, 2026-07-31 bmad-dev-story remediation: 11 of the 12 patch findings closed and back to `review`. AC-4 now met — the projector's html/body reset extracted to src/lib/use-projected-shell.ts and called by the slideshow too, verified in the browser and with the defect reproduced first; the slideshow route shell repainted in literal colours and both shells added to the guarded set. Three sub-AA chromatic text pairs fixed (indigo 2.54→9.72, logout 3.76→6.21, emerald 4.23→10.56); the 29 newly-live `dark:` overrides — 18 sites, 8 files, not the 19/9 the review headlined — reviewed and measured, all pass; non-text contrast measured for the first time and filed as DESIGN.md Open Item 6 (--border 1.29:1 dark / 1.26:1 light, light focus ring 2.58:1, no owner). theme-chrome tests rebuilt with comment stripping and every guard negative-tested, 17→28. AC-5 stays structural per the owner and is now filed as EXPERIENCE.md Open Item 4 owned by the new story 17-6. Suite 365/364 pass/0 fail/1 skipped, tsc clean, guard 4/4. STILL OPEN: **AD-24** — the localStorage/root-client-boundary decision needs a `bmad-architecture` Update run, which this workflow is not allowed to substitute for # 2026-08-01 LATEST, bmad-code-review round 4 (three parallel layers, uncommitted tree = the round-3 remediation): **all 15 patch items applied in-session; 1 decision item OPEN, which is why this is `in-progress` and not `review`.** Owner's call on that item: route the stale `ARCHITECTURE-SPINE.md:392` ceiling bullet through a `bmad-architecture` Update run, and 17.1 does not go to `done` until it lands — the same refusal rounds 1 and 3 made, for the same reason. Round 3's own headline recurred a FOURTH time and inside the guards that round rewrote to encode rules: `openingTag` was written for the `<Link>` truncation and not called for the byte-identical one in the `className` guard; the focus-ring exclusion list grew by four spellings and still accepted `outline-transparent` (invisible) and `outline-inherit` (paints from `--ring`); the props reader followed a named type and then read only its first `{ … }`, so `React.ComponentProps<'div'> & { … }` restored a projected `className` with `tsc` clean. Fixed by encoding the rule each time: the props shape resolves transitively and fails loudly on anything not declared locally; `stripComments` models JSX children as a fourth quoting form (`walkJsx` already did, and it runs downstream of the lexer that did not — one URL in room-facing copy was hiding a live `bg-card` + `dark:` + `border-2` from four guards at once); one shared `projectedTree()` answers *what is projected* for both the token guard and the focusable sweep, which had answered it at different widths; the AC-6 pairing splits template arms in `classValues` so a ternary's two arms stop validating each other; the AC-3 surface root descends the single-element chain and reports ambiguity instead of taking the first classed element in source order. Nine defect injections, each green before and failing after, each reverted. Verification: theme-chrome **48/48** (47 → 48, the new test pins the shell reset), public-repo guard **4/4**, full suite **387 / 386 pass / 0 fail / 1 skipped**, `tsc` clean, lint **31** unchanged from baseline with **0** in any changed file. Record fixes: the pinned exception list is **18**, not the 20 claimed in three places; `DESIGN.md` rows 99 and 194; `EXPERIENCE.md` gains a projector State-Patterns row and its focusable claim resized to the assertion. `DARK_VARIANT`'s `dark:!…` / `dark:<digit>…` gap deferred, pre-existing, filed in deferred-work.md.
  17-2-muted-foreground-contrast: done # 2026-08-03 Reviewed: fresh browser measurement and Node 22.23.2 full suite pass; oklch(0.543 0 0) clears all three light hosts; theme-chrome 57/57; DESIGN.md Open Item 1 closed.
  17-3-app-metadata: backlog
  17-4-canvas-dirty-state-guard: backlog
  # 17.5 added 2026-07-30 by bmad-ux Update — the only finding of that run that was
  # a code gap rather than a documentation defect. EXPERIENCE.md had specified
  # "Lost sync" as a shipped state since 2026-07-19; no detection exists.
  17-5-projector-liveness: backlog
  # 17.6 added 2026-07-31 during 17.1's review remediation, at the owner's direction:
  # AC-5's structural-only status needed an owning key rather than a `last_updated`
  # comment. `sonner` is installed, `Toaster` is mounted nowhere, `toast(` is called
  # nowhere — and two artifacts describe toasts as shipped. Its first job is the
  # decision (channel or no channel), not an implementation.
  17-6-toast-channel-decision: backlog
  # 17.7 added 2026-07-31 at the owner's direction, from round 2 of 17.1's code
  # review. It owns AC-4's *shell* guarantee — `html`/`body`, which no component in
  # the projected tree can see — after 17.1 was scoped in writing to AC-4's *token*
  # guarantee. Four paths leak it and no hook can reach any of them: first paint on
  # every projected load (the leaked paint is the server's, so `useLayoutEffect` is
  # not a fix), the two Server-Component error branches, `notFound()` at six sites
  # with no not-found.tsx/error.tsx anywhere in `src/`, and any future route shell.
  # Owner chose one route-group layout over four point-fixes, because only the layout
  # catches the fourth. This story is what takes AD-24 from [ADOPTED, partial] to
  # [ADOPTED], so it carries an `EXPERIENCE.md` IA-table update and a
  # `bmad-architecture` Update run in the same change set, per AGENTS.md.
  17-7-projected-shell-route-group: backlog
  17-8-guard-criteria-encoding: done # 2026-08-03 bmad-code-review: all six patch findings fixed; seventh combined injection compiled under tsc and made the fixed guard fail, then reverted; focused 54/54, public guard 5/5, tsc clean, lint 31 unchanged.
  epic-17-retrospective: optional

  # --- Epic 18: Member data stays gated even when the perimeter moves ---
  # Realizes FR-18 + NFR-6. Kept separate from Epic 17 deliberately: one epic is
  # what an operator sees, this one is what a visitor must never see. Pressure-test
  # watch-list item L4, accepted un-actioned, now coming due.
  epic-18: backlog
  18-1-in-route-authorization-nine-routes: backlog
  epic-18-retrospective: optional

  # --- Epic 19: Liturgical rules live in data, not in the planner ---
  # Created 2026-07-30 at the owner's direction, to give the song-title-skip item a
  # tracked home instead of the bare "Consider" it had carried since 2026-07-29.
  # NOT opened as a Story 15.2: Epic 15 is `done`, and reopening a closed epic is the
  # contradiction Correct Course closed on Epic 14 the day before.
  # MAY BE ABSORBED: spec-artifact-registry-authoring CAP-1 targets exactly the
  # TypeScript plan constants this epic moves. If that SPEC is adopted, fold this in
  # rather than deliver it twice.
  # RETIRED 2026-07-30 by owner decision (AD-20). Goal met, method discarded: the three
  # skipTitle songs (#671, #684, We Have This Hope) become hand-edited General registry
  # entries, and a General generates no title slide — so skipTitle is DELETED, not moved
  # into data. Nothing to migrate, no flag to store. Work happens in Story 20.1 seed.
  epic-19: retired
  19-1-song-title-suppression-as-data: retired
  epic-19-retrospective: n/a # nothing was delivered to retro

  # --- Epic 20: The registry becomes where the deck is authored ---
  # Created 2026-07-30. Owner decision: spec-artifact-registry-authoring is the final
  # change, adopted whole. That SPEC is marked "Canonical contract", declares it
  # supersedes Story 16.1 non-goals and that "this SPEC wins" over conflicting Epic 16
  # companions — and until now was referenced by NO epic, story or sprint key. An
  # artifact claiming authority over shipped code with no delivery status is the
  # inverse of the 2026-07-29 drift: there code ran ahead of docs, here docs ran ahead
  # of code and nothing recorded that it was waiting.
  #
  # TWO BREAKING CONSEQUENCES, both the SPEC's explicit instruction:
  #  (a) seven base types collapse to three kinds (General/SongSet/Announcement).
  #      SPEC Constraints retires TextPlaceholder/ImagePlaceholder/MixPlaceholder/
  #      FullScreenImage by name. Placeholders stop being a kind and become elements
  #      inserted onto a General from the Placeholder Catalog (CAP-4).
  #  (b) CAP-6 REVERSES AD-14 (registry edits are global and immediate, no
  #      per-service override by design). Story 20.8 must not ship before the epic-16
  #      architecture spine carries a new AD-n superseding AD-14 — never renumber — and
  #      EXPERIENCE.md Venue & Projection Constraints + Flow 5 are reconciled.
  #
  # Epic 19 is a SUBSET: story 19-1 moves the skipTitle literals, and CAP-1 forbids
  # exactly those "TypeScript plan constants". Deliver inside 20.1 or retire Epic 19.
  # Migration is cheap only while no production exists — after deploy the same change
  # needs a backfill over live artifact_templates rows and every service snapshot.
  epic-20: backlog
  20-1-ordered-registry: backlog
  20-2-three-slide-kinds: backlog
  20-3-add-delete-rename-reorder: backlog
  20-4-general-canvas-authoring: backlog
  20-5-placeholder-catalog: backlog
  20-6-announcement-expands: backlog
  20-7-songset-slots: backlog
  20-8-service-clone-and-sync: backlog
  epic-20-retrospective: optional

  # --- Epic 21: Scripture is on hand, in the translation being read ---
  # Created 2026-08-01 by Correct Course. No architecture gate: it seeds empty
  # tables from zero, so AD-21 does not reach it. No Epic 20 dependency either —
  # startable immediately, in its own worktree. Realizes FR-19 (the last product
  # Partial) and the new FR-22.
  epic-21: in-progress
  # done 2026-08-01: data/bible/kjv.json committed (4.36 MB) and seeded from zero
  # on first boot. Completeness asserted structurally in tests/corpus.test.mjs —
  # 66 books / 1,189 chapters / 31,102 verses — not sampled. The .work/tp_bible_*
  # export was deleted only after that assertion was green, in that order.
  # PATH SUPERSEDED 2026-08-01 by FR-24, after this story closed: the corpus moves
  # to data/en/bible-translation/kjv.json. This story's AC names data/bible/kjv.json
  # and is left standing as the record of what shipped — Story 21.2 performs the
  # move. Superseded in writing (story file + epics.md), never silently rewritten.
  21-1-verse-database-ships: done
  # AMENDED 2026-08-01 (FR-24): also performs the data/<locale>/bible-translation/
  # path move, the translation -> translation_code rename, and isKjvCorpusEmpty() ->
  # isBibleTranslationEmpty(code). Never-filter rule binds: listing endpoints return
  # every installed translation with its locale; no WHERE locale = <default> reaches
  # the database. 'KJV' literal survives at scripture.ts :6 :106 :128 :144 (measured).
  # STORY FILE 2026-08-01: stories/21-2-translation-is-a-parameter.md. Two things the
  # epic text understates and the story now owns, both assigned by the spine's
  # Deferred by story number: the AD-25 RECONCILE for the bible family (the
  # seed's early return goes, removal arrives, a bad file reconciles nothing) and
  # the GUARD that AD-25's closure and AD-26's never-filter rule both rest on.
  # Two corrections against measurement: the 503 clause the epic asks for was
  # already done by 21.1, and three operator-facing KJV labels were uncounted
  # (PresenterOperator.tsx:692 is assigned to this story by EXPERIENCE.md:45;
  # CreateForm.tsx:581 / EditForm.tsx:612 are a recorded scope call).
  21-2-translation-is-a-parameter: done
  # AMENDED 2026-08-01 (FR-24): owns default_bible_translation AND the shared
  # default_data_locale. The translation control opens on the default locale and
  # carries an always-present way to browse the others. settings.ts is the
  # append-shaped contact point with Story 22.3 — one const + getter/setter each.
  21-3-default-translation-and-presenter-choice: backlog
  # NEW 2026-08-01 (FR-24). Book names ship inside each translation's corpus.
  # SUPERSEDED THE SAME DAY by the input-model Correct Course (third run of the
  # day): input is NOT generous. Search is scoped to the CHOSEN translation on
  # operator surfaces; only the rundown matches all installed translations,
  # because a Telegram sender picks none. ONE matcher, scope as a parameter.
  # Longest-prefix match over corpus-supplied names replaces the regex, so the
  # two-word cap and the hyphen stop being concepts rather than being widened.
  # BOOK_ALIASES is REPLACED, not deleted: non-prefix aliases (Jn, Mt) live in
  # the matcher and BELONG TO A TRANSLATION, so Kej must not resolve under KJV.
  # shortName dropped - 6 sites, measured. Crosses an epic boundary:
  # parser.ts:152/:162 carry a second copy of scripture.ts:42's regex, and one
  # rule may not have two implementations (spine Boundaries convention).
  # The canonical-identity SHAPE is the bmad-architecture Update's, not this story's.
  21-4-book-names-belong-to-the-translation: backlog
  # NEW 2026-08-01 (FR-24), the operator half of 21.4 - client only, so the two
  # run in separate worktrees over an append-shaped contact point (the matcher's
  # suggestion endpoint). One field with inline autocomplete, NOT a book picker
  # plus chapter/verse fields: the owner chose this over the recommendation, and
  # it is why paste survives and why the rundown needs no second mechanism.
  # Accepting a suggestion leaves the FULL corpus name in the field, which is
  # what pays for dropping shortName. Precedent HymnNumberAutocomplete.tsx
  # (4 mounts in CreateForm.tsx, 4 in EditForm.tsx) - copy its debounce, settled
  # cache and in-flight dedup, not merely its shape. No proxy.ts change:
  # /api/scripture and /api/hymns carry no exclusion, so a sibling suggestion
  # endpoint is gated by default and proxy-matcher.test.mjs is untouched.
  21-5-one-field-inline-autocomplete: backlog
  epic-21-retrospective: optional

  # --- Epic 22: The song book is a choice, and its titles are real ---
  # 22-2 is BLOCKED on a bmad-architecture Update: no AD governs a shipped
  # reference corpus, and correcting 695 persisted titles is AD-21 case work
  # whose counter does not exist (spine :374). It also needs the owner-supplied
  # number-to-title list — see action_items.
  # 22-3 is GATED on Story 20.7: a per-song book qualifier hangs off the binding
  # AD-19 governs, and AD-19 DELETES song1Number..song4Number rather than
  # aliasing them, so building it earlier ships four fields Epic 20 then removes.
  epic-22: in-progress
  # done 2026-08-01: corpus moved to data/song-book/sdah.json carrying book.code,
  # book.attribution and the takedown statement; hymns rebuilt as
  # UNIQUE(book_code, number) with a one-time boot migration recording existing
  # rows as SDAH; import:hymnal RETIRED (its .work/lirik-lagu.json source does not
  # exist), replaced by corpus:verify. The committed file is now the source of record.
  # PATH + COLUMN SUPERSEDED 2026-08-01 by FR-24, after this story closed: corpus
  # moves to data/en/song-book/sdah.json and book_code is renamed song_book_code.
  # This story's AC names the old spellings and is left standing as the record of
  # what shipped — Story 22.3 performs both. The UNIQUE(..., number) constraint,
  # the attribution and the retired importer are unaffected.
  22-1-song-book-ships-with-book-code: done
  # done 2026-08-01: all 695 titles replaced from the owner-supplied index, which
  # arrived the same day and cleared the blocker action_items recorded. Fixed in
  # the corpus, not hand-patched per row.
  #
  # The architecture gate was NOT bypassed and is NOT closed. Story 22.2 shipped
  # without an AD because it introduced no channel: upsertHymns already overwrote
  # title and lyrics from the corpus on every boot, so corrected titles propagate
  # through the path that was already there. What the bmad-architecture Update
  # still owes is whether that every-boot channel should SURVIVE for a shipped
  # reference corpus — see action_items. If the answer is AD-21's counter, the
  # migration target is upsertHymns, not this data.
  22-2-hymn-title-is-a-title: done
  # AMENDED 2026-08-01 (FR-24), and the amendment is most of the story now: owns
  # default_song_book AND reads the shared default_data_locale; performs the
  # data/<locale>/song-book/ path move and the book_code -> song_book_code rename
  # superseding Story 22.1; makes the EIGHT unqualified FROM hymns read sites
  # book-aware (the ninth, db/index.ts:47, is the boot migration, not a read site).
  # Never-filter case that must work: an Indonesian service singing one English
  # hymn — no setting changes. STILL GATED on Story 20.7 (AD-19 slot identities).
  22-3-default-song-book-and-per-song-override: backlog
  epic-22-retrospective: optional

  # --- Epic 23: A fresh clone runs ---
  # Neither corpus family owns this; kept separate so scripts/, tests/ and docs/
  # are not under two epics at once.
  #
  # Sequence: 23.1 first. Story 23.2 then waits on both 23.1's demo seed and
  # Story 22.3's move from data/song-book/ to data/<locale>/song-book/. Story
  # 22.3 remains gated on Story 20.7.
  epic-23: in-progress
  23-1-opt-in-demo-seed: done
  # Partial work delivered 2026-08-01: tests/corpus.test.mjs rejects retired
  # import commands, data/hymns.json, and data/bible/ in reader-facing tracked
  # files. The FR-24 data/song-book/ criterion remains open until Story 22.3
  # performs that path move. The fresh-clone end-to-end assertion also remains
  # open until Story 23.1 supplies npm run seed:demo. No Story 23.2 file exists,
  # and no implementation is active.
  23-2-fresh-clone-verified: backlog
  epic-23-retrospective: optional

  # --- Epic 24: The interface speaks the operator's language ---
  # Created 2026-08-01 by the SECOND Correct Course of the day. Realizes FR-25
  # (UI Locale). Deliberately separate from Epics 21/22, which carry the DATA half
  # (FR-24) of the same day's locale work: the two halves share a word and nothing
  # else — no table, no module, no test, no file. This epic touches no corpus file
  # and no corpus table; Epics 21-22 touch no .tsx string. Own worktree, no gate.
  # Measured 2026-08-01: NO i18n infrastructure exists — lang="en" hard-coded at
  # src/app/layout.tsx:31 is the entirety of it. 39 .tsx, 26 client components,
  # ~55 JSX/attribute literals + ~158 src/lib and src/app/api messages (many
  # dev-facing). Estimate 100-150 real translatable strings.
  # in-progress 2026-08-01: first story of the epic written (bmad-create-story).
  epic-24: in-progress
  # Infrastructure half: string catalogue, ui_locale setting on the shipped
  # per-concern pattern, switcher, <html lang> following the setting. An
  # UNRESOLVED KEY MUST BE VISIBLE AS A DEFECT, never rendered blank. ui_locale is
  # the fourth of FR-24's four settings keys and the only one this epic owns.
  # STORY FILE 2026-08-01: stories/24-1-string-catalogue-switcher-and-lang.md.
  # Four mechanism decisions 24.2 inherits 100-150 times, so they are ACs: the
  # catalogue is CODE not database rows (AD-25 excludes it from the corpus channel
  # by name); NO second root-level client provider (server-read state travels as an
  # initial* prop, and AD-24's Deferred already holds that question open for Story
  # 17.6); a missing key never falls back to English, with key-set parity asserted
  # so the sweep adds keys in pairs; and the UI-locale vocabulary is NOT the
  # data-locale vocabulary — catalogues ship with the build, corpora are installed.
  # Own component, not a shared language card: EXPERIENCE.md:73 says per-concern,
  # and a shared card is a file Stories 21.3/22.3 would each have to rewrite.
  24-1-string-catalogue-switcher-and-lang: done
  # The sweep. Includes the slide-plan.ts operator-facing labels ('Welcome',
  # 'Opening Song', 'Congregation, please stand', 'Prayer Partners', 'Break Time')
  # — verified 2026-08-01 they populate LegacyProjection (slide-plan.ts:76), read
  # ONLY by present/presenter-model.ts and SlidePreviewList.tsx; pptx.ts and the
  # projector read it zero times. OUT OF SCOPE and stated so the drive-by fix is a
  # decision: slide-plan.ts:261 hard-codes 'Bandung International Community', a
  # church name in code — not a translatable string, different capability.
  24-2-the-strings-move: backlog
  epic-24-retrospective: optional

action_items:
  - epic: 14
    action: "Harden BMAD gate in AGENTS.md (+ sync .agents/AGENTS.md, .cursorrules, CLAUDE.md): forbid large feature work without Epic→Story→Spec; call out Antigravity/Google AI Pro jump-to-code bias"
    owner: "kodesh87"
    status: done
  - epic: 14
    action: "Commit and push worship web input CR patches"
    owner: "kodesh87"
    status: done
  - epic: 14
    action: "Extract fat api/services* handlers to src/lib/*"
    owner: "Developer"
    status: done
  - epic: 14
    action: "Optional: reduce hymnIndex HTML payload if needed"
    owner: "Developer"
    status: done
  - epic: 16
    action: "Inspect a generated deck and the projector BEFORE FIRST USE (registry now owns layout; only order/content are machine-verified). Reframed 2026-07-29: the owner confirmed there is no production deployment yet, so this is a pre-launch gate, not a pre-Sabbath one. The readiness report ranked it the single highest-consequence open item on the premise that a live service depended on it — that premise was wrong, and the urgency drops accordingly. The check itself does not: nobody has looked at a projector since Epic 16 moved layout ownership into the registry"
    owner: "kodesh87"
    status: open
  - epic: 16
    action: "Reset welcome, verse-reading, special-song, family-youth, bible-verse-contemplation on the FIRST DEPLOYED DB, if its rows predate the current seed. Reframed 2026-07-29: the owner confirmed there is no production deployment, so there is no stale production row to reset today — this becomes a first-deploy check rather than an outstanding repair. Diagnose it with `npm run registry:doctor` against the deployed DB_PATH; it reports per template what the seeder would do. NOTE (corrected 2026-07-29): the 'missing-only seeding' premise is obsolete — src/lib/registry/seed.ts now self-heals at startup via reseedArtifactTemplateIfUntouched, and src/lib/db/index.ts backfills seed_hash once for pre-column rows. A rebuild+restart re-seeds any row still provably untouched; only rows reported 'kept (edited by an administrator)' or 'kept without a recorded seed hash' need a manual Reset. Read the [registry] seed lines in the startup log to see which. Manual path: /admin/artifacts editor Reset button, or POST /api/admin/artifacts/<id>/reset with {updatedAt}"
    owner: "kodesh87"
    status: open
  - epic: 16
    action: "Add a seed conformance test: every declared placeholder bound to exactly one element, every planner template id present"
    owner: "Developer"
    status: done # 2026-07-29 tests/registry-seed-conformance.test.mjs (6 assertions). Covers the direction validate.ts does not: a declared-but-unbound placeholder, whose value is computed then never rendered — the failure pptx-content.test.mjs records for the verse-reading citation, special-song performer and welcome date. Also asserts no layout binds one placeholder twice, and every literal templateId in slide-plan.ts exists in the seed
  - epic: 16
    action: "Add a ceiling assertion for deck bytes / generation time"
    owner: "Developer"
    status: done # 2026-07-29 tests/pptx-ceiling.test.mjs. Measured baseline: 53 slides, ~4.2s, 29.99 MB. Ceilings 120 slides / 30s / 45 MB — regression signals with headroom, not NFR-2 breaches. Actuals print on every run so drift is visible before the assertion trips. NOTE: 30 MB is dominated by full-bleed background images, not slide count; it is the file an operator downloads before a service (NFR-1)
  - epic: 16
    action: "Repair the two pre-existing stale checks in scripts/smoke-deck-fidelity.mjs left from the Epic 14 field renames"
    owner: "Developer"
    status: not-reproducible # 2026-07-29 ran npm run build + node scripts/smoke-deck-fidelity.mjs: 30 checks, 30 PASS, 0 FAIL, exit 0. Includes the seven static source greps and the EditForm field check the Epic 14 renames would have broken. Either the checks were repaired before this run or the readiness-report finding was wrong. Nothing was changed — there was no failing check to repair. Re-open with the failing check name if it resurfaces
  - epic: 15
    action: "Consider moving the hardcoded song-title skip rules out of slide-plan.ts now that the Artifact Registry exists"
    owner: "Developer"
    status: done # 2026-07-30 at the owner's direction: converted from a "Consider" into Epic 19 / story 19-1-song-title-suppression-as-data, with the three slide-plan.ts call sites and their liturgical reasons tabulated in epics.md. The item is closed as an ACTION ITEM, not as work — a bare "Consider" owned by "Developer" is how a decision stays unmade, so it now lives where sprint planning can see it. NOT reopened as Story 15.2: Epic 15 is done

  # --- Added 2026-07-29 by Correct Course (sprint-change-proposal-2026-07-29.md) ---
  - epic: 16
    action: "Operator tooling: scripts/registry-doctor.mjs (npm run registry:doctor) reports what the seeder would do to every template on a given DB_PATH, and applies only the resets startup will not do itself. Reuses the app's own serializeTemplate/hashTemplatePayload/resetArtifactTemplate so it cannot drift from the behaviour it describes"
    owner: "Developer"
    status: done # 2026-07-29 user-requested tooling. Exists because the 'reset five template rows' item was written against insert-missing-only seeding and could not tell an admin-edited row (must not be reset blindly) from an untouched one (startup fixes it)
  - epic: 14
    action: "Assert that the create/edit form preserves operator-chosen Announcement List order (FR-11b). The date-collision override is already covered by tests/services-lib.test.mjs:161"
    owner: "Developer"
    status: done # 2026-07-29 tests/services-create.test.mjs — 'announcement sync preserves operator-chosen order, including a reorder'. Asserts the reorder case the prior coverage missed (its inputs were already in stored order, so a position-ignoring sync stayed green), plus distinct ascending sort_order so slide order cannot be arbitrary. Ties FR-11b to FR-6 'in list order'
  - epic: null
    action: "Route UX findings F4-1 (protagonists contradict PRD: Sari/Bimo/Elen are authoritative), F4-3 (UJ-3 has no flow), F4-4 (UJ-5 demoted to a branch), F4-5 (FR-16 blanking / NFR-5 unmapped-input channel / NFR-2 progress state have no UX owner), F4-6 (congregation-facing deck has no UX document) through bmad-ux Update. ADDED 2026-07-29: DESIGN.md open item 2 states the dark palette is unreachable dead code. Verified false — PresenterOperator.tsx:449 and SlideGridDialog.tsx:176 pin the class themselves, so the palette renders today in the two surfaces used during a service. Left for bmad-ux rather than corrected in place, since that skill owns the file; the correction is recorded in story 17-1 so the implementer is not misled meanwhile"
    owner: "kodesh87"
    status: done # 2026-07-30 bmad-ux Update ran. SCOPE WAS WRONG WHEN WRITTEN: the UX .memlog.md already recorded F4-1, F4-3, F4-4 and F4-5 as closed by the 2026-07-29 run (protagonists renamed to Sari/Bimo/Elen with UJ labels; flows expanded 5→7; FR-16 blanking + NFR-5 + NFR-2 added as states), and Correct Course itself closed F4-7 by adding EXPERIENCE.md to epics.md inputDocuments. This item re-listed work already done because it was written from the readiness report rather than from the memlog. Actually delivered 2026-07-30: dead-code claim corrected in DESIGN.md; F4-6 closed as a stated ownership boundary in both spines (no readability standard invented); all four per-surface states verified against src/ — three shipped, "empty plan" unreachable by construction, "lost sync" NOT shipped → new story 17-5; every Open Item in both spines now names its owning story key
  - epic: null
    action: "Close or waive the three unrecorded Phase-1 go/no-go spikes (PRD §6): font proven on a clean machine, church fidelity sign-off on a sample slide set, 5-10 historical Rundown corpus"
    owner: "kodesh87"
    status: partially-done # 2026-07-29 owner decision: the two gates requiring the church are WAIVED and will not be sought. Recorded in PRD §6 with the risks carried rather than closed — fidelity is unvalidated by the people who will see it (an adoption risk no test can detect; the pre-launch projector inspection is the compensating control), and the parser stays fit to one sample, so the first unfamiliar rundown is where real variance gets measured. The font-on-a-clean-machine gate is technical and remains open with the maintainer
  - epic: null
    action: "Create the epic and stories for the readiness-report product defects"
    owner: "kodesh87"
    status: done # 2026-07-29 owner asked for the dark-mode work to go through an epic/story. Delivered as TWO epics rather than one, because C5-1's remediation applies the user-value standard to new epics: Epic 17 is what an operator sees (dark mode, muted-foreground contrast, app metadata, canvas dirty-state guard), Epic 18 is what a visitor must never see (in-route authorization for the nine proxy-only routes; FR-18 + NFR-6). Stories 17.1 and 17.8 have full story files; 17.8 is now at `review` and the remaining planned stories are `backlog`. Epic 17 records its own requirement ancestry as a DECISION — operator-chrome visual identity is DESIGN.md-governed, not FR-governed — rather than leaving the silence FR-20 had to be added to fix
  - epic: null
    action: "SUPERSEDED 2026-07-29 — merged into the single purge item below. The history rewrite covered the F4-2 names and the second-instance names in one operation, and one Support ticket covering all eight pre-rewrite SHAs settles both. Two separate items would have implied two tickets"
    owner: "kodesh87"
    status: done
  - epic: null
    action: "SECOND PII instance, found 2026-07-29 by widening the F4-2 sweep from _bmad-output/** to the whole tracked tree: ten real names across data/asset-map.json (slide-56 evidence — a family surname, three given names AND their prayer request, verbatim from the source deck), tests/fixtures/sample-rundown.txt (seven role-holders), docs/QUICKSTART.md, docs/picoclaw-webhook.md, scripts/smoke-deck-fidelity.mjs, tests/parser.test.mjs, tests/pptx-content.test.mjs. Working tree remediated: all replaced with invented names, prayer-request body redacted, twelve fingerprints added to FORBIDDEN_NAME_HASHES. The count grew from ten to twelve during verification: one given name turned out to belong to a real predecessor rather than to the invented brainstorming persona it resembled, and the sermon speaker's full name was found glued into a single token in asset-map.json. Three names are deliberately left unblocked, for the reason the guard already documents: one is a word in 182 hymns and in 'Amazing Grace', one is a figure in the Bible, and one predecessor's given name is also a book of the Bible — that person's surname is what identified them, and the surname is blocked. Verified: git grep -w finds none, guard 4/4, npm test 332 pass / 0 fail, smoke 30/30"
    owner: "kodesh87"
    status: done # working tree only
  - epic: null
    action: "RESIDUAL of the second PII instance: history rewrite. DONE 2026-07-29 — all 12 commits rewritten with git filter-branch (filter-repo is not installed and no external tool was downloaded) and force-pushed with --force-with-lease. origin/main moved 2dd8069 -> a9277ff; every SHA changed. Four passes were needed, and the reason each was needed is the finding: pass 1 the ten known names; pass 2 two more REAL role-holders — the volunteer who built the deck and one predecessor — still present in Initial commit even though the F4-2 remediation reported the history clean, one of them also surviving at HEAD in the brainstorming transcript where it had been mistaken for an invented persona; pass 3 the family surname quoted in abbreviated form in deferred-work.md, glued into one token so no word-boundary sweep can see it; pass 4 the sermon speaker's real FULL name, glued as <Given><Surname> in asset-map.json slides 40 and 50 — a fourth real person, found only while verifying. Verified by replaying the guard's own fingerprint logic over all 603 unique text blobs reachable from main: zero hits on any of its 26 fingerprints. No tracked document cited any of the eight rewritten SHAs, so no reference repair was needed"
    owner: "kodesh87"
    status: done
  - epic: null
    action: "Residual of the history rewrite: GitHub keeps the pre-rewrite objects and serves them by direct SHA until it garbage-collects on its own schedule"
    owner: "kodesh87"
    status: waived # 2026-07-29 owner decision: no Support purge ticket will be filed; GitHub's own garbage collection is accepted as sufficient. What that accepts, stated plainly so it is a decision and not an oversight: the eight pre-rewrite commit SHAs were public while they were reachable, so anyone who noted one can still fetch that commit — including the family name and prayer request — until GC runs, and GitHub does not publish that timing. A clone or fork made before the rewrite is out of reach either way. The local rollback (refs/original plus the verified 36 MB bundle in the session scratchpad) can be dropped whenever you are satisfied: `git gc --prune=now` and delete the bundle
  - epic: null
    action: "Re-infection vector: scripts/extract-pptx-assets.mjs build-map regenerates data/asset-map.json from the real deck, and its `evidence` field is the slide's own text runs — so re-running it reintroduced real member names and prayer requests. Closed by filtering at the generator rather than by blocking afterwards"
    owner: "Developer"
    status: done # 2026-07-29. `evidenceFor` now reads the distinction out of the registry instead of a hand-kept list: a template with no placeholders is chrome and its runs stay (that is how the Part C plates were audited); a template that declares placeholders may contribute only runs matching copy it declares in an element `content`. Matching is whitespace-collapsed, case-insensitive, substring-wise, because one declared two-line label arrives as two separate runs. Withheld runs are COUNTED, not silently dropped, so a filtered row stays distinguishable from an unchecked one. Applied to the committed map offline (the deck is gitignored, so build-map cannot run in CI): 7 rows changed — family-youth keeps its two labels and withholds the name plus prayer body, closing-prayer-ds keeps Closing/Prayer and withholds the speaker, sermon/song-set/verse-reading/bible-verse-contemplation withhold everything. Asserted by tests/asset-map-evidence.test.mjs (4 assertions, stated independently of the generator so it cannot agree with the bug). Negative-tested: injecting one payload run fails two of the four. Rule written into AGENTS.md, .agents/AGENTS.md, .cursorrules and .constitution/public-repository.md — the three agent files verified still identical in their shared blocks

  # --- Added 2026-07-30 with Epic 20 ---
  - epic: 20
    action: "BLOCKS Story 20.8. Amend the epic-16 architecture spine via bmad-architecture Update: CAP-6 reverses AD-14, which this item called AD-4 because the Epic 16 child spine still existed (registry edits are global and immediate; no per-service override, by design) because a service must hold a cloned snapshot that only Sync Artifact refreshes. Add the next AD-n superseding it — never renumber an existing one — then reconcile EXPERIENCE.md (Venue & Projection Constraints states the global-and-immediate rule, and Flow 5's climax turns on it) in the same change set"
    owner: "kodesh87"
    status: partially-done # 2026-07-30 bmad-architecture Update ran on architecture-epic-16. SPINE HALF DONE, EXPERIENCE.md HALF STILL OPEN — the item asked for both in one change set and only one landed, so it is not closed. Delivered: AD-16 supersedes ONLY the "global across services" clause of AD-14 (admin-only authorization and read-through-server-side-modules clauses stand; AD-4 LiveServer durable paths is a different decision and untouched). Owner decided Sync is permitted on any service including one already presented, and CORRECTED the rationale on 2026-07-30: the freeze event is service CREATION (a service is self-contained against the registry from adoption), and what is frozen is the supporting data entry — sermon speaker, sermon flyer, hymnal number per slot — not the generated deck. So there is no historical-fidelity cost to accept; three deliberate non-goals follow instead: Sync is unrestricted, Announcement membership stays live from the master list, and a later structural change need not keep an old snapshot renderable. Three more decisions landed because the gate found them: AD-17 (seed is bootstrap-once — resolves the previously unrecorded AD-11 vs CAP-2 collision where a deleted row left no evidence and was resurrected every boot via insertArtifactTemplateIfMissing), AD-18 (value migrations are explicit and marker-gated, precedent artifact_seed_hash_backfilled; the seven-to-three base_type collapse is a total replacement with no incremental migration under an owner waiver that EXPIRES AT FIRST DEPLOY), AD-19 (a cross-boundary key is a server-owned value the administrator cannot edit — closes the SongSet-slot-to-hymn-number binding and the Placeholder Catalog key set, which both reviewer lenses found independently). AD-19's SongSet half uses the OWNER'S mechanism, decided 2026-07-30 and simpler than the reviewer's proposal: the binding key is the slot's own immutable identity carried by the type vocabulary (songset-bt-open / -bt-close / -ds-open / -ds-close), so identity is immutable by construction and there is no separate identity mechanism. Kind vocabulary stays exactly the three the SPEC fixes (general, song-set, announcement); text-placeholder / image-placeholder / mix-placeholder / fullscreen-image are GONE not renamed, and only song-set expands. Whether the slot identities live in the base_type column or a discriminator beside it is DELIBERATELY LEFT to Story 20.2/20.7 — the spine fixes the invariant, not the schema. Consequence for gap 4: authoring-boundaries.md line 18 offers "Edit Label (and optionally baseType)", which AD-19 forbids for a slot-carrying type — a contradiction inside the new SPEC itself. This also removes Story 20.7's stated "central design decision" from story altitude. Also fixed: AD-6 (optimistic concurrency) was missing from the Inherited Invariants table while Epic 16 already shipped expectedUpdatedAt/RegistryStaleError, and AD-16 made that omission load-bearing since Sync is a service mutation. Reviewer Gate ran sequentially, reviews in archived/architecture-epic-16/reviews/*-epic-20.md. TWO ESCALATIONS TO THE PARENT INITIATIVE SPINE REMAIN, both one-line fixes awaiting the owner: (1) its State convention "services are immutable once presented" MISLOCATES THE FREEZE and is factually wrong, not merely narrow — the owner corrected this on 2026-07-30 and the epic-16 Inherited Invariants table now carries the correction as an escalation row; (2) AD-9's Binds names the bootstrap path where AD-18 operates, so AD-18 could be read as a child decision on parent-owned ground. Item (2) also wants a sync of project-context.md line 85. ARCHITECTURE CONSOLIDATED 2026-07-30 by owner directive, after these notes were written: BMad's default is one spine per project, so the Epic 16 child spine was FOLDED INTO the project spine and its AD-1..AD-9 renumbered to AD-11..AD-19 (AD map published in both files; AGENTS.md never-renumber rule waived once by the owner, recorded as a non-precedent in all three gate files; 36 live citations repaired). The two escalations above therefore stopped being cross-document conflicts and are now Open items INSIDE the one spine, which is where they get settled. Every AD number cited in this file is the post-merge number. SUPERSEDED IN ONE DETAIL 2026-07-30 (later the same day): where this note says AD-18 makes value migrations "marker-gated, precedent artifact_seed_hash_backfilled", that mechanism is gone. AD-21 replaces the per-change boolean with ONE global monotonic data version counter in `settings`, declared while the change is coded as version n -> n+1; unreleased transitions compact into a single transition before release and developer databases are reset to it, while a released version is frozen. Read AD-21 before writing any migration. Also added the same day: AD-22 (free canvas is General's alone; a songset row gets a bounded configuration surface) and the closed six-key recognized set in AD-19
  - epic: 20
    action: "Decide Epic 19's fate when Story 20.1 is planned: deliver story 19-1 inside 20.1 or retire Epic 19. CAP-1's success criterion forbids exactly the TypeScript plan constants 19-1 moves, so shipping both means doing the same work twice with two owners"
    owner: "kodesh87"
    status: done # 2026-07-30 RETIRED — and by a route neither option in this item anticipated. The owner decided the three suppressed songs (#671, #684, We Have This Hope) become hand-edited General registry entries rather than data-driven song entries. A General generates no title slide, so skipTitle is REMOVED not migrated: there is no flag left to store anywhere, so there was never a second implementation to avoid. Recorded as architecture AD-20 — the planner injects nothing the registry did not ask for; buildSlidePlan applies rules and does not hold them. Epic 19 goal met, method discarded; epic-19 and 19-1 set to retired. ALSO CORRECTED the same day while grounding the decision in source: epics.md named slide-plan.ts:460 as "Around the Special Song" — the source shows group intercessory-684, and NO skipTitle site touches the Special Song at all. That table was the only record of these three sites and Story 20.1 was told to rely on it
  - epic: 20
    action: "Gap 4 — bmad-spec Update. Three notes AD-19 shed when the architecture spine was trimmed to Binds/Prevents/Rule on 2026-07-30; an initiative-altitude spine does not carry story-level reconciliations, but nobody should have to rediscover them: (1) authoring-boundaries.md offers 'Edit Label (and optionally baseType) in the slide inspector', which AD-19 forbids for a slot-carrying identity — a contradiction inside the new SPEC itself; (2) slide-kinds.md describes SongSet as 'One row per song slot' without saying that the slot's IDENTITY is what the hymnal binding hangs on; (3) placeholder-catalog.md defers key spelling ('Exact key names follow implementation naming') and lists a mix of sermon.speaker, familyPrayerRequest, serviceDate — free while the catalog was a UI list, but under AD-19 the key is server-enforced and persisted into saved layouts, so it is chosen once (Story 20.5) and is an AD-18 / AD-21 versioned-migration concern after that. TWO MORE for the same pass, added by the 2026-07-30 owner session: AD-19 now states the recognized entry set is CLOSED at six keys over three kinds (general, songset-bt-open, songset-bt-close, songset-ds-open, songset-ds-close, announcement) and that bare song-set names the kind but is never an entry key — so any SPEC text implying a single SongSet row needs reconciling, and the seed's one song-set row becomes four; and AD-22 is new, fixing that free canvas is General's alone while a songset row gets a bounded configuration surface (two background images, font style, font size) with layout composition developer-owned"
    owner: "kodesh87"
    status: done # 2026-07-31 bmad-spec Update on spec-artifact-registry-authoring. All five routed notes closed, plus THREE cross-document findings the 2026-07-30 review explicitly folded into this same pass and this item did not name — F-7 (placeholder-catalog.md:8 admitted catalog placeholders on ANY slide, contradicting both AD-22 and its own :34), F-24 (authoring-boundaries.md gave the SongSet surface as the bare word "backgrounds", so Story 20.7 would have shipped with no font controls; and claimed Presenter/PPTX read a service snapshot directly, which AD-12/AD-16 forbid), F-25 (slide-kinds.md left Announcement membership per-service where AD-16 keeps the master list live). Eight items, not five. Landed: authoring-boundaries.md drops the editable baseType from the inspector step and gains a "What the administrator may not edit at all" section — kind, slot identity and catalog key are server-owned, set at creation, closed at six keys over three kinds; slide-kinds.md now leads with the four slot IDENTITIES as the binding key and names the three consequences (reorder and rename cannot touch a binding, delete leaves it inert with the entered hymn number surviving per AD-16); placeholder-catalog.md retires the "exact key names follow implementation naming" deferral (chosen once in Story 20.5, an AD-18/AD-21 migration concern after that) and narrows insert to General rows only. SPEC.md kernel took three minimal edits — CAP-2 success gains the claim-an-unclaimed-slot clause, CAP-5 success gains "the label is the only part an administrator edits", one new Constraint states the cross-boundary-key rule; Constraints 8 -> 9, CAP-1..8 unchanged and nothing renumbered. TWO THINGS WORTH KNOWING. (1) One consequence the review did not name: narrowing :8 to General-only would have left hymnNumber / songTitle / lyric slots / announcement image slots standing in an ENFORCED admitted key set that only a General may insert and no General can carry — they are removed as system-expansion internals rather than left as a new contradiction. (2) One DERIVED reading needs owner ratification: AD-19 fixes that the set is closed, that at most one row carries each slot identity, and that a deleted slot leaves an inert binding — it does NOT say in so many words that adding a SongSet row means claiming an unclaimed identity. That is the only reading under which CAP-2 keeps its add verb and AD-19 keeps its closed set, so it is written into SPEC.md and both companions and logged as an assumption at .memlog.md, flagged rather than quoted as spine text. Self-Validate PASS both passes. STILL OPEN and now the blocking pair for the same corpus, both outside this spec folder: review findings F-4 and F-10 against ../spec-slide-artifact-model/SPEC.md — an ADOPTED COMPANION of the spec just fixed, which Stories 20.1 and 20.3 read — where :64 still presents the missing-only seed as live (AD-17 reversed it), :65 still presents templates as global across services (AD-16 reversed it), CAP-8 still requires a TextPlaceholder (AD-19 abolished the kind), and the supersession note at :16 enumerates only two reversals, which implies the rest still binds. That is a second bmad-spec Update, on a different spec folder, and it is not tracked as an item anywhere yet"
  - epic: 20
    action: "Gap 4b — bmad-spec Update on spec-slide-artifact-model, opened 2026-07-31 by the Gap 4 pass because the findings had a fix route and no tracker. Review findings F-4 and F-10 in planning-artifacts/architecture/architecture-bic-pptx-workflow-2026-07-10/reviews/review-cross-document-coverage-2026-07-30-validate-b.md. This file is an ADOPTED COMPANION of spec-artifact-registry-authoring, so Stories 20.1 and 20.3 read it as contract: SPEC.md:64 still presents data/default-registry.json as a seed that inserts missing template IDs only — AD-17's whole Prevents is that a gap-filler resurrects a deleted row on every boot, forever; SPEC.md:65 still presents artifact templates as global across services — AD-16 reversed it; and CAP-8's success still requires BibleVerseContemplation to remain a TextPlaceholder, a kind AD-19 abolished (gone rather than renamed), with deferred-work.md:83 compounding it by recording welcome's change to text-placeholder as current state. The trap is the supersession note at :16, which says 'Two reversals matter to anyone reading this file directly' and enumerates the seven base types and the create/delete/reorder boundary — affirmatively implying everything else still binds. Fix: extend :16 from two reversals to four naming AD-17 against :64 and AD-16 against :65, mark both constraint bullets superseded in place, extend the note to CAP-8 and state the successor (a General carrying a Placeholder Catalog text element)"
    owner: "kodesh87"
    status: done # 2026-07-31 bmad-spec Update. Delivered exactly the named fix plus one step for coherence: banner :16 now enumerates four reversals with CAP-8's successor stated; both Constraints bullets carry their superseding AD in place (the clauses that still bind — SQLite SSOT, admin-only with DB role re-check — stand unmarked); registry-contract.md:5's own banner had the same two-item trap the review did not name and was matched to the same four. The bullets sit at ~:72/:73 after the banner grew; the only citations to the old :64/:65 are review records, verified by grep. CAP text and IDs untouched — rewriting the seven-type table stays Story 20-2's delivery. deferred-work.md:83 not edited (implementation artifact, outside bmad-spec's write surface); logged in the spec memlog. Self-Validate PASS both passes; memlog at 65 entries
  - epic: 20
    action: "Gap 3 — one finding to fold into the bmad-ux pass, NOT the whole gap-3 scope (see the 2026-07-30 handover for that): CAP-5 shows a registry entry as '[kind] label', and AD-19 now fixes six recognized keys over three kinds with song-set naming the kind but never an entry. So for a songset-bt-open row, does the chip read [song-set] or [songset-bt-open]? AD-19 implies the kind, but the spine does not say it and the display is EXPERIENCE.md's to own. Raised by the rubric-walker lens (R9) in reviews/review-rubric-walker-2026-07-30-post-trim.md and deliberately routed rather than decided in the spine"
    owner: "kodesh87"
    status: done # 2026-07-31 bmad-ux Update, scope held to R9 alone per this item's own instruction. Decided in EXPERIENCE.md (Inside /admin/artifacts, 'Row display'): the chip reads the KIND, never the entry key. Three grounds, all pre-existing: AD-19 makes slot keys server-owned binding vocabulary; CAP-5's success makes the label the only administrator-editable part; EXPERIENCE.md's own voice rule bars system vocabulary from human surfaces, and a raw hyphenated key in a chip is exactly that. Recorded with its consequence rather than as a bare verdict: four SongSet rows share one chip and are told apart by editable labels, so the bounded-configuration surface must state the row's slot identity read-only in worship vocabulary — a rename must not orphan the row's liturgical identity. Owners in place: Story 20.3 (chip), Story 20.7 (slot statement). DESIGN.md untouched — display content, not visual identity
  - epic: 20
    action: "Story 20.1 seed authoring — the SongSet layout specification the owner gave on 2026-07-30. Recorded here rather than in the architecture spine deliberately: it is one seed with one answer, so no two units can choose it incompatibly, and AD-22 fixes only that the layout is developer-owned seed data. TITLE slide: follow the sample slide as closely as possible; full-screen background image. LYRIC slide: the verse marker at the top, the lyric text below it filling roughly the top two thirds, sized to be read from the back of the room. Verse and refrain SHARE ONE lyric layout and ONE background image — the only difference is the label value, so do not author a third layout. What the owner's description did not cover and the seed author must handle: src/lib/lyrics.ts emits 'n/total' for a verse (line 384), 'Reff' (386) AND 'Chorus' (388), and for lyrics with no headers it emits an EMPTY label (413) or '1/1' (452) — so the label element must tolerate an empty value. It is required:false in the seed today; keep it that way"
    owner: "Developer"
    status: open

  # --- Added 2026-07-31 ---
  - epic: null
    action: "COLLATERAL DAMAGE of the 2026-07-29 PII remediation, in a class none of the items above anticipated: the text substitution that replaced real names with invented ones ran over the whole tracked tree, including files where a name is not prose. A four-character real name occurred as a substring inside the base64 sha512 of set-function-name@2.0.2 in package-lock.json, and the five-character replacement went in with it. The digest became 89 base64 characters where a sha512 is always 88, and `npm ci` failed with EINTEGRITY repo-wide, on every machine, for two days. Nothing caught it because npm is the only thing in this repository that verifies a checksum, and npm only speaks when someone installs — `npm test` never looked at a derived value at all. FIXED in 488eb19 by restoring the registry's authentic digest (an upstream checksum, not congregation data; the substring is not fingerprinted and the public-repo guard passes 4/4 with it). Integrity verification was NOT disabled to get past it"
    owner: "kodesh87"
    status: done
  - epic: null
    action: "RESIDUAL of the lockfile corruption: measure the real blast radius. The 2026-07-29 detector was base64 LENGTH, which by construction only sees a substitution that changes length — an equal-length rewrite decodes to the right byte count and is invisible to it. So `exactly one affected` was an unproven claim when it was made"
    owner: "kodesh87"
    status: done # 2026-07-31. Measured, not assumed, and by a method that does not depend on length. (1) LOCKFILE: all 819 registry-resolved integrity values compared against the registry's own dist.integrity via the per-version manifest endpoint — 0 mismatches, 0 unverifiable. That is exhaustive and settles the length-preserving case for package-lock.json. (2) WHOLE TREE: 973 tracked files swept with two independent detectors — name-targeted (an invented name glued inside a long mixed alphanumeric token, which is the exact signature of the damage) and name-agnostic (SRI size, base64 decodability and canonical round-trip, hex fields carrying non-hex letters). Every hit was a benign coincidence: `ada` is itself valid hex and occurs naturally in sha256 digests, `adver-sari-al` in a review filename, `re-ada-ble` in a package name, and several natural base64 runs inside digests already registry-verified. (3) OTHER DERIVED VALUES: _bmad/_config/files-manifest.csv recomputed — of 275 rows, 262 point at files not tracked here, 12 match their committed blob exactly, and the single mismatch (cis/module-help.csv) is a CRLF-vs-LF artifact in vendor content last touched 2026-07-28, before the remediation, containing no invented name. (4) data/asset-map.json has zero invented-name hits in any field, consistent with acc8df0 having withheld payload runs, and tests/fixtures/ contains only the prose rundown. CONCLUSION: the lockfile was the only casualty
  - epic: null
    action: "RESIDUAL of the lockfile corruption: recover the substitution pairs the 2026-07-29 pass applied, so the sweep above could be aimed. NOT FULLY POSSIBLE and that is by design — the history rewrite worked. refs/original is gone, `git fsck` finds no dangling commits, the 36 MB rollback bundle is not on this machine, and the local clone postdates the rewrite (`main@{6}: clone`), so the real names are unrecoverable from git here. What WAS recovered is the pair that mattered: the damage itself carries it. The corrupt digest and the authentic one differ in exactly one substring, which names both halves of one pair, and the invented half is traceable in the tree to a predecessor operator in the brief's tool-churn history. The remaining invented cast was enumerated from the tree the remediation left behind. NOTE FOR ANYONE EXTENDING THE NAME GUARD: the real name in that pair is a substring of an authentic upstream sha512 that must stay in package-lock.json. Adding it to FORBIDDEN_NAME_HASHES would fail the guard on a legitimate file and pressure someone into weakening the guard or corrupting the lockfile again — the exact trap that produced this incident. It is deliberately not fingerprinted"
    owner: "kodesh87"
    status: done
  - epic: null
    action: "Close the recurrence gap the lockfile corruption exposed: nothing in `npm test` verified any derived value, so a rewritten checksum could only be found by installing"
    owner: "kodesh87"
    status: done # 2026-07-31. tests/lockfile-integrity.test.mjs added and wired into the package.json test list. Asserts every integrity value is <algorithm>-<base64> with a known algorithm, strict base64 charset, the exact character count and decoded byte count for that algorithm, and canonical base64 round-trip. Offline and deterministic — no network, so it runs in CI. NEGATIVE-TESTED both ways in a window that reverts: reinjecting the exact 2026-07-29 damage fails it with `sha512 payload is 89 base64 characters, must be 88`, naming the package; and an equal-length substitution PASSES, which is the documented limit stated in the file header rather than left for someone to discover. The verdict for a length-preserving rewrite remains `npm ci` against a clean cache or a direct dist.integrity comparison; the offline guard is a filter, not a proof

  # --- Added 2026-08-01 with Epics 21-23 (Correct Course) ---
  - epic: 22
    action: "STILL OPEN, and re-scoped 2026-08-01: Story 22.2 shipped without waiting on this, which needs stating precisely so the gate is not quietly treated as closed. It shipped because it introduced NO new channel — upsertHymns already overwrote title and lyrics from the corpus on every boot, so the corrected titles reached the table through the path that was already there, and no schema or seeding channel was added for them. What this run still owes is the forward question, unchanged: SHOULD a shipped reference corpus keep an every-boot overwrite? If the answer is AD-21's counter, the migration target is upsertHymns in src/lib/db/index.ts, not the corpus data. Note the new bible seeder deliberately does NOT use that channel: seedBibleCorpus only fills a translation holding zero verses, so it never overwrites a persisted value — one precedent for the decision, not the decision itself. ORIGINAL TEXT FOLLOWS. bmad-architecture Update: no AD governs a SHIPPED REFERENCE CORPUS at all. AD-11 and AD-17 cover the registry seed only, while the hymnal is upserted on every boot (src/lib/db/index.ts:262), overwriting title and lyrics — the boot-time value-change channel AD-17 removed for the registry and AD-21 routes through a declared transition n to n+1. Correcting 695 persisted titles is exactly that case, and the spine records at :370 that AD-21 counter does not exist and no story owns introducing it. Add the next AD-n — never renumber — settling whether the shipped corpora ride the boot upsert or the counter arrives here. The same run amends the Structural Seed tree (:315 names data/ hymnal corpus (hymns.json); :322 the scripts list) and clears both Deferred bullets (:360 FR-19 corpus ops, :363 SDAH licence). Line numbers re-measured after merging origin/main on 2026-08-01, which moved all five by four"
    owner: "kodesh87"
    status: done # 2026-08-01, settled in ONE run with the epic-21 item below. ANSWERED AS A THIRD OPTION rather than inside the binary the item posed: neither the boot upsert nor AD-21's counter. AD-25 [ADOPTED, partial] — a shipped reference corpus is a PROJECTION of its committed file; the file is authoritative and the table is reconciled to it, completely within the corpus code it names. The premise both offered answers shared is false: AD-17 exists because an administrator owns registry rows, and AD-21 counts transitions over values somebody persisted deliberately, and neither is true of a corpus table — MEASURED, zero INSERT/UPDATE/DELETE against hymns, bible_verses or bible_books outside src/lib/db/index.ts. So correcting 695 titles was never AD-21's case, and the counter's landing story stays unassigned at Epic 20's first release (spine Deferred amended to say so). The two shipped shapes are wrong in OPPOSITE directions and both change: upsertHymns removes nothing, seedBibleCorpus early-returns so a correction never arrives. The owner also chose the reconcile to be COMPLETE — rows the corpus no longer holds are removed, bounded to that corpus code — which is free today only under AD-4's no-deployment date and expires with AD-18's licence on the same event. Structural Seed tree repaired (the data/ and scripts/ lines) and both Deferred bullets struck as closed with their record kept: FR-19 corpus ops (KJV committed, import:kjv retired, verified structurally) and the SDAH licence (an accepted risk, not a cleared one — attribution and takedown statement verified inside the file, guarded by verify-corpora.mjs:133-138 and tests/corpus.test.mjs:102-104)
  - epic: 21
    action: "bmad-ux Update: EXPERIENCE.md:143 states, as a shipped state, that lookup is unavailable when the corpus was never imported (an ops step). Once the corpus ships the default flips, and that message survives only as the unreadable-file path. One State Patterns row; no route changes, so the IA table is untouched"
    owner: "kodesh87"
    status: done # 2026-08-01 bmad-ux Update. This is clause (1) of the epic-24 item below and was closed in that same run, as that item asked. THE ROW WAS FALSE IN A SECOND WAY THIS ITEM DID NOT KNOW ABOUT, found by reading src/ rather than the row: it also claimed 'the presenter says so instead of returning empty results'. /api/scripture does say so — 503 naming the file and npm run corpus:verify (route.ts:18-27) — but PresenterOperator.tsx:432 collapses every non-404 status into a flat 'Lookup failed', so that message reaches the operator on the two service forms (CreateForm.tsx:300, EditForm.tsx:316) and NOWHERE on the surface used during a service. One product, one fault, two answers, and the mute one is the room-facing half. Row rewritten as 'Corpus empty' with both corrections stated and the old text quoted; routed to Story 21.5, which already owns this field on all three surfaces and carries 'an unresolved reference is visible as a defect'
  - epic: 22
    action: "Supply the number-to-title list for the 695 SDAH hymns. Story 22.2 cannot start without it: the source dump carried no title column, deriveTitle() takes the first lyric line by design, and .work/lirik-lagu.json is gone — so there is nothing in the repository to derive a real title from. Owner chose to supply the list rather than have an agent source one"
    owner: "kodesh87"
    status: done # 2026-08-01 the owner supplied all 695, complete and contiguous 1..695 with no duplicates. Applied verbatim into data/song-book/sdah.json apart from whitespace collapse (two entries carried a double space). Longest title is now 39 chars, against 40 titles over 45 chars before. NOT applied: four entries the corpus lyrics contradict, left as supplied because the owner index is authoritative — see the follow-up item below
  - epic: 22
    action: "Decide four SDAH titles the owner index spells differently from the lyrics beneath them, left VERBATIM rather than silently corrected: #81 'Thou I Speak With Tongues' (lyric: 'Though I speak with tongues'); #231 'Bless Be the King' (lyric agrees, but the hymn is 'Blest Be the King Whose Coming' — both the index and the lyric carry it, suggesting one upstream source); #234 'Christ Is the Worlds Light' (lyric: \"Christ is the world's Light\" — missing apostrophe); #356 'All Who Love and Serve Youre City' (lyric: 'All who love and serve your city'). Each projects onto a slide as written. One TSV-shaped correction fixes all four; nothing else depends on it"
    owner: "kodesh87"
    status: dropped # 2026-08-01 raised to the owner and EXPLICITLY DROPPED. The owner-supplied index is authoritative and stays verbatim; all four project as written. Closed by the second Correct Course of the day. Do not re-raise — a later pass rediscovering these four spellings should read this line and stop, which is the whole reason the finding is recorded rather than deleted
  - epic: 21
    action: "Delete .work/tp_bible_book_translations_*.json and .work/tp_bible_verses_*.json from the main checkout AFTER Story 21.1 completeness assertion is green (66 books / 1,189 chapters / 31,102 verses). Not before: they are the only copy of the source, and the hymnal corpus is already in exactly the state this ordering exists to avoid"
    owner: "kodesh87"
    status: done # 2026-08-01, in that order. The assertion went green first (tests/corpus.test.mjs + npm run corpus:verify), then a full round-trip comparison confirmed all 31,102 verses and all 66 book name/shortName pairs reproduce byte-for-byte from the committed file, and only then were both exports deleted. No tp_bible_* file remains anywhere under the project root. data/bible/kjv.json is now the only copy, which is why its completeness is asserted structurally on every test run rather than sampled
  - epic: 21
    action: "bmad-architecture Update — SCOPE WIDENED 2026-08-01 by the second Correct Course of the day; this EXTENDS the still-open epic-22 architecture item above rather than replacing it, and both should be settled in ONE run. What the locale axis adds to that run: a bible_translations registry and a song_books registry, each carrying locale, name, licence and provenance; per-translation book names as a first-class table; a canonical book identity stable across translations, so 'same passage, another translation' stays one query; and the renames the axis implies — bible_verses.translation to translation_code, hymns.book_code to song_book_code, isKjvCorpusEmpty() to isBibleTranslationEmpty(code). The target shape was deliberately NOT decided by Correct Course; it is named there only so the stories can cite something. Never renumber an existing AD-n — add the next one. NOT part of this run and NOT an open item: per-locale template identity on artifact_templates. Rejecting projection_locale makes one global template set a recorded accepted trade (PRD 4.12), not work awaiting a decision"
    owner: "Developer"
    status: done # 2026-08-01, same run as the epic-22 item above. AD-26 [TARGET] — a corpus is a REGISTERED ENTITY: one row per corpus in bible_translations or song_books carrying code, name, locale, licence and provenance. The corpus CODE is globally unique across locales and is the cross-boundary key (AD-19's class); LOCALE IS AN ATTRIBUTE, never part of a key and never a predicate — which is what makes FR-24's never-filter rule STRUCTURAL rather than disciplinary, since no key holds a locale so no read path can need one. The file declares and the path merely locates: the loader already refuses a declared code disagreeing with the loaded code (corpus.ts:91, :176) and refuses a declared locale disagreeing with its directory on the same terms; two files declaring one code refuse the boot; a default_* naming an uninstalled corpus is INERT, not an error, and is not rewritten. All three renames carried, and under AD-25 they are rebuilds rather than migrations because the tables are projections. AD-27 [TARGET] — book identity is canonical, translation-independent and carries NO display text; every name belongs to a translation; a corpus MAPS onto the identity and a book outside the canon is refused with the book named. SPELLING SETTLED (owner): the declared field is `locale`, not `language` — both committed files ship `language` today and are corrected in the same change set. Story 21.4 is unblocked and cites AD-27. FOUND WHILE SIZING IT, filed in the spine's Deferred rather than fixed here (an architecture run does not patch production code): the SHIPPED DEFAULT CORPUS MISNAMES A BOOK, and the alias map is what caught it. BOOK_ALIASES targets "Song of Solomon" for book 22 while data/bible/kjv.json says "Song of Songs", so three entries resolve to nothing — but the KJV's own title IS "Song of Solomon" (verified against Project Gutenberg's KJV: the book heading reads Song of Solomon, and "the song of songs" is a phrase inside verse 1). "Song of Songs" is the Hebrew-derived title other translations use and it came in through the SQL export the corpus was normalised from. So the CORPUS is wrong, it is a live AD-27 output-is-exact violation on the default translation, and it is a one-word data fix; checked across all 66 books, exactly one mismatch. The parser is the harder half and the corpus fix does not reach it: parseScriptureRef caps a book name at two words and ASCII, so "Song of Solomon 2:1" is rejected exactly as "Song of Songs 2:1" is — book 22 is the only book whose name exceeds two words, which is why the cap hid this long — and "Kisah Para Rasul 1:8" is what FR-24's generous input walks into
  - epic: 24
    action: "bmad-ux Update — EXPERIENCE.md is now stale in TWO ways and one run should close both. (1) The pre-existing item: the State Patterns row at EXPERIENCE.md:143 states, as a shipped state, that lookup is unavailable when the corpus was never imported (an ops step). The corpus now ships, so the default flips and that message survives only as the unreadable-file path. (2) NEW 2026-08-01: FR-24 adds a corpus picker with an always-present control for browsing locales outside the default, and FR-25 adds a locale switcher — both are UX surfaces no artifact describes. The never-filter rule is what the picker must make visible: an Operator has to be able to SEE that other locales are reachable, or a default that only filters the view will read to them as one that filters the data. Whether either adds a route determines if the IA table moves; the pre-existing item alone did not. (3) NEW 2026-08-01, from the input-model Correct Course, and it lands in the SAME file so the same run should take it: the scripture reference input becomes ONE FIELD WITH INLINE AUTOCOMPLETE serving every operator surface (Presenter Mode lookup plus the create/edit service forms), on the HymnNumberAutocomplete precedent. That is a component-inventory row beside HymnNumberAutocomplete at EXPERIENCE.md:97, not a route — so on its own it leaves the IA table alone. Worth stating in the row because the choice is load-bearing rather than cosmetic: a book PICKER was recommended and the owner chose the single field, which is what keeps paste working and what lets the rundown reuse the same matcher instead of needing a second one. (4) NEW 2026-08-01, from the AD-28 Reviewer Gate, and it lands in the SAME file for the same reason - take it in the same run. Under AD-28 the rundown matcher spans EVERY INSTALLED TRANSLATION and an ambiguous reference is refused rather than guessed, which is correct and fail-closed. The consequence nothing describes: ADDING A CORPUS IS THEREFORE NOT PURELY ADDITIVE ON THAT SURFACE - a reference that resolved unambiguously against one translation can become ambiguous against two and start arriving as unmapped input. NFR-5 already makes unmapped input VISIBLE; what is missing is the EXPLANATION. An operator sees a reference that was fine last Sabbath arrive unmapped, with nothing connecting it to an install nobody associated with the rundown, and this surfaces on a Sabbath morning on a rundown they did not edit - AD-1 shape. An affordance question on the same footing as the stale-snapshot and Reset-reverts-a-rename items the spine already handed to EXPERIENCE.md. It blocks neither Story 21.4 nor 21.5"
    owner: "Developer"
    status: done # 2026-08-01 bmad-ux Update, ALL FOUR CLAUSES CLOSED IN ONE RUN as this item asked, plus the epic-21 item above which is clause (1). (1) Row rewritten as 'Corpus empty' — see that item for the second falsehood it turned out to carry. (2) THE ROUTE QUESTION IS ANSWERED, NO: neither the picker nor the switcher is a route, so the IA table does not move and three rows join the Sub-surfaces table instead — translation control in the Presenter panel (21.3), the four language settings on /admin (21.3, 22.3, 24.1), the ui_locale switcher on /admin (24.1). Reasoning recorded in the file rather than only here: what WOULD have moved the IA table is a browse-the-corpora surface of its own, and FR-24 asks for the opposite — reachability from where the choice is made. Corpus picker added as a Component Patterns row whose stated job is making the never-filter rule VISIBLE, because the failure is silent: an operator cannot tell a view filter from a data filter, and one who believes the English hymnal is unreachable never asks for it. (3) Scripture reference field added beside HymnNumberAutocomplete, with the single-field choice recorded as load-bearing rather than cosmetic. Measured while writing it: three plain inputs today, no component — PresenterOperator.tsx:696, CreateForm.tsx:584, EditForm.tsx:615. (4) Open Item 8, no owner, on the footing this item named. It gained three bounds the item did not have: the ambiguity is INTRA-translation and live on the single corpus shipping today (Phil, Jo), so an explanation written only for the cross-translation case is missing where it is already needed; the colliding candidates are known at the moment of refusal, so naming them is cheap; and the operator cannot act on the corpus, so it must not read as an instruction to fix an install. ALSO IN THIS RUN: Flow 9 added so the three new sub-surfaces are landed on by a journey — the every-surface-is-landed-on assertion is testable and adding rows without it would have broken it — and one PRE-EXISTING CITATION ROT repaired, the Venue bullet pointing at 'Open Item 5' for the stale-snapshot affordance when the 2026-07-31 toast insertion had pushed it to 6; item 5 is session revocation, so it resolved to a real entry about something else. DEVIATIONS: Reviewer Gate not offered and not run (session instruction forbids subagents unless asked; every lens is one), and doc_standards run as a targeted self-check against prior findings rather than as full passes — same two deviations as the 2026-07-30 runs, and a full pass can be requested

  # --- Added 2026-08-01 by the input-model Correct Course (third run of the day) ---
  - epic: 21
    action: "bmad-architecture Update — the owner direction REVERSES a clause AD-27 shipped hours earlier, so it needs a NEW AD rather than an edit to that one. NEVER RENUMBER: add AD-28. What it must settle. (1) AD-27's 'input tolerance is the matcher's concern, never the corpus's' SURVIVES, but its 'one SHARED server-side matcher' does not: the matcher is one implementation carrying a SCOPE — the chosen translation on operator surfaces, all installed translations on the rundown, because a Telegram sender picks none. (2) AD-27 enumerates tolerance as 'case, punctuation, abbreviation, singular against plural' and treats the four as uniform across translations. THREE OF THEM ARE; ABBREVIATION IS NOT. 'Jn' is English tolerance, so an unscoped alias list reintroduces exactly the cross-language input this direction removes — through the matcher rather than through a corpus file, which is the door AD-27 was watching. Aliases therefore BELONG TO A TRANSLATION while the comparison rules do not, and the enumeration has to be split rather than kept as one list. (3) A tolerance collision between two translations in rundown scope is surfaced as unmapped input (NFR-5) and NEVER guessed — the fail-closed posture AD-5, AD-8 and AD-17 already take. (4) shortName leaves AD-27's Rule and the bible_books column; under AD-25 that is a rebuild rather than a migration, because the table is a projection. (5) The Boundaries convention is ALREADY violated on shipped code — scripture.ts:42 and parser.ts:152/:162 carry the same regex — and the one-matcher rule is what closes it, which also means Story 21.4 legitimately reaches into Epic 2/5 code. What SURVIVES of AD-27 and must not be re-litigated: the canonical translation-independent identity, names owned by the translation, output exact, and refusal with the book named"
    owner: "Developer"
    status: done # 2026-08-01, bmad-architecture Update. AD-28 [TARGET] added as the next id; NOTHING RENUMBERED and AD-27 keeps every clause that survives, with the two reversed ones struck IN PLACE and pointed at AD-28 rather than deleted, because AD-27 is [TARGET] and unbuilt - which is exactly when a half-reversed rule reads as a whole one. All five points settled. The Reviewer Gate then found FOUR THINGS THE FIVE POINTS DID NOT COVER, all closed in-run. (1) NOBODY SAID WHO SUPPLIES THE SCOPE: Story 21.5 could reasonably send none and expect the server to read default_bible_translation while 21.4 could reasonably read an absent scope as ALL INSTALLED - the other scope the same decision names - which compiles, passes both stories AC, and REINSTATES CROSS-LANGUAGE OPERATOR INPUT THROUGH A DEFAULT. Now a required, registry-validated corpus code; absent or unrecognised is REFUSED; all-installed is a property of the rundown surface, never a fallback. (2) LONGEST-PREFIX MATCH NEVER SAID WHICH SIDE THE PREFIX WAS ON, and both directions are required by the decision own examples - corpus-name-prefixes-input to find where a book name ends, typed-text-prefixes-corpus-name for Ps to reach Psalms, which is the whole case for dropping shortName. Implement only the first and the server refuses Ps 23:1 while the autocomplete beside it offers Psalms, ON PASTE. Now stated in both directions with a disambiguation rule. (3) THE COLLISION CLAUSE WAS WRITTEN FOR TWO TRANSLATIONS COLLIDING and the measured ambiguity is INTRA-translation and live on the single corpus that ships today - Phil matches Philippians and Philemon, Jo matches five books - so a builder in a single-corpus install would reasonably conclude the rule did not apply yet. Generalised. (4) STORY 21.2 translation parameter and the matcher SCOPE are the same value on an operator surface and nothing said so, so two parameters that must always agree could land separately in the same module. Stated. ALSO FOUND, and filed in the spine Deferred rather than fixed because an architecture run does not patch production code: A SECOND LIVE output-is-exact VIOLATION distinct from the Song of Songs corpus misnaming - scripture.ts:139-142 echoes the reference from the OPERATOR TYPED STRING, so ps 23:1 comes back as ps 23:1, and AD-28 makes it worse because every tolerance it grants is another abbreviation that can reach a reference the congregation sees. Owned by Story 21.4. Story 21.4 and 21.5 are UNBLOCKED. SEQUENCING WARNING CARRIED OUT OF THIS RUN: Story 21.2 and 21.4 both rewrite src/lib/scripture.ts - 21.2 removes the KJV literals at :6 :106 :128 :144 while 21.4 replaces :42, :65 and :87-88 - different lines, same file, and 21.4 replaces the whole resolution path, so run 21.2 THEN 21.4 rather than in parallel worktrees
