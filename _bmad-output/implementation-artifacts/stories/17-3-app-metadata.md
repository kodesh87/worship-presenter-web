---
baseline_commit: 0dc3c08e1d373bf38a25643f42f3a7cef19e0416
---

# Story 17.3: The App Says Its Own Name

Status: ready-for-dev

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As anyone with the hub open,
I want the browser tab and bookmarks to name this application,
so that it is not filed as *Create Next App*.

## Acceptance Criteria

1. **The root `metadata` export no longer carries create-next-app boilerplate.** In `src/app/layout.tsx`, `export const metadata: Metadata` no longer has `title: "Create Next App"` or `description: "Generated by create next app"`. `title` names the real application — **`"Worship Presenter Web"`**, the canonical name already recorded in `DESIGN.md` frontmatter (`name: Worship Presenter Web`) and matching `package.json`'s `name: "worship-presenter-web"`. `description` is a short, accurate one-line description of what the hub does (an operator tool for preparing and presenting a worship service). The exact description wording is product-owned per the epic and this Dev Notes section's guidance — it may be refined at review — but it must not be the create-next-app default and must not name any real congregation, person, or organization.

2. **The change is scoped to the one export, nothing else.** Only the `metadata` object's `title`/`description` fields change in `src/app/layout.tsx`. `export const dynamic = 'force-dynamic'`, the `getUiLocale()` call, the `<html lang>` / `suppressHydrationWarning` / `className` attributes, the `<body>` structure, and the `ThemeProvider` mounting stay byte-for-byte unchanged. No favicon, icon, manifest, other route, component, or projected surface (`slide-surface`, PPTX, projector, slideshow) is touched — this story is the title/description fix only, not a broader branding pass. `tests/theme-chrome.test.mjs`'s existing AC-1/AC-2 assertions that read `src/app/layout.tsx` for the `ThemeProvider` mount and the `suppressHydrationWarning` html mismatch comment continue to pass unmodified.

3. **A regression test protects the property.** A test asserts that `src/app/layout.tsx`'s `metadata` export contains neither the literal string `"Create Next App"` nor `"Generated by create next app"`, and that `title` resolves to the real product name. Extend the existing `tests/theme-chrome.test.mjs` (it already does `read('src/app/layout.tsx')` for AC-1/AC-2, so this avoids introducing and registering a new test file). If a separate new test file is created instead, it **must** be added to the explicit `scripts.test` file list in `package.json` in the same change set — an unregistered test file never runs, in CI or locally, and nothing detects the omission.

4. **The UX authority document is synchronized.** `DESIGN.md` Open Item 3 (*"`metadata` is still create-next-app boilerplate"*, owner: Story 17.3) is marked closed, recording the shipped `title`/`description` values and the date. No other Open Item, token, or component row in `DESIGN.md` is touched. `EXPERIENCE.md` needs no change — this story adds no route, surface, or IA-table entry, and the epic's own preamble records this as `DESIGN.md`-governed operator-chrome identity, not a PRD/FR item.

5. **Repository verification is clean in the supported environment.** On Node.js 22.x (`>=22.12`), the focused theme test, TypeScript check, public-repository guard, and full registered suite pass. `npm run lint` introduces no new problem relative to the current baseline (31 problems as of 2026-08-01/2026-08-03 measurements — re-confirm the live baseline at implementation time rather than trusting this number, per the project-context rule that a working copy with agent worktrees can wildly inflate the count).

## Tasks / Subtasks

- [ ] Establish fail-first regression coverage (AC: 3)
  - [ ] Add an assertion to `tests/theme-chrome.test.mjs` (or a new registered test file) that `src/app/layout.tsx` no longer contains `Create Next App` / `Generated by create next app`, and that `title` matches the real product name.
  - [ ] Run the test before editing the source and confirm it fails against the current boilerplate (fail-first evidence for the Dev Agent Record).

- [ ] Update the metadata export (AC: 1, 2)
  - [ ] Change only `title` and `description` inside `export const metadata: Metadata` in `src/app/layout.tsx` to `"Worship Presenter Web"` and a short accurate one-line description.
  - [ ] Confirm from the diff that no other line in `layout.tsx` changed — `dynamic`, `getUiLocale()`, the `<html>`/`<body>` JSX, and `ThemeProvider` mounting are untouched.
  - [ ] Do not touch `src/app/favicon.ico`, any icon/manifest file, or any other route — out of scope per the epic's "one-line change" framing.

- [ ] Run the new test and confirm it now passes (AC: 3)

- [ ] Synchronize UX authority (AC: 4)
  - [ ] Close `DESIGN.md` Open Item 3 with the shipped values and today's date, following the closure convention the other Open Items in the same file already use (state what was true before, what changed, and the evidence).
  - [ ] Update `epics.md` Story 17.3 label and the Epic 17 summary line to reflect the new status, keeping the other story statuses in that line unchanged.

- [ ] Run supported verification and complete the record (AC: 5)
  - [ ] Run `node --import ./tests/register-ts-resolve.mjs --test --experimental-strip-types tests/theme-chrome.test.mjs`.
  - [ ] Run `npx tsc --noEmit`.
  - [ ] Run `node --import ./tests/register-ts-resolve.mjs --test --experimental-strip-types tests/public-repo-guard.test.mjs`.
  - [ ] Run `npm test` on the supported Node 22 environment; use `npm ci` then `npm run build` first if the native dependency ABI/build state is stale.
  - [ ] Run `npm run lint` and compare against the freshly measured baseline (not a number copied from a prior story) — introduce zero new findings.
  - [ ] Inspect the final diff for anything outside `layout.tsx`, the test file, `DESIGN.md`, `epics.md`, `sprint-status.yaml`, and this story file. Update the Dev Agent Record and File List.

## Dev Notes

### Scope and authority

- This story deliberately has **no PRD functional-requirement ancestry**, same as Story 17.2. Epic 17's preamble records operator-chrome visual identity and self-presentation as `DESIGN.md` territory, not a PRD FR — this story changes what the browser tab/bookmark shows, not any Deck/Slide/payload contract.
- `EXPERIENCE.md` is **not** expected to change: it covers IA/routes/flows, and this story adds no route or surface. Do not add an IA-table row for a metadata string.
- The wording is explicitly called out as product-owned in both `epics.md` ("the wording is product-owned") and `DESIGN.md` Open Item 3 ("wording is product-owned"). `"Worship Presenter Web"` for `title` is not a guess — it is the exact canonical name already used in this project's own `DESIGN.md` frontmatter (`name: Worship Presenter Web`) and `package.json` (`name: "worship-presenter-web"`), so it requires no new naming decision. The `description` string has more latitude; keep it short, accurate, and free of any real congregation/person/organization name per the repository's public-repo rule (`AGENTS.md`).

### Implementation guardrails

- Current source (`src/app/layout.tsx:17-20`):
  ```ts
  export const metadata: Metadata = {
    title: "Create Next App",
    description: "Generated by create next app",
  };
  ```
  This is the entire diff surface for AC-1/AC-2. Everything else in the file — the `Geist`/`Geist_Mono` font setup, `getUiLocale()`, `export const dynamic = 'force-dynamic'` (load-bearing: `ui_locale` is read from SQLite per request, per its own comment), the `<html>` attributes, and `<ThemeProvider>` — is out of scope.
  - Also correct here: since this involves `src/app/layout.tsx`, review the file's UPDATE, not just the metadata literal — nothing else in it needs to change, but confirm that by reading it, not by assuming.
- `tests/theme-chrome.test.mjs` already does `const layout = read('src/app/layout.tsx')` for its AC-1/AC-2 tests (`'AC-1\\AC-2: the root layout mounts the theme provider and suppresses the html mismatch'` and another read at line ~2015 checking `<ThemeProvider>` is above `sonner`). A metadata-only edit must not perturb those regexes; verify the existing tests still pass rather than only trusting a diff review.
- Do not introduce a colour, token, favicon, or icon change — Open Item 3 and the epic text both scope this to the `metadata` export's `title`/`description` fields only.
- No architecture spine amendment is needed: this changes no structural invariant, route/surface, storage target, schema, auth gate, slide-order source, or sync channel (AGENTS.md's four-artifact-family list). No `bmad-architecture` Update run is required for this story.

### Previous story intelligence (Story 17.2)

- Story 17.2 (`muted-foreground-contrast`, `done`) is the immediately preceding story in this epic. Relevant carryover:
  - The same "one narrow token/export change, nothing else" discipline applies here at an even smaller scope (two string literals vs. one CSS token).
  - 17.2 closed its DESIGN.md Open Item with a dated, evidence-bearing paragraph (see `DESIGN.md` Open Item 1) rather than a bare "done" — follow that same closure convention for Open Item 3.
  - 17.2's review found and fixed a stale `epics.md`/tracking-status mismatch (keeping the story marked `review` until the reviewer gate actually closed). Do not mark this story `done` in `sprint-status.yaml` prematurely; that transition happens via `bmad-code-review`, not this workflow.
  - 17.2 registered its lint/test baseline by measuring it fresh rather than trusting an older story's number — the project-context.md rule that a worktree-inflated lint count is a measurement error, not a real regression, applies again here.

### Testing Standards

- Supported runtime: Node.js 22.x (`>=22.12`). Use `node:test` only (`node --import ./tests/register-ts-resolve.mjs --test --experimental-strip-types tests/<file>.test.mjs`) — never Jest/Vitest.
- Required focused test command (if extending the existing file):
  `node --import ./tests/register-ts-resolve.mjs --test --experimental-strip-types tests/theme-chrome.test.mjs`
- Required public-repository guard command:
  `node --import ./tests/register-ts-resolve.mjs --test --experimental-strip-types tests/public-repo-guard.test.mjs`
- Run the full registered suite with `npm test`, TypeScript with `npx tsc --noEmit`, and lint with `npm run lint`.
- If a new test file is added instead of extending `tests/theme-chrome.test.mjs`, it must be appended to the `scripts.test` command string in `package.json` in the same change set — this is the single highest-cost, structurally undetectable omission in this repository per `project-context.md`.

### Project Structure Notes

- Expected implementation files:
  - `src/app/layout.tsx` — `title`/`description` literals only.
  - `tests/theme-chrome.test.mjs` — new regression assertion (preferred), or a new registered test file.
  - `_bmad-output/planning-artifacts/ux-designs/ux-bic-pptx-workflow-2026-07-10/DESIGN.md` — Open Item 3 closure.
  - `_bmad-output/planning-artifacts/epics.md` — Story 17.3 label / Epic 17 summary line sync.
  - this story file and `_bmad-output/implementation-artifacts/sprint-status.yaml` — normal tracking updates.
- No new source module, dependency, route, component, API, database/schema change, architecture decision, or design token is expected. `EXPERIENCE.md` and `deferred-work.md` have no Story 17.3 entries to reconcile (verified: no matches for "17.3"/"17-3" in either file).

### References

- [Source: `_bmad-output/planning-artifacts/epics.md:298-301`] — Story 17.3 statement: browser tab/bookmark naming, `layout.tsx` boilerplate citation, "one-line change... wording is product-owned."
- [Source: `_bmad-output/planning-artifacts/epics.md:280,284`] — Epic 17 status line and non-PRD, `DESIGN.md`-governed operator-chrome authority.
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-bic-pptx-workflow-2026-07-10/DESIGN.md:234`] — Open Item 3, exact current boilerplate literals and line citation (`layout.tsx:16-17` in that artifact's own line numbering).
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-bic-pptx-workflow-2026-07-10/DESIGN.md:2,64`] — canonical product name `Worship Presenter Web` and its identity framing (an operator tool used under pressure; nothing decorative).
- [Source: `src/app/layout.tsx:1-46`] — current file in full, confirming the metadata literal is the only boilerplate and `dynamic = 'force-dynamic'` is load-bearing (comment at line 22).
- [Source: `tests/theme-chrome.test.mjs:15-19,1926-1929,2013-2017`] — existing reads of `src/app/layout.tsx` this story must not perturb; the file header explicitly assigns the *shell behind* a projected route to Story 17.7, not this one.
- [Source: `_bmad-output/implementation-artifacts/stories/17-2-muted-foreground-contrast.md`] — previous story in this epic: closure convention, scope discipline, fresh-baseline measurement practice.
- [Source: `package.json`] — `"name": "worship-presenter-web"` and the authoritative `scripts.test` file list.
- [Source: `_bmad-output/project-context.md:36,77-87,89-97,116`] — `node:test` testing convention, unregistered-test-file hazard, lint-baseline worktree-inflation caveat, and (at line 116) the mandatory public-repo commit/push guard requirement.

## Dev Agent Record

### Story Context Completion

Ultimate context engine analysis completed — comprehensive developer guide created. Verified against Epic 17 text, `DESIGN.md` Open Item 3 (the authoritative scope statement for this story), the current `layout.tsx` source, the previous story (17.2) for continuity, and the existing `theme-chrome.test.mjs` reads of this same file to ensure the new assertion does not collide with AC-1/AC-2 coverage. Confirmed `EXPERIENCE.md` and `deferred-work.md` carry no Story 17.3 obligations.

### Agent Model Used

claude-sonnet-5-thinking-high (bmad-create-story)

### Debug Log References

### Completion Notes List

### File List
