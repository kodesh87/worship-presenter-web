---
topic: bic-pptx-workflow UX
updated: 2026-07-30T07:45
---

- (decision) Fast path UX design for Web SPA operator interface and presentation views, heavily utilizing standard Shadcn/Tailwind.
- (decision) AD-UX-1: Clean, high-contrast, uncluttered UI meant for weekend operations. Utilize simple Tailwind/Shadcn defaults with minimal custom brand colors.
- (event) spines finalized
- (change) Update run 2026-07-29: as-built ratification, NOT a redesign. Trigger was drift found by the implementation-readiness assessment: both spines last touched 2026-07-19 and missed two shipped surfaces.
- (override) SHAPE CORRECTION: DESIGN.md previously carried a surfaces/route table and a component-inventory list. Under the bmad-ux contract DESIGN.md owns visual identity per the Google Labs design.md spec; IA and surfaces belong to EXPERIENCE.md. Content moved rather than deleted. The earlier suggestion to keep DESIGN.md as a 'surface register' was wrong and is withdrawn.
- (decision) AD-UX-2 [ADOPTED]: the palette is achromatic by shipped reality, not by taste. Every token in src/app/globals.css is oklch(L 0 0) -- chroma exactly zero -- except --destructive and a stray dark-mode --sidebar-primary (oklch 0.488 0.243 264.376, an unused shadcn leftover; there is no sidebar, the app uses Header.tsx). AD-UX-1's 'minimal custom brand colors' is literally zero brand hue. Ratified from code, not invented.
- (decision) AD-UX-3 [ADOPTED]: typography is Geist Sans / Geist Mono via next/font/google, verified in src/app/layout.tsx. --font-heading aliases --font-geist-sans, so there is no separate display face. Radius ramp derives from a single --radius: 0.625rem (10px) -> sm 6 / md 8 / lg 10 / xl 14 / 2xl 18 px.
- (assumption) OPEN GAP (do not claim as working): dark mode is unreachable as shipped. globals.css defines a full .dark block (33 tokens) behind @custom-variant dark (&:is(.dark *)), but no ThemeProvider is mounted anywhere -- next-themes is imported only by src/components/ui/sonner.tsx for toast theming. Nothing ever adds .dark to <html>, so the entire dark palette is dead code. The previous DESIGN.md claimed 'Dark/light via existing theme tokens', which was false.
- (assumption) OPEN DEFECT: src/app/layout.tsx still exports the create-next-app metadata -- title 'Create Next App', description 'Generated by create next app'. The browser tab and any bookmark of the production hub read 'Create Next App'. Recorded here as UX truth; fixing it is a one-line product change, not a UX decision.
- (change) Surfaces added to IA that shipped after the 2026-07-19 snapshot: /services/new (Epic 14 worship web input) and /admin/artifacts (Epic 16 canvas editor). /login was previously described only as 'Auth'. Verified by enumerating src/app/**/page.tsx: /, /login, /announcements, /services/new, /services/[id], /services/[id]/slideshow, /services/[id]/present, /services/[id]/present/projector, /admin, /admin/artifacts.
- (decision) Component reality (src/components): shadcn ui/ = button, card, dialog, popover, sonner (five only). App components = Header, LogoutButton, HymnNumberAutocomplete, ImageUploadField, ImageFieldPreview, SlideView, SlidePreviewList, artifacts/ArtifactSlide, admin/ArtifactEditor. Detailed per-surface UX specs stay in SPEC companions (form-fields.md, edit-page-chrome.md, slide-kinds.md) and are referenced, not duplicated.
- (decision) status set to 'final' (not 'as-built') on both spines deliberately: bmad-ux Create mode scans ux_output_path for folders whose DESIGN.md status is NOT final and offers to resume them. Leaving 'as-built' would make this completed run look unfinished and trigger a spurious resume offer on the next run. The as-built honesty note lives in the body instead.
- (event) spines distilled and finalized 2026-07-29 (Update mode). DESIGN.md reshaped to the Google Labs design.md spec (frontmatter tokens + canonical section order); EXPERIENCE.md given the full section set (Foundation, IA, Voice and Tone, Component Patterns, State Patterns, Interaction Primitives, Accessibility Floor, Key Flows) plus an invented 'Venue & Projection Constraints' section for Sabbath-specific constraints. Three named-protagonist flows with climax beats. Six open items recorded rather than papered over. HELD PENDING USER: the opt-in Reviewer Gate and the two doc_standards editorial passes.
- (event) Reviewer Gate run at Update/Finalize (sequential). Lens: rubric walker only -- finalize_reviewers is empty for bmad-ux; accessibility ad-hoc lens judged unearned (internal single-congregation tool, a11y already an explicit Open Item). Findings: 5 high, 6 medium, 8 low. All high and medium resolved before polish per the gate rule. Artifacts: review-rubric.md, validation-report.md.
- (assumption) NEW OPEN ITEM from the gate: computing the contrast ratios the rubric demands showed muted-foreground on muted is approx 4.4:1 -- BELOW WCAG AA (4.5:1) for normal text -- and on background approx 4.7:1, only narrowly passing. This is shadcn's default token and carries secondary text throughout. Values are Oklab-derived estimates (Y approx L^3), NOT tool-measured; both must be confirmed with a contrast checker before any accessibility claim. A single token darkening would fix it globally.
- (assumption) HONESTY BOUND on the state coverage added during gate fixes: per-surface states (lost sync, save rejected, last-admin refusal, empty plan) are the DESIGNED behavior distilled from the architecture invariants. Whether the shipped code implements each was NOT verified. Recorded as EXPERIENCE.md Open Item 6 rather than asserted as working; verification belongs to the implementation-readiness assessment.
- (event) Gate fixes: 3 new flows (4 announcements, 5 account onboarding) + 4 failure branches (1a manual create, 1b stale write, 2a lost projector, 3a validation refusal) so all ten IA surfaces are landed on; per-surface state table added; Responsive & Platform section added (tablet/phone recorded OUT OF SCOPE, not undecided); component tables reconciled across both spines; {DESIGN.md} brace pseudo-token replaced with real relative links; glossary standardised on 'run sheet'; contrast table added. DEVIATION STATED in validation-report.md: HTML twin + browser open skipped (that pipeline targets the Validate intent where findings are delivered, not applied).
- (question) OUTSTANDING: doc_standards polish passes (skill:bmad-editorial-review-structure then skill:bmad-editorial-review-prose) are part of the Finalize sequence and are NOT opt-in. Not yet run.
- (event) doc_standards pass 1 of 2 complete: bmad-editorial-review-structure, reader_type=llm, model Reference/Database. 10 recommendations (6 action, 3 preserve, 1 question). Applied: de-duplicated Open Items across the pair by ownership (DESIGN owns tokens/visual, EXPERIENCE owns behavior, each links the other); contrast caveat stated once instead of three times; muted-foreground promoted to Open Item 1 on severity order; offline-PPTX primacy stated once in Foundation instead of three times; Flow 5 stopped restating rules it should merely exercise; 'Stale write (above)' antecedent named. Net ~260 words (5.6%). PRESERVED against generic editorial instinct: thin Elevation/Shapes sections (spec order-locked, thinness is accurate), YAML frontmatter comments (load-bearing for llm readers), and the 'every surface is landed on by a journey' assertion (a testable IA invariant, not filler).
- (event) doc_standards pass 2 of 2 complete: bmad-editorial-review-prose, reader_type=llm. 6 fixes: glossary violation DESIGN.md:107 ('run-sheet'); duplicate/inaccurate lead-in left behind by pass 1; 'Open Items 1' -> 'Open Item 1'; unclear antecedent 'the tokens above'; and two editorial-voice phrases removed from EXPERIENCE.md outside its journeys ('Two rules with teeth', 'Silence here is the failure mode that hurts mid-service') per the contract that only DESIGN.md carries voice. No hedging found in either file.
- (event) SELF-CORRECTION recorded in validation-report.md: that report claimed the glossary drift was 'resolved' when only EXPERIENCE.md had been fixed. The prose pass found DESIGN.md:107 still carrying 'run-sheet'. Report annotated rather than silently edited.
- (event) bmad-ux Finalize CLOSED. Outstanding from this run: one QUESTION left to the user (the honesty notes in DESIGN.md and EXPERIENCE.md are near-duplicates in substance; one could be canonical and the other a one-line link, but that is a tone decision). Key-screen mocks deliberately not rendered: for an as-built ratification the running application is itself the visual reference.
- (override) F4-1 FIXED (readiness assessment Step 4): Key Flows rewritten to use the PRD's protagonists verbatim per the bmad-ux mirror-source-names rule -- Sari, Bimo, Elen. The invented Yohana/Yosef are gone. 'Yosef' additionally had to go because it collided with a real predecessor's given name found during the PII sweep. Pronouns follow the PRD: Bimo he, Elen she, Sari she. Flows now carry explicit UJ labels.
- (change) Flow set expanded 5 -> 7 to close F4-3 and F4-4: UJ-5 (create by hand) promoted from a sub-branch to Flow 2; UJ-3 (Saturday Telegram correction) given Flow 4, scoped to the reviewer-facing half since the confirmation exchange belongs to the agent. UJ-1 documented as deliberately outside this product's surfaces -- Sari never opens the hub.
- (change) F4-5 FIXED: three behaviors whose FR/NFR already specified them had no UX owner. Added (a) FR-16 projector blanking as a full state -- black at any time and restore, deck position preserved, projector window not lost, scripture overlay undisturbed, blanked state indicated on the operator view, reload-while-blanked comes up blank -- plus Flow 3 step 5 and Branch 3a; (b) NFR-5 general unmapped-input channel as a cross-cutting state, explicitly not hymn-only; (c) NFR-2 deck-generation progress, with double-fire prevention.
- (event) PII remediation 2026-07-29: three real congregation names removed from the planning corpus (21 occurrences across brief/.memlog/addendum, prd.md, pressure-test-findings.md) and replaced with invented ones. Fingerprints added to FORBIDDEN_NAME_HASHES in tests/public-repo-guard.test.mjs. Full suite green: 322 pass, 0 fail, 1 skipped. UNRESOLVED: the names remain in this public repository's git history (commit 1ced308, pushed to origin/main) -- a working-tree edit cannot reach that. Escalated to the user.
- (change) Update run 2026-07-30 (bmad-ux Update mode). Trigger: Correct Course 2026-07-29 routed readiness findings F4-1/F4-3/F4-4/F4-5/F4-6 plus a dead-code correction here. SCOPE CORRECTED ON READING THIS MEMLOG: F4-1, F4-3, F4-4 and F4-5 were already closed by the 2026-07-29 run (entries above), and F4-7 was closed by Correct Course, which added EXPERIENCE.md to epics.md inputDocuments. Real remaining scope: the dark-mode dead-code claim, F4-6, and verifying the per-surface states this file admitted were unverified.
- (override) CORRECTION, AD-UX-4: the claim that the dark palette is dead code is FALSE and was carried by three artifacts (DESIGN.md frontmatter + Open Item 2, the readiness report, and this memlog entry of 2026-07-29). Verified against src/ at 2026-07-30: PresenterOperator.tsx:449 and SlideGridDialog.tsx:176 both pin className dark on their own wrappers, and globals.css:5 @custom-variant dark (&:is(.dark *)) matches any descendant, so the palette renders today in the two surfaces used while a service is running. What is actually missing is OPERATOR CHOICE, not reachability. The earlier grep looked only for a mounted ThemeProvider and concluded from its absence; that inference was wrong.
- (event) EXPERIENCE.md Open Item 1 discharged by verification against src/ at 2026-07-30. It said four per-surface states were designed-but-unverified and deferred the check to the readiness assessment; that assessment ran and never answered it, so it is answered here. SHIPPED: FR-16 projector blanking, all four PRD consequences (PresenterOperator.tsx blank state + toggleBlank + keyboard B + aria-pressed + Projector blanked indicator; setIndexAndSync carries blank so advancing while blanked keeps the projector black; blankStateOf answers request-sync so a projector reloaded while blanked comes up blank). SHIPPED: last-admin refusal, as a refusal not a warning (auth/accounts.ts:158 role change, :195 delete). SHIPPED: registry save rejected, keeping the operators work (ArtifactEditor.tsx:728,760). UNREACHABLE not unimplemented: empty plan. slide-plan.ts:253 pushes the welcome leaf unconditionally at the head of every plan, so zero slides cannot occur; what IS implemented on /slideshow is a plan-build FAILURE card, a different state.
- (assumption) NEW OPEN ITEM, and a documentation defect this file caused: LOST SYNC IS SPECIFIED BUT NOT SHIPPED. EXPERIENCE.md carried it as a cross-cutting state and Flow 3 Branch 3a asserted the presenter surfaces lost sync immediately. Verified false at 2026-07-30: there is no liveness detection of any kind. BroadcastChannel is fire-and-forget with no delivery signal, present-channel.ts defines no heartbeat or pong message, and projectorRef.current.closed is consulted only inside openProjector, i.e. only if the operator happens to click Open projector again. The only surfaced projector state is projectorBlocked, which is the popup blocker, not a lost window. An operator can therefore advance the deck for the rest of a service with nothing on the second screen and no indication. Routed to a story rather than left as prose.
- (decision) AD-UX-5 [ADOPTED], closing F4-6: the congregation-facing deck gets a STATED boundary rather than a new specification. DESIGN.md owns operator chrome; the Artifact Registry payload plus its SPEC companions own slide interiors; both were already true and both are now written down as a boundary instead of appearing as a silence. What is NOT owned is named explicitly: is this readable from the pews. No artifact answers it, no test can, and the only control is the pre-launch projector inspection carried as an owner action item in sprint-status.yaml. Deliberately NOT invented here: minimum type sizes, contrast floors or a readability spec for projected slides. Per the bmad-ux capture rule, geometry that arrived from a real source deck is evidence and inventing numbers over it would replace evidence with taste.
- (decision) AD-UX-6 [ADOPTED]: every Open Item in both spines now names the story key that owns it, so the pair stops being an orphan defect list. DESIGN.md: muted-foreground to 17.2, dark-mode choice to 17.1, metadata to 17.3, dead chart/sidebar tokens left unowned on purpose as harmless. EXPERIENCE.md: unsaved canvas to 17.4, lost sync to NEW story 17.5, accessibility left unowned because it needs a scoping decision before a story can be written. This is the loop Correct Course opened on 2026-07-29 and did not close: findings were routed to skills, not to keys.
- (event) Stale copies of the corrected claim hunted down rather than left behind, because the false statement had propagated across four artifacts. FIXED IN PLACE: epics.md Story 17.1 So-that clause ended with stops being dead code; DESIGN.md frontmatter comment, Colors note on --sidebar-primary, and Open Item 2; story 17-1 AC-7, which told the implementer to correct a claim that is now already corrected. ANNOTATED NOT REWRITTEN: implementation-readiness-report-2026-07-29.md:91, a dated assessment rather than a living contract, marked CORRECTED inline with the evidence. LEFT AS IS: memlog entry 13, superseded by entry 28 -- this log is append-only and rewriting history here would defeat its purpose.
- (event) DEVIATIONS from the Finalize sequence, stated rather than skipped silently. (1) Reviewer Gate NOT offered and NOT run: this session operates under an explicit instruction not to spawn subagents unless the user asks, and every lens is a parallel subagent. (2) doc_standards passes bmad-editorial-review-structure and bmad-editorial-review-prose NOT run as full passes -- both files went through them on 2026-07-29 and this Update is a bounded delta. Ran a targeted self-check against the specific rules those passes found last time instead: glossary standardised on run sheet (grep for run-sheet across both spines returns nothing); Open Items not duplicated across the pair, DESIGN owning tokens and EXPERIENCE owning behaviour with each linking the other; editorial voice kept out of EXPERIENCE.md outside its journeys, which caught and removed one line in the state table; no hedging introduced. NOT checked: everything else those two passes cover. The user was told, so a full pass can be requested.
- (event) spines finalized 2026-07-30 (Update mode). Both files status final, updated 2026-07-30. Key-screen mocks again deliberately not rendered: this is an as-built reconciliation, so the running application remains its own visual reference. Tracker changes made in the same change set per the AGENTS.md rule that a UX/IA delta updates its artifacts together: epics.md gained Story 17.5 and Epic 19 + Story 19.1; sprint-status.yaml gained keys 17-5-projector-liveness, epic-19 and 19-1-song-title-suppression-as-data, and closed two action items -- the bmad-ux routing item and the Epic 15 Consider item.
- (event) doc_standards pass 1 of 2 run 2026-07-30: bmad-editorial-review-structure, reader_type=llm, model Reference/Database for both spines (EXPERIENCE.md with a Tutorial/Linear tail for Key Flows). 9 recommendations, owner accepted all: 5 CONDENSE, 1 MOVE, 1 MERGE, 1 PRESERVE, 1 QUESTION. THE PATTERN THE PASS FOUND: six of nine findings were against text written earlier the same day, and four were the same mistake -- recording HOW a finding was discovered inside the contract, when the memlog and commit messages already hold that permanently. Correcting a false claim justified narrating it once; it did not justify narrating it in four artifacts. Applied: honesty note un-stranded (the F4-6 subsection had been inserted in front of a note about the whole file, so it read as a footnote to slide ownership); presenter states promoted out of a 150-word table cell into their own table, because a cell that long defeats the random access a Reference table exists for; F4-6 boundary de-duplicated across the pair, canonical in DESIGN.md with EXPERIENCE.md reduced to one bullet; lost-sync evidence left with Epic 20 story 17.5 instead of repeated in Open Item 1; discovery narrative trimmed from DESIGN.md Open Item 2 and from Flow 3 Branch 3a; contrast prose stopped restating its own table's WCAG column. PRESERVED and re-listed so a third pass does not re-litigate: frontmatter comments, thin Elevation/Shapes, the every-surface-is-landed-on assertion. Actual reduction 259 words (6361 to 6102, 4.1 percent) -- well under the 580 estimated, because the estimates assumed cuts that would have taken load-bearing content with them, and one recommendation ADDED words to gain scannability.
- (event) doc_standards pass 2 of 2 complete 2026-07-30: bmad-editorial-review-prose, reader_type=llm. 12 entries: 11 fixes applied, 1 left as a query. ONE STYLE-GUIDE VIOLATION, same class the 2026-07-29 prose pass caught twice: editorial voice in EXPERIENCE.md outside a journey -- A specification that reads like a description of working software is the failure mode this marker exists to prevent, in the honesty note. Removed; the preceding sentences already carried the fact. Four unclear antecedents fixed (three instances of it or this one referring back to a deferred question or an unnamed fourth state). Two ambiguous uses of here in DESIGN.md restored to in this repository and this file -- the structure pass had traded that precision for word count, and for an llm reader unambiguous reference outranks brevity, so pass 2 partially reversed pass 1 on purpose. One positional table reference (the last two rows) replaced with the row names, because a positional pointer breaks silently when a row is added. Filler removed: Note that, actually. One double negative unwound. ONE PRE-EXISTING MARKDOWN DEFECT found at a boundary neither pass was looking at: EXPERIENCE.md had a heading directly abutting the final list item of Flow 7 with no blank line, which some renderers absorb into the list. LEFT AS A QUERY for the owner: the surface whose entire job is to report what the congregation sees will show no sign of it in Open Item 1 -- rhetorical in form but it names why the gap matters, and comparable phrasing in this file survived the previous pass, so it was flagged rather than decided.
