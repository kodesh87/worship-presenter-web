---
topic: BIC Worship Presentation Automation
updated: 2026-07-19T15:24
---

- (decision) Intent=Create PRD via Fast path; stakes read as internal-tool (solo dev, real weekly use, target: sticks >=1 quarter); output language English, conversation Bahasa Indonesia
- (decision) Web render scope v1 = PPTX download AND in-browser web slideshow replicating dual-screen presenter mode (projector full-screen + laptop current/next + run-sheet). Not PPTX-only.
- (decision) Revision flow v1 = BOTH web-form edit per week AND re-send corrected text to Telegram; either triggers regenerate.
- (decision) Auth v1 = per-person accounts (individual login), not single shared password.
- (decision) Fidelity v1 = rebuild clean master template (real layouts/placeholders) with close-but-not-pixel-perfect resemblance; safe font substitution acceptable (avoid commercial Cooper BT licensing/headless risk).
- (event) Full PRD draft written (18 FRs across 8 features; spine + NFRs/Constraints/Dependencies clusters; 9 open questions; 8 assumptions indexed).
- (change) PRD restructured into delivery phases. Phase1(MVP)=PPTX automation: Telegram->picoclaw->API assembly, offline PPTX download, single-screen full-screen Web Slideshow (NO presenter view), hub(list/preview/run-sheet/manual delete), auth. Phase2=edit(web form)+Telegram correction+regenerate+auto retention cleanup. Phase3=dual-screen presenter view. Phase4=standalone scripture display.
- (decision) OQ1 Scripture display = standalone special feature, decoupled from PPTX workflow, lives in web app, from user-provided Verse Database. Deferred to Phase4.
- (decision) OQ2 confirmed: unknown SDAH Number does NOT block Service creation (flag-and-continue).
- (decision) OQ3 Telegram correction targeting: app exposes API to list + get-detail of worship Services queryable by text; picoclaw uses it to identify which Service a correction targets and confirms.
- (decision) OQ4 Web Slideshow: requires online for initial load, usable after loaded; data operations may show connection errors offline. PPTX remains sole hard offline guarantee.
- (decision) OQ5 Auto-cleanup = configurable retention (default ~2 months) that deletes ONLY generated PPTX + uploaded videos. Posters, participant text, worship rows persist and are manual-delete only.
- (decision) OQ6 Roles required: Admin, Operator (Multimedia Team), Events Department. Supersedes earlier 'all operators equal / no role tiers'.
- (decision) OQ7 Presenter view operator screen also shows participant list to help operator (in addition to current/next slide + run-sheet).
- (decision) OQ8 Theme verse is part of the Telegram Weekly Data Payload (variable), not the fixed Template Skeleton.
- (decision) OQ9 Concurrency web-vs-Telegram = first-save-wins (optimistic lock); the second save fails with an error.
- (change) Events Department elevated from Telegram-only non-user to an app role (with access), alongside Admin and Operator.
- (event) PRD revised: added §6 Delivery Phases (Phase1 MVP..Phase4), split FR-10/FR-10b retention, roles in FR-18, FR-12 targeting API, FR-13b concurrency, FR-15 online-load/offline-after, FR-19 standalone scripture display, theme verse in payload. 20 FRs total.
- (decision) Phases resequenced to 6: P1 MVP=Telegram intake+hymn-validating input API+announcement-list API+deck generator+offline PPTX download+service list+run-sheet+web-form edit+regenerate+auth(2 roles). P2=single-screen web slideshow(+slide preview FR-9). P3=Telegram correction FR-12+first-save-wins FR-13b. P4=retention auto-delete of generated PPTX only FR-10b. P5=dual-screen presenter mode FR-16. P6=scripture display FR-19. Phases 2-6 nice-to-have, contingent on P1 proving valuable.
- (decision) FR-2 reshaped: picoclaw does NOT resolve hymns; one app input API validates SDAH numbers against Hymnal Database and resolves lyrics server-side; invalid number flagged in response, non-blocking.
- (decision) FR-3 reshaped: announcements are IMAGES ONLY - MP4/video upload out of scope entirely; persistent ordered announcement list with recurring items; picoclaw instructs keep/replace/add/remove+order via API; one-off flyers are occasional (special reports), not weekly.
- (decision) FR-5 expanded: lyric slides governed by readability, not just pagination - each verse and each reff starts a new slide; overflow splits across slides; songs may have no reff; slide count = f(verses, reff availability, readability).
- (decision) FR-6: theme verse text is sender-supplied via picoclaw (translation choice matters; owned Verse DB is KJV-only and not used for deck slides); when absent, standing skeleton theme verse is used.
- (decision) Roles reduced to 2: Admin + Operator. Events Department members get Operator accounts for now; finer split deferred.
- (decision) Retention (P4) deletes ONLY generated PPTX. Worship rows, posters, participant text all persist (manual delete only). MP4 moot (out of scope). Post-cleanup regeneration uses stored assets, need not be byte-identical.
- (decision) Web slideshow caching depth = single Sabbath service (one worship, one deck).
- (decision) Telegram correction targeting: picoclaw asks for confirmation; default proposal = nearest upcoming Sabbath; explicit date supported after confirmation.
- (decision) FR-19 Scripture Display: KJV-only Verse Database; surfaced INSIDE Presenter Mode, not a separate app screen - hence P6 follows P5.
- (change) User supplied annotated 68-slide operational map (when each slide is shown, payload-changeable inventory, closing-prayer person = sermon speaker, announcements occasional/one-off, guest+speaker personal PPTX out of scope) - captured to addendum.md; PRD gains Deck Blueprint distillation in 4.2.
- (assumption) Intercessory response songs (slides 36/38, #671/#684) treated as FIXED skeleton per user slide map even though sample Rundown lists them weekly - flagged as open question.
- (assumption) FR-9 slide-level preview moved to P2; P1 Friday review = Run-Sheet + web-form data + downloaded PPTX - flagged for user confirmation.
- (event) Adversarial verification (3 parallel reviewers): fidelity PASS, remnants PASS, consistency 1 medium+low. Applied all 7 fixes: §4.3 phase-tag removed, SM-1 notes FR-9, assumption locator, FR-1 standing-default term, FR-9 extends UJ-2, JTBD reviewer wording, FR-19 phase clarity.
- (decision) Reconciled newly-spawned open questions against user's prior answers: RESOLVED retention default=2mo, slides 36/38 fixed standing pair, theme/verse-reading sender-supplied text, Phase-1 review surface=runsheet+data+PPTX. Only 3 items remain, all deferred-by-choice (finer events-dept role, scripture trigger UX, retention per-service granularity). §8 reframed, §12 trimmed to 4 real assumptions.
- (event) Pressure-test conducted (pre-mortem + red-team + assumption audit + second-order), grounded in the 67 rendered source slides. 3 CRITICAL / 5 HIGH / 5 MEDIUM / 4 LOW findings + 3 scope challenges. Report at pressure-test-findings.md. PRD itself unchanged.
- (assumption) [C1, load-bearing] Phase-1 Friday review has NO in-browser slide preview (FR-9 is Phase 2), so verifying the deck requires opening the PPTX in desktop PowerPoint - reintroducing the exact desktop dependency the product exists to remove, and leaving SM-4/SM-C1 (readability/no-cramped-lyrics) unobservable in the MVP. Scope challenge S1: pull a minimal render-to-image preview into Phase 1.
- (assumption) [C2, unresolved] Font strategy is contradictory: FR-14 'fonts intact offline' vs 4.2/11 'bundled or substituted'. Undecided between embed-in-file (licensing/size risk) vs install-on-presentation-laptop (per-machine setup dependency). Wrong choice reflows lyrics live on the projector. Acceptance test must be 'renders on a clean machine that never had these fonts'.
- (assumption) [H1, unspecified] Image->role binding has NO syntax in the rundown; picoclaw's mapping of uploaded images to sermon-graphic vs family/youth-photo vs 0..N ordered announcement flyers is undesigned. Source slide 56 already shows a broken-image placeholder that week. Needs an explicit binding convention + a missing/unbound-image visible failure before finalize.
- (assumption) [H2, silent failure] A valid-but-WRONG SDAH number is undetectable: FR-2 only flags INVALID numbers; the human-typed title next to the number (a free checksum) is discarded, so a transposed digit yields confident correct-looking lyrics for the wrong hymn. Scope challenge S2: fuzzy-compare typed title vs DB-resolved title, flag on mismatch.
- (assumption) [H3, n=1 risk] Parser is fit to ONE sample rundown; only hymn-invalidity fails visibly - no general 'unmapped input' surface for dropped roles/timings/compound fields/curly quotes/forward-ref 'The Speaker'. Scope challenge S3: collect 5-10 historical rundowns+decks before locking parse rules; generalize the flag beyond hymns.
- (assumption) [H4, foundational] Hymnal Database + picoclaw are unvalidated deps not yet in hand (brief memlog: 'belum di tangan'); FR-5 readability presumes clean verse/refrain structure. Make 'acquire+validate Hymnal DB (structure/coverage/numbering)' and 'confirm picoclaw customizable' explicit Phase-1 go/no-go spikes.
- (assumption) [C3+M5, bus factor] The 3-layer solo-dev system relocates the single-point-of-failure from a learnable manual task to an unlearnable distributed system, against a hard weekly Sabbath deadline with the manual skill deliberately atrophied. Needs an explicit break-glass hand-editable-template fallback + dev-unavailable runbook in 9.
- (assumption) [M1, PII] Family/Youth-of-the-Week PII (full names, real photos, prayer requests, likely minors - see source slide 56) persists indefinitely: retention (FR-10b/SM-C2) deletes ONLY the PPTX, never the payload; 9 assumes no consent/retention regime needed. Validate the church's actual expectation; consider a retention window on PII-bearing payload.
- (decision by user) [resolves C1] Phase-1 WITHOUT a web slide preview is ACCEPTED; the priority is that the workflow is no longer manual. Consequence accepted explicitly: the system must be reliable - generation correctness becomes load-bearing and Friday verification leans on the downloaded PPTX. Preview stays Phase 2.
- (decision by user) [resolves H1] Image binding decided: sender posts the images in sequence AND writes a textual description explaining them; picoclaw orders/assigns roles by interpreting that description. Residual to spec: the description must reliably distinguish sermon-graphic vs family/youth-photo vs 0..N flyers, plus a visible failure for count/description mismatch and missing images (broken placeholder seen on source slide 56).
- (decision by user) [resolves C2] Font strategy decided: PREFER embedding fonts in the generated PPTX; if headless embed is not feasible, fall back to a STANDARDIZED font that is installed on the presentation laptop(s). Pick a freely-licensed face. Acceptance: verify render on a clean machine when embedded, or document+install the standard font on the presentation machine(s) when relying on install.
- (decision by user) [waives C3/M5] Solo-dev 3-layer maintainability / bus factor is explicitly NOT a consideration for the maintainer: a stable system yields large net time savings. Risk accepted by the owner. (Cheap optional insurance, no added maintenance: keep the hand-editable master template as a break-glass fallback.)
- (decision by user) [resolves risk-area-5] Offline downloaded PPTX is accepted as a sufficient day-of guarantee for Phase 1; the only sub-risk (fonts) is handled by the font decision above.
- (event) After maintainer decisions, STILL OPEN: H2 (valid-but-wrong SDAH title checksum), H3 (parser robustness + unmapped-input/missing-image surface + historical-rundown corpus), H4 (validate Hymnal DB + picoclaw), M1 (PII consent/retention), and lighter M2/M3/M4. NOTE: decisions on C1 (no preview) and H1 (description-driven image ordering) INCREASE the weight of H3 - less human visual gating + more free-text parsing load on picoclaw - so generator readability tests and a visible unmapped/missing-image flag matter more now, not less.
- (decision by user) [resolves H2] No auto-checksum. Instead, on save picoclaw reports back to the sender WHAT WAS STORED - including each resolved Hymn title - so a valid-but-wrong SDAH number is caught by the human reading the readback. Folded: FR-2 (API echoes resolved titles) + FR-1 (picoclaw save-readback to sender).
- (decision by user) [resolves M1] PII kept AS-IS: access-by-Role + manual delete only, no consent/retention regime; family/youth names+photos+prayer-requests persist until manual delete. Owner accepts the risk for v1. Folded: 12 (marked decided, not open) + 9 unchanged.
- (decision by user) [resolves M3] Intercessory response songs #671/#684 stay FIXED skeleton (not editable payload) - confirmed, matches the user slide map. No PRD change; the prior open assumption is now closed.
- (decision by user) [resolves M4] Service keyed by date: a re-send of the Rundown for the same date UPDATES the existing Service's payload (no duplicate). Consequence: Phase 1 has no web-vs-Telegram concurrency guard (that is FR-13b/Phase 3), so a re-send overwrites current payload incl. prior web-form edits. Folded: FR-1 consequence + 6 Phase-1 note.
- (event) PRD + addendum UPDATED to fold all maintainer decisions (first edits to prd.md this cycle): FR-1 (image binding by description, save-readback, re-send-updates-by-date), FR-2 (resolved-title echo), 4.2/FR-14/11 (font embed-else-standardize + clean-machine acceptance), 9 (break-glass fallback), 10 (general unmapped-input + missing-image surface), 6 (new Phase-1 go/no-go spikes block), SM-3 (week-4 leading gate), 12 (font+consent marked decided). pressure-test-findings.md gains a Resolution log. Scope unchanged (no FRs added/removed).
- (decision) Added FR-11b requirement: Create a Service via Web Form in Phase 1 as a second channel alongside Telegram/picoclaw
- (decision) Split weekly photo into two separate fields (familyPhotoUrl and youthPhotoUrl) in the Weekly Data Payload and slide plan
- (decision) Integrated service form flyer management directly with the live announcement_items database table (allowing reordering, deleting, and toggle recurring/one-off of master and service-specific flyers)
- (event) Updated prd.md and addendum.md to include web creation form (FR-11b), split family/youth photos, and integrate announcement manager with the form
