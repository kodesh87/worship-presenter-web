---
scope: bic-pptx-workflow
purpose: build-substrate
altitude: initiative
updated: 2026-07-17T14:06
---

- (direction) Fast path architecture generation requested by user for bic-pptx-workflow project.
- (decision) AD-1: Use a web-based client-side presentation rendering engine with a local presentation format export (PPTX or equivalent) as the fallback. Binds: Web render, offline support. Prevents: Depending solely on venue internet.
- (decision) AD-2: Single repository monolith to reduce operational overhead for a solo developer. Binds: API, Web App, Agent skill logic. Prevents: Complex deployment and synchronization.
- (decision) AD-3: Maintain strict separation between the data ingestion layer (Telegram/Agent) and the presentation layer (Web App/API), using standard JSON as the interface. Binds: Data boundaries. Prevents: Tightly coupled integrations.
- (event) spine finalized
