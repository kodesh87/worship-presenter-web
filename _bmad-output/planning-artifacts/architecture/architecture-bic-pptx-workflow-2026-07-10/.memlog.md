---
scope: bic-pptx-workflow
purpose: build-substrate
altitude: initiative
updated: 2026-08-01T21:00
---

- (direction) Fast path architecture generation requested by user for bic-pptx-workflow project.
- (decision) AD-1: Use a web-based client-side presentation rendering engine with a local presentation format export (PPTX or equivalent) as the fallback. Binds: Web render, offline support. Prevents: Depending solely on venue internet.
- (decision) AD-2: Single repository monolith to reduce operational overhead for a solo developer. Binds: API, Web App, Agent skill logic. Prevents: Complex deployment and synchronization.
- (decision) AD-3: Maintain strict separation between the data ingestion layer (Telegram/Agent) and the presentation layer (Web App/API), using standard JSON as the interface. Binds: Data boundaries. Prevents: Tightly coupled integrations.
- (event) spine finalized
- (version) Stack table resynced to package.json on 2026-07-29: Next 16.2.10, React 19.2.4, TypeScript ^5, Tailwind ^4, better-sqlite3 ^12.11.1, pptxgenjs ^4.0.1, jszip ^3.10.1, fabric ^6.6.1, @base-ui/react ^1.6.0. Previous table claimed Next v14+/React v18+/pptxgenjs v3+.
- (event) Structural seed tree root renamed bic-pptx-workflow/ -> worship-presenter-web/ (legacy repo is frozen); child_spines now points at architecture-epic-16.
- (event) Reviewer Gate run (sequential; subagents unauthorized): lint_spine 0 findings, rubric walker + both finalize_reviewers lenses. Reviews in reviews/. 9 findings, 2 HIGH.
- (decision) AD-5 [ADOPTED] Single request gate in src/proxy.ts: matcher regex IS the authorization boundary (new exclusion ships with its tests/proxy-matcher.test.mjs assertion), per-request SQLite session re-check fails closed, /api/webhook secret-only, safeNextPath on redirects, no-store+Vary:Cookie on gated responses (Cloudflare Tunnel edge cache would otherwise bypass the origin), never reintroduce middleware.ts (Edge runtime loses the DB re-check). Binds: every route/page. Prevents: per-route authz drift and privilege surviving demotion/logout.
- (decision) AD-6 [ADOPTED] Optimistic concurrency: every service mutation carries client updated_at as precondition; stale value -> 409. Binds: service mutation APIs, hub edit UI, webhook corrections. Prevents: operator edit silently erased by a late correction.
- (decision) AD-7 [ADOPTED] buildSlidePlan is the only slide-order and content source for PPTX, slideshow, presenter, projector. Binds: all render surfaces. Prevents: projector and PPTX showing different slides mid-service.
- (decision) AD-8 [ADOPTED] Image refs resolve only through shared src/lib safety helpers (announcement allowlist + /api/uploads/<32-hex>, registry /assets/). A new vocabulary extends an existing helper, fail-closed. Binds: announcements, uploads, registry refs, PPTX embedding. Prevents: a new surface reintroducing SSRF via its own laxer resolver.
- (decision) AD-9 [ADOPTED] Schema evolution through startup DDL on the getDb path only; no migration framework without an explicit product decision. Binds: SQLite schema, bootstrap. Prevents: two sources of schema truth between dev and a fresh container.
- (decision) AD-10 [ADOPTED] One presenter sync channel via @/lib/present-channel BroadcastChannel; no second channel name and no server realtime channel. Binds: presenter, projector, slideshow. Prevents: split sync where the projector ignores one controller.
- (event) Gate fixes applied: 'Web SPA' -> App Router/Server-Components-by-default (contradicted the codebase); Deferred refreshed (auth hardening and Artifact Registry moved to Shipped; observability named instead of left silent; nine single-layer APIs recorded); structural seed now shows src/proxy.ts, data/local/, tests/, .constitution/; structural diagram redrawn to carry the gate, the plan, and the sync channel; binds filled.
- (question) OPEN, routed out of this spine: _bmad-output/project-context.md instructs agents to 'respect middleware.ts matcher' but src/middleware.ts was deleted (gate is src/proxy.ts). project-context.md is the default persistent_facts entry for every BMad skill, so every agent loads a pointer to a deleted file. Owner: bmad-generate-project-context. docs/architecture.md:79 and docs/development-guide-monolith.md:86 carry the same stale prose, already tracked in deferred-work.md:170.
- (event) spine finalized
- (direction) OWNER DIRECTIVE 2026-07-30: enforce the BMad default of ONE architecture spine per project. The Epic 16 child spine is folded into this file and this becomes the only spine. Reason given: multiple specs are the BMad norm, multiple architecture spines are not - customize.toml states the default pattern fits one spine per project at the altitude above epics, and per-epic spines are an opt-in.
- (decision) Fold-in mechanics: epic-16 AD-1..AD-9 renumbered to AD-11..AD-19; AD-1..AD-10 of this file keep their numbers, so the INIT AD-n citation form is simply retired by dropping the prefix. AD map published in this spine AND in the epic-16 tombstone so old citations resolve from either end. Each renumbered heading carries its former identity inline. 36 live citations repaired in the same change set across epics.md, sprint-status.yaml, EXPERIENCE.md, DESIGN.md and spec-artifact-registry-authoring/SPEC.md; 5 path references repaired across spec-slide-artifact-model/SPEC.md, epics.md frontmatter, story 16-1, prd.md.
- (constraint) AGENTS.md rule Never renumber an existing AD-n was WAIVED ONCE by the owner for this merge - one file cannot hold two AD-1. Recorded as an explicit non-precedent in AGENTS.md, .agents/AGENTS.md and .cursorrules (all three verified byte-identical in the shared gate block afterwards). The waiver is defensible only because the rule intent is that citations stay resolvable, and the AD map plus same-change-set repair preserves that.
- (constraint) The fold-in removed a live ambiguity rather than only relocating text: while two spines existed, AD-6 meant optimistic concurrency in one document and the service snapshot in the other, and AD-9 meant startup DDL in one and cross-boundary keys in the other. One file, one meaning per number.
- (constraint) The epic-16 Inherited Invariants table was folded rather than dropped: each registry-specific binding note moved into the rule of the parent decision it qualified - registry rows and per-service snapshots into AD-4, the /admin/artifacts matcher note into AD-14, the registry optimistic-concurrency shape into AD-6, no-second-BroadcastChannel into AD-10, src/lib/registry boundary into the Boundaries convention. Two of its rows were escalations, not bindings, and are now Open items in this file.
- (question) Two Open items carried forward UNRESOLVED at the owner request (to be discussed after the consolidation): (1) the State consistency convention still says services are immutable once presented, which AD-16 contradicts and the owner has already corrected in substance - the row is struck through and flagged but the replacement wording is not written; (2) AD-9 Binds names the bootstrap path where AD-18 operates for values, and project-context.md line 85 states only the schema half.
- (event) spine finalized as the single project spine (AD-1..AD-19). lint_spine 0 findings.
- (constraint) Citation audit caught a bug the mechanical repair introduced, recorded because the failure mode is easy to repeat: the scripted replace only matched the PREFIXED forms (epic-16 AD-n, INIT AD-n). Citations written as BARE AD-n inside an epic-16 context were left alone and therefore silently retargeted to the wrong decisions - AD-4 became LiveServer durable paths instead of Global Template Administration, AD-6 became optimistic concurrency instead of the service snapshot, AD-9 became startup DDL instead of cross-boundary keys. Found by grepping every remaining AD-n in live files and reading each in context, not by the dangling-citation check, which reported clean. 16 bare citations corrected across epics.md and sprint-status.yaml. A dangling citation is visible; a retargeted one is not.
- (event) Consolidation verified: no dangling INIT/epic-16 citations in live files (only the deliberate AD-map note in epics.md), lint_spine 0 findings, public-repo guard 4/4, and the three gate files byte-identical in their shared block. Historical records left untouched by design - dated reports, UX and architecture review records, and both memlogs describe past state correctly and resolve through the published AD map.
- (event) Validate intent run 2026-07-30 at the owner request (is this BMad-conformant?). Reviewer Gate: lint_spine 0 findings, rubric walker against the good-spine checklist AND the spine template, plus both configured finalize_reviewers. Report in reviews/validation-2026-07-30-post-consolidation.{md,html}. 13 findings: 1 critical, 3 high, 4 medium, 5 low. Spine NOT modified, per the Validate intent.
- (question) CRITICAL C1 from that run: the merged spine now CONTRADICTS ITSELF - the State convention row is struck through as wrong while AD-16 states the opposite in force, and Open items item 1 says the replacement is unwritten. This was defensible while two spines existed (parent held stale text, child held the correction, conflict visibly cross-document); one file removed that alibi. BMad has no third state for a known-false statement left in the spine - triage resolves blockers or defers with a revisit condition in the memlog.
- (question) HIGH H1/H2 from that run, both traceable to one skipped step: the template mandates a Capability -> Architecture Map when a spec drove the run, and it is absent - which is why nobody noticed CAP-1 has NO invariant. Nothing in the spine fixes that deck order and per-slide liturgical flags are registry data rather than TypeScript constants, so a unit could comply with every AD and still add a fourth skipTitle literal in slide-plan.ts. Finalize step 2 (Reconcile inputs) never ran for either driving SPEC.
- (question) HIGH H3 and MEDIUM M1/M2/M3/M4 from that run: rationale sits in AD-16..AD-19 where the template says memlog (decisions not rationale); the spine is no longer distilled from this memlog because AD-11..AD-19 were transcribed from the rendered epic-16 spine and their record lives only in that folder memlog, which companions: does not reference; story-level detail sits in an initiative-altitude spine; the tombstone invents type/status/superseded_by frontmatter AND keeps the ARCHITECTURE-SPINE.md filename that activation step 4 scans for resumable runs; Open items is a non-template third bucket.
- (decision) ITEM 1 CLOSED (C1), owner 2026-07-30, and the owner fixed the premise rather than picking an offered option: there is NO immutable concept in this project. Verified in code first - the services table (src/lib/db/index.ts) has no presented column and no status of any kind, and the only occurrence of immutable in src/ is a Cache-Control header, so the old rule was unenforceable by construction, not merely mis-worded. What immutability actually meant to the owner: HISTORICAL ENTERED FORM DATA must not change. Byte-identical regeneration is explicitly NOT required. The Friday review period is just the weekly workflow and has no bearing on any of it, so it leaves the spine.
- (constraint) New State convention, all four clauses enforceable: (1) a service entered data changes only through that service own mutation path - operator form edit FR-11 or authorized webhook correction FR-12 - and never as a side effect, so no registry edit, no Sync Artifact and no value migration may alter stored form input (a test can assert Sync leaves the payload columns untouched); (2) structure is the opposite, adopted as a clone at creation and refreshed only by Sync, permitted on any service (AD-16); (3) byte-identical regeneration is not required; (4) concurrent overwrite arbitrated by AD-6 updated_at precondition. The scope point the owner answer forced: the rule is not never changes but only through its own path - FR-12 webhook corrections are a legitimate writer, so a stricter reading would have broken a shipped feature.
- (constraint) Same change set: AD-16 stale pointer to the open item replaced with the positive statement (no notion of a presented service exists anywhere; what Sync must not touch is entered data), and Open items item 1 removed. The section now holds only the AD-9/AD-18 split, which is item 2 of the remediation queue.
- (decision) ITEM 2 CLOSED (M4 + escalation 2), 2026-07-30. Smaller than the validation report claimed: AD-9 already named the schema/value split (written during the merge), so what was actually broken was a CIRCULAR REFERENCE - AD-9 said see the open item, and the open item said AD-9 should name the split. Fixed by stating the division positively in AD-9 Rule (schema = shape e.g. adding a column; AD-18 = contents e.g. rewriting every base_type) and widening AD-9 Binds to SQLite schema and the bootstrap path it shares with AD-18. Added the clause neither licenses the other mechanism: a value change is not a reason to add a framework, a schema change is not a reason to rewrite data.
- (decision) Open items section DELETED - it is not a BMad section and after items 1 and 2 closed it held nothing. BMad vocabulary for undecided things is Deferred (with the reason it can wait) plus memlog revisit conditions; the bespoke third bucket created a place no downstream skill knows to read.
- (constraint) project-context.md synced in the same change set - the substantive half of item 2. That file is loaded by every agent at session start and stated only the schema rule, so an agent reading it alone would either refuse the value migration Story 20.2 needs after first deploy or reach for the framework AD-9 forbids. It now carries the value-migration channel with its precedent named, cites AD-9/AD-18 by number, and states that boot-time re-seeding is NOT a value channel because seeding is bootstrap-once (AD-17).
- (decision) ITEM 3 CLOSED (H1), 2026-07-30. Added the template-mandated Capability -> Architecture Map, in two blocks: CAP-1..CAP-8 for spec-artifact-registry-authoring and six areas for spec-slide-artifact-model. It earned its place immediately - building it made the CAP-1 hole visible as a row rather than as a reviewer opinion, which is exactly what the template says the section is for (the consistency auditor checklist). Also widened frontmatter binds to name spec-artifact-registry-authoring CAP-1..CAP-8, closing L2: the template asks for capability ids from the driving spec and only FR and epic ranges were listed.
- (question) The map records ONE gap, deliberately left visible rather than quietly filled: CAP-1 is governed by AD-7 and AD-16 but nothing forbids sequence or liturgical flags living in TypeScript, which its success clause requires and the three skipTitle literals in src/lib/slide-plan.ts still violate. That is remediation item 4 and needs an owner decision, because the AD that closes it also decides whether Epic 19 is delivered inside Story 20.1 or retired.
- (decision) ITEM 4 CLOSED as AD-20 (H2), owner 2026-07-30, and the owner chose a fourth route none of the three offered options covered: the three fixed liturgical songs (#671, #684, We Have This Hope) become GENERAL registry entries, edited by hand, not data the system processes. Cleaner than every option offered, because a General generates no title slide - so skipTitle is REMOVED rather than migrated. No flag, no slot identity, no hymnal binding for those three. AD-20 states it positively: every slide originates from an ordered registry entry; buildSlidePlan APPLIES rules and does not HOLD them.
- (decision) EPIC 19 RETIRED by the same decision - its goal (a liturgical rule stops needing a deploy) is met by AD-20, its method (move the suppression flag into data) is discarded because there is no flag left. sprint-status epic-19 and story 19-1 set to retired, retrospective n/a, and the open action item asking to decide its fate closed as done. That was the second of the two owner action items opened with Epic 20.
- (constraint) FACTUAL CORRECTION found while grounding item 4 in source: epics.md named slide-plan.ts:460 as Around the Special Song. Wrong - the source shows group intercessory-684, the second half of the fixed #671/#684 pair, and NO skipTitle site touches the Special Song at all. The three sites are intercessory-671 (438), intercessory-684 (460), hope (550, weHaveThisHopeFixed). It mattered because that table is the only record of these sites and Story 20.1 was told to rely on it. Also learned from the same read: none of the three is one of the four predefined SongSet slots - all three are planner-injected FIXED songs, which is why moving only a flag would have satisfied CAP-1 by half.
- (question) Deferred with a revisit condition rather than decided: AD-20 makes those lyrics canvas text, so they stop passing the FR-5 verse/Reff splitter, stop tracking corrections to data/hymns.json, and need one General row per lyric page. If they ship inside the committed data/default-registry.json they duplicate corpus text already in data/hymns.json - a second source of truth for the same lyrics, touching the open SDAH licence item. Revisit when Story 20.1 authors that seed.
- (constraint) Self-inflicted inconsistency caught and fixed in the same pass: setting epic-19 to retired and its retrospective to n/a introduced vocabulary the sprint-status STATUS DEFINITIONS block did not define - the same class of defect Correct Course closed on Epic 14, where in-progress contradicted the file own definition. Added retired for epics (deliberately not implemented, superseded by a decision recorded elsewhere; distinct from done because nothing shipped and from backlog because nothing is waiting), retired for stories, and n/a for retrospectives.
- (direction) OWNER 2026-07-30: dated provenance markers leave the spine - (owner correction, 2026-07-30) / (owner decision, 2026-07-30) are memlog material, not spine content. Applies to AD-16, AD-19, AD-20. Provenance is not lost: every one of them is dated in this memlog already.
- (decision) ITEM 5 / AD-16 TRIMMED (H3), owner approved 2026-07-30. The fourth block 'What the snapshot is for - and what it is not' is gone; AD-16 now carries only Supersedes / Binds / Prevents / Rule. Five clauses were LIFTED into Rule because they bind: creation is the only freeze event; Sync is permitted on any service; Sync may not alter the service entered data; Announcement membership is not cloned and the master list reaches an existing service at render time; a later structural change obliges nobody to keep an older snapshot renderable. Nothing binding was dropped. 440 words -> 230.
- (constraint) Seven sentences dropped from AD-16, all of them rationale or evidence and all already held by entries 38-40 of this memlog: 'no historical-fidelity claim is surrendered, because none is made'; 'the project has no notion of a presented service at all, and services carries no such column'; the resolveAnnouncementUrls / service_id IS NULL OR service_id = ? evidence; 'this ratifies shipped behaviour rather than changing it'; 'each is a deliberate non-goal rather than a cost'; 'the design target is the service being prepared now'; the dated marker. The trade-off the owner accepted explicitly: two of those were not only rationale but DEFENCE - the no-presented-service fact is what answers the obvious objection to 'Sync is permitted on any service' ('so a service presented last week can change?'). The rule stays true and enforceable without it, but the next reader has to open this memlog to learn why that is not a problem. Accepted because the spine's job is keeping a small agent from drifting, not persuading a reader.
- (constraint) OWNER CLARIFICATION 2026-07-30 on initial-data concepts, given while reviewing AD-17. TWO mechanisms, not one, and each has exactly one job: (1) DEFAULT DATA SEEDER runs only when data is initialised from zero - initial install, initial run. It is not a gap-filler. (2) INCREMENTAL DATA MIGRATION is what fills a data gap in production on an incremental update; the need is recognised during coding once a deploy exists. Release discipline the owner adds and the spine does not yet carry: the whole batch of unreleased data changes is COMPACTED INTO ONE MIGRATION CYCLE at release time, rather than shipping one marker per code change. This ratifies AD-17 (seeder = bootstrap only) and gives it the positive counterpart it lacked - a production gap is AD-18's channel, never the seeder's.
- (decision) OWNER 2026-07-30, SHARPENING entry 47 which recorded this too vaguely as 'compaction': incremental data migration means a MONOTONIC DATA VERSION NUMBER, not a per-change boolean marker. A transition is declared EXPLICITLY while the change is being coded (n -> n+1), never inferred at deploy. Owner scenario given verbatim: production sits at 3; development bumps to 4, then 5, then 6; at release those three compact into PRODUCTION VERSION 4, and developer databases are reset to it rather than migrated through it - after the merge development is version 4 holding data identical to what version 6 held. Reason given: so changes do not reach production in dribbles. Small steps are a development convenience and must be merged before release. The invariant underneath: an UNRELEASED version is not yet history and may be rewritten; a RELEASED version is frozen. This supersedes the marker-shape clause of AD-18 (artifact_seed_hash_backfilled, a per-change boolean) - the counter replaces the boolean as the channel's shape. AD-18's prohibition on a migration framework and its rule that persisted rows change only through an explicit migration are untouched.
- (decision) AD-21 ADDED (owner 2026-07-30): one global monotonic data version counter in settings, scope chosen by the owner as ONE counter for the whole database rather than per-domain, so 'which version is production on' has a single answer. Written as a NEW AD rather than an amendment to AD-18 because the counter REPLACES a mechanism AD-18 named (per-change boolean marker) rather than clarifying it - the same Supersedes-one-clause pattern the spine already uses at AD-16 (over AD-14) and AD-17 (over AD-11). AD-9's no-framework prohibition restated inside AD-21 so the counter cannot be read as licensing one.
- (decision) ITEM 5 / AD-17 TRIMMED (H3 + M2), owner approved 2026-07-30. Fourth block 'Operational consequence this rule must carry' is gone. The binding trap was LIFTED into Rule intact: AD-11's two-layer seed is read only at first boot, a deployment that drops data/local/default-registry.json in afterwards is never seeded from it, and recovery is an explicit administrator action. Owner's seeder/migration division ALSO sharpened the Rule beyond the trim: it now states the seeder's job positively (initialises from zero only, never a gap-filler) and hands a production gap to AD-18/AD-21 - the old text said the seeder must not fill a gap but never named who may. The vaguer 'explicit re-seed' recovery was narrowed to Reset-from-seed per template plus an explicit ban on a bulk re-seed of a live database, because a bulk re-seed is exactly what mechanism 1 is not for. Only a DUPLICATE phrase was dropped ('the mechanism that keeps congregation data out of a public repository'), which AD-11's own Rule already states word for word. M2: Binds no longer says 'the ordering column Story 20.1 adds' - it names the registry's ordering column, and Story 20.1's ownership stays in sprint-status.yaml where it belongs.
- (constraint) Same change set as AD-21, and load-bearing: project-context.md line 86 still prescribed the superseded shape ('one explicit marker-gated one-time migration ... the shape already in src/lib/db/index.ts as artifact_seed_hash_backfilled'). That file is loaded by every agent at session start, so leaving it would have had the next agent build the boolean instead of the counter - the exact divergence AD-21 exists to prevent, arriving through the one document nobody re-reads. Now carries the counter, the compaction, the dev-reset, the frozen-released-version rule, and the supersession by name. Also updated in the spine for citation hygiene: the Capability map row for value/vocabulary evolution and the registry diagram's migration node now cite AD-21 alongside AD-18.
- (decision) ITEM 5 / AD-18 TRIMMED (H3), owner approved 2026-07-30, and one dropped sentence was not rationale but a now-WRONG PRESCRIPTION: 'the shape already in the codebase as artifact_seed_hash_backfilled (src/lib/db/index.ts)'. Left standing it would have kept telling an implementer to build the boolean AD-21 replaced. A matching 'Superseded in part by AD-21' line was added, the same courtesy AD-11 and AD-14 already carry, so a reader of AD-18 learns of the supersession without having to find AD-21 first. The pre-first-deploy waiver was folded into Rule and SPLIT BY ACTUAL LIFESPAN: the Epic 20 total-replacement licence and its expiry at first deploy stay here (now naming production data version 1), while 'developer databases are rebuilt, not migrated' MOVED to AD-21 because under the counter that is standing policy at every compaction, not a waiver - it was mislabelled from the start. Also dropped: the dated marker, 'obeying the rule above' (a repeat), and the word 'just' in 'which AD-17 has just removed' (a merge artefact that no longer refers to anything).
- (decision) OWNER 2026-07-30, the recognized entry vocabulary stated as a CLOSED SET of six keys over three kinds: general (free canvas), songset-bt-open / songset-bt-close / songset-ds-open / songset-ds-close (songset, expandable, each already bound automatically to its own predefined SDAH slot), announcement (expandable, bound to the Announcements set). 'Hanya ini yang diakui' - nothing else is admitted, which the spine had never said. This COMPLETES AD-19's three-kind clause rather than replacing it: three kinds, six keys. Grounded in the seed before recording: data/default-registry.json today holds ONE song-set row (baseType song-set, four placeholders hymnNumber/songTitle/label/lyrics, two layouts title and lyric), so the four keys are a real restructure of that single row into four ordered rows, and bare song-set does not survive as a recognized value.
- (question) HOLE FOUND while recording the above, same shape as the CAP-1 hole that became AD-20: CAP-3 says 'full canvas authoring, General only' and NO AD states it. AD-13 owns canvas state, AD-15 owns write validation (it does prevent required-placeholder deletion), AD-19 owns key authority - none says which entries may be freely authored. Two units can obey every AD and still disagree about whether the canvas editor opens for a songset row. Proposed as AD-22. Owner then gave the songset surface a THIRD mode rather than either of the two offered: songset configuration is a SEPARATE, BOUNDED surface - the administrator sets a background image (different for title and for lyric), font style and font size, while element POSITIONS are 'set in coding'. So authoring authority is three modes, not two: free canvas (general), bounded property form (songset), bound-to-a-set (announcement).
- (decision) ITEM 5 / AD-19 TRIMMED (H3 + M2), owner approved 2026-07-30. 700 words -> ~340, and four bullets went four different places rather than into the bin. LIFTED into Rule 1 because they bind: the three-kind vocabulary with the four placeholder kinds GONE not renamed; the closed six-key recognized set; semantic name never an ordinal; extending the vocabulary is code-plus-tests, never administrator configuration. To Deferred with its owning story: whether a slot identity lives in the base_type column or a discriminator beside it (Story 20.2/20.7), in the same form Deferred already uses for the snapshot's physical home. To sprint-status.yaml as a gap-4 bmad-spec item: the catalog-key spelling advice and the two SPEC reconciliations, which were addressed to another skill's pass and had no business in a spine. DROPPED: the standalone Note contrasting AD-15's identity stability with AD-19's identity authority (legible from the two titles, and it binds nobody), and the self-referential 'it is the same species of hole, not the same hole' - the useful half survives as 'Independent of the SongSet clause above'. FIXED a pre-existing internal contradiction the merge left: Rule said the identity was 'carried by the type vocabulary' (i.e. the base_type column) while the bullet below deliberately deferred exactly that choice. The phrase is gone; Deferred holds the question.
- (decision) AD-22 ADDED (owner 2026-07-30), closing the CAP-3 hole: authoring authority is fixed per kind and no surface widens it. general = free canvas including Placeholder Catalog keys. A songset-* row = a BOUNDED CONFIGURATION SURFACE, not a canvas: exactly two background images (one title, one lyric - verse and refrain share the lyric layout and its background, so there is no third), plus font style and font size; layout composition is developer-owned seed data and is not exposed there; the placeholder set and the SDAH slot binding are server-defined and cannot be added to, removed from or rebound. announcement = bound to the Announcements master set. Only songset and announcement expand; a General is one slide. The 'set di coding' ambiguity was resolved by EVIDENCE rather than by asking a third time: the coordinates already live in data/default-registry.json, not in TypeScript, and AD-12 requires the plan to hydrate them from the registry - so 'in code' means a developer authors the seed. Had it meant coordinates in a renderer it would have contradicted the Design Paradigm, AD-12 and AD-20 at once, and those would have needed conscious amendment.
- (constraint) AD-22's last clause is a consequence nobody had written down and it would have bitten later: SongSet layout coordinates live in the seed, and AD-17 reads the seed ONCE. So a developer fixing a coordinate in a later release does NOT reach a deployed database by restarting. Reset-from-seed would reach it but restores the WHOLE shipped template, discarding the administrator's background image and font choices (AD-11) - that is not a coordinate fix, it is undoing someone's work. After first deploy the only sound channel is a versioned data migration (AD-21). Stated inside AD-22 so no implementer assumes a restart suffices. ALSO: the owner's SongSet layout description itself was deliberately kept OUT of the spine and routed to a Story 20.1 seed-authoring action item - one seed with one answer cannot be chosen incompatibly by two units, so it fails the spine's own admission test, and putting it in would have re-created the M2 defect this pass exists to fix. Recorded with a fact the description omitted: lyrics.ts emits Chorus as well as Reff, and an EMPTY label for header-less lyrics, so the label element must tolerate empty and must stay required:false.
- (decision) ITEM 6 DONE (M1, M3, L3, L4), 2026-07-30. M1: ../architecture-epic-16/.memlog.md is now a companion of record and the AD map says plainly that AD-11..AD-19 were decided there, warning that a resume reading only this memlog will not find them. No entries were invented here to fake a history. M3: the tombstone was git mv'd ARCHITECTURE-SPINE.md -> SUPERSEDED.md, because activation step 4 scans the architecture output path for that filename to offer a resume, and a run resumed from a tombstone would amend a document nothing reads; the file now says so in its own body. Its three invented fields are gone - type: architecture-spine, purpose: report, status: final, all BMad-valid, supersession stated in the body instead of in a superseded_by key. Verified first that NO live file cites the old filename: all six hits are dated reports and review records, which are off-limits and correctly describe the past. Also repaired a dangling pointer found while editing it: it still sent readers to an 'Open items' section of the project spine that was deleted when items 1 and 2 closed - it now states both escalations were settled on 2026-07-30. L3: child_spines removed. L4: the Stack note claimed 'synced on 2026-07-29' while the spine said updated 2026-07-30; re-verified row by row against package.json - all ten rows exact, no drift - and the note now says so.
- (constraint) Citation audit after adding AD-21 and AD-22, run because the scripted-repair failure mode of the fold-in is easy to repeat: no renumbering happened this time, so silent retargeting was not the risk - a STALE PRESCRIPTION was. Grepped every AD-n across live docs (highest cited is AD-22, all resolve) and every mention of artifact_seed_hash_backfilled. Two live files still prescribed the superseded boolean: project-context.md line 86 (fixed with AD-21) and sprint-status.yaml's Epic 20 architecture item, whose status note told a future implementer that value migrations are 'marker-gated, precedent artifact_seed_hash_backfilled'. That note is a record of a past run, so it was NOT rewritten - a superseding sentence was appended in the file's own established 'NOTE (corrected ...)' style, pointing at AD-21 and telling the reader to read it before writing any migration. The remaining hits are both memlogs, the epic-16 reviews and the validation report: all correctly historical and all off-limits.
- (event) Reviewer Gate re-run 2026-07-30 against the TRIMMED spine (AD-1..AD-22), sequential because subagents are unauthorized this session - the fallback reviewer-gate.md permits and the 2026-07-29 run used. lint_spine 0 findings before and after fixes. Three lenses, full reviews in reviews/*-2026-07-30-post-trim.md: rubric walker (9 findings), version reality check (verdict passes; one honest gap - no WEB check this session, currency confirmed against package.json instead, and no version is load-bearing for AD-20/21/22), adversarial two-units (4 new pairs, 2 earlier pairs verified CLOSED by AD-20 and AD-22). Verdict: passes. Six fixes applied in the gate rather than handed over. Public-repo guard 4/4. Three gate files verified byte-identical in their shared block. No src/ or tests/ file touched, so npm test was not required.
- (decision) Two HIGH gate findings, both of the worse kind - the spine's own words PERMITTED the divergence rather than merely omitting it, so a careful implementer still got it wrong. (1) AD-18 ended 'a migration is NOT OBLIGED to keep existing service snapshots renderable', which is a licence and forbids nothing: one unit migrates the registry only, another also rewrites every snapshot to keep past services renderable, and both obey the letter while producing different databases. AD-16 should settle it (structure reaches a service only through Sync) but AD-18 never drew the inference. Replaced the licence with a PROHIBITION: a migration operates on the live registry and does not rewrite service snapshots; an older snapshot may stop being renderable, which AD-16 accepts. (2) AD-22 gave the administrator font size and background while calling layout composition developer-owned - but those live in the SAME layout JSON as the coordinates (verified: the shipped song-set carries backgroundColor, backgroundImage and per-element style.fontSize beside x/y/w/h). One unit writes the admin's choice in place, another keeps it beside the layout; both comply, and against the first AD-21's later coordinate migration cannot tell a human's fontSize 52 from the developer's 46.67, while AD-11's Reset keeps no second copy. Added the enforceable minimum - administrator-configured values must stay DISTINGUISHABLE from developer-authored layout - without dictating the storage shape, which stays a schema call.
- (constraint) Four more gate fixes applied. MEDIUM: AD-22 created a NEW image-accepting surface (two SongSet backgrounds) outside AD-8's enumeration, which names announcements, uploads, registry /assets/ refs, PPTX embedding and 'the canvas editor' - and AD-22 insists this surface is NOT the canvas editor, so a builder could accept an arbitrary remote URL and still comply; AD-22 now cites AD-8. MEDIUM: AD-17's seeding marker and AD-21's version counter both live in settings and never mentioned each other, so one unit could gate seeding on data_version >= 1 and delete the marker as redundant - AD-21 now states they are distinct and neither substitutes for the other. MEDIUM: AD-21's counter has no implementation owner anywhere and the superseded boolean still ships, so a Deferred entry names the work, the compaction into production data version 1, and the three code sites where AD-22 loosens READ_ONLY_BASE_TYPES (types.ts, ArtifactEditor.tsx:104, store.ts:226) - recorded because AD-22 RATIFIES shipped behaviour for its read-only half but EXTENDS it for the bounded surface, and a reader must not mistake one for the other. LOW: the Design Paradigm still cited AD-11..AD-19 after three ADs joined that same subject, and CAP-4's 'styled locally' had no AD saying locally means on a General. Two LOWs deliberately NOT fixed: the operational-envelope diagram (owner left it open) and CAP-5's [kind] label chip for a slot row, which is EXPERIENCE.md's to own and is routed to gap 3.
- (event) spine finalized after items 5 and 6 (AD-1..AD-22, altitude initiative, status final, updated 2026-07-30). Gap 1 of the six-gap handover is closed. Nothing committed - the working tree carries every change for the owner to review.
- (decision) Owner accepted the two post-approval gate fixes on 2026-07-30: AD-18's permissive 'not obliged to keep snapshots renderable' becomes a prohibition (a migration does not touch service snapshots), and AD-22 gains the requirement that administrator-configured values stay distinguishable from developer-authored layout. Gap 1 (architecture) is CLOSED. Nothing committed.
- (event) Validate intent re-run (fresh independent lenses): lint 0 findings, stack 0 drift, prior 6 post-trim fixes confirmed holding. New findings: 1 CRITICAL (EXPERIENCE.md contradicts AD-16, wrong AD-4 citation), 3 HIGH (seed/migration boot-order gap; canvas editor dir mis-cited as artifacts/ instead of admin/; SongSet slot-identity column fork), 1 MEDIUM-HIGH (AD-22 distinguishability shape still unnamed, reopened residual), 4 MEDIUM (AD-19 unshipped-state flag missing; no backup/DR deferred item; announcements master-list scoping unstated; no stale-snapshot render failure mode), 3 LOW. Report: reviews/validation-2026-07-30-validate.md(.html). Spine not modified this run.
- (event) Reviewer Gate run B (Validate intent, 5 parallel subagents authorized by owner): lint_spine 0 findings; rubric walker + version/reality-check + adversarial two-units + ad-hoc brownfield-ratification + ad-hoc cross-document-coverage. Consolidated 9 CRITICAL / 22 HIGH / ~30 MED-LOW into reviews/validation-2026-07-30-validate-b.{md,html}. Spine NOT modified. Key: two [ADOPTED] rules false about shipped code (AD-6 webhook bypasses the precondition at api/webhook/route.ts:123-127; AD-17's seed resurrects deleted rows at plan-build time via registry-snapshot.ts:85-90). No status vocabulary on AD-16..AD-22 is the mechanism behind 5 findings. Node 20 floor is EOL 2026-04-30 with no engines field. All 13 findings of the morning validate run persist (spine untouched since c04fea0); 7 of 9 re-derived independently, 3 sharpened. Parent resolved 2 lens disagreements by direct verification: skipTitle DOES still ship (5 sites); AD-22's schema call IS foreclosed by rejectUnknownKeys.
- (decision) Status vocabulary introduced (closes the gate's highest-leverage finding). Every AD now carries one of: [ADOPTED] ratified against shipped code; [ADOPTED, partial] mechanism ships with a named gap recorded in Deferred; [TARGET] decided but not yet built, so its present tense describes intended state and src/ does not match it yet. Prevents: a reader mistaking decided-but-unbuilt for already-true. Before this, [ADOPTED] sat on 6 of 14 shipped decisions and none of the 7 unbuilt ones, so its ABSENCE meant both things at once -- the mechanism behind five separate review findings.
- (decision) AD-6 amended, not scoped. The rule stays absolute (no write path may bypass the precondition) because that IS the invariant; tag becomes [ADOPTED, partial] and the AD now names the four shipped bypass paths (webhook correction + intake, DELETE /api/services/[id], PATCH/DELETE /api/announcements/[id]) plus the second-granularity weakness, both filed in Deferred. DECLINED alternative: scoping the rule to cookie-authenticated mutations. That would have made the spine self-consistent by abandoning the exact hazard AD-6's Prevents names -- the late agent correction erasing an operator edit -- which is the wrong direction to resolve a doc-vs-code conflict.
- (decision) AD-17 amended: the seed read boundary extends from 'boot' to 'no path'. The filesystem seed is read at exactly two moments -- first-boot bootstrap and explicit per-template Reset -- and a template id the database does not hold does not exist in the deck. Prevents: the resurrection AD-17 already forbade at boot, relocated to plan-build time by registry-snapshot.ts:85-90 where AD-17's wording did not reach. Also added: an administrator-authored row has no seed, is validated against its own persisted state, and exposes no Reset -- Reset is defined only where shipped content exists to restore.
- (decision) AD-18 amended: a registry value persisted in more than one place has exactly one authoritative copy, named here. The row's payload JSON is authoritative for every template field; a column duplicating a payload field (label, base_type, any slot discriminator) is a derived index maintained by the same write; a value migration rewrites the payload and re-derives the columns in one statement, asserted by a test that column and payload agree for every row. Prevents: the admin list reading the column while the plan reads the payload, so a column-only migration makes the admin screen and the congregation screen disagree about every row's kind, undetectably from either one.
- (decision) AD-19 amended in three places. (a) A weekly value a slot binding names has exactly one persisted home -- the service's own entered data; the binding is a key INTO it, never a second copy. The four songset-* identities replace song1Number..song4Number in the same change set, deleted rather than aliased. (b) The closed six-key set is a statement about the registry, not the rundown: a hymn no slot claims is surfaced, never dropped and never fatal, and the identity-to-rundown-position map lives in one module (the shipped planner renders unbounded ds-middle-* groups, so a third Divine Service hymn otherwise vanishes silently or crashes the whole plan build). (c) The Placeholder Catalog is one server-side module holding the admitted key AND its resolver, so a key cannot be admitted without a filler.
- (decision) AD-21 amended: the counter's absence is not version 0. A database created by the AD-17 bootstrap is stamped with the current data version by the bootstrap itself, in the same transaction as the seeding marker. The getDb order is fixed and asserted by a test: DDL (AD-9) then data migrations (AD-18) then first-boot bootstrap (AD-17), so a migration never observes rows the bootstrap wrote in the same boot. Also: schemaVersion (validate.ts:449, re-stamped at :505) is a payload-shape discriminator a transition may raise, never the answer to 'which version is production on' -- that is the settings counter alone.
- (decision) AD-22 amended: the deferred schema call is promoted into an invariant. Administrator-configured values on a bounded-surface row persist OUTSIDE the layout, as an override record keyed by row and field, re-applied over the developer layout at hydration; the layout JSON stays developer-owned in full and a migration may replace it wholesale; a bounded surface that writes into the layout is refused by the validator on every write path. Promoted because the two options AD-22 offered were not equally available: validate.ts's rejectUnknownKeys over ALLOWED_LAYOUT_KEYS forecloses the in-layout marker, leaving only the unmarked shape AD-22 itself forbids -- after which AD-22's own migration clause silently reverts every background and font the administrator chose.
- (decision) AD-16 amended twice. Sync Artifact is a structural write and is admin-only, re-checking the role from SQLite as AD-14 requires of every registry management surface, even though it is reached on a service route that src/proxy.ts gates for any signed-in account; an operator may see that their snapshot is stale and request a sync, but not perform one. And a service without a snapshot renders from the live registry until its first Sync, which is a clone-in-place -- so no-snapshot is a defined runtime state rather than an unclosed fork, which matters because every service in the existing database is in it on day one (SPEC.md:88 assumed this and AD-16 forbade it).
- (decision) AD-10 amended: every present-channel message carries a plan identity -- a fingerprint of the service-bound snapshot and the resolved announcement set that produced the deck -- and a receiver whose own identity differs refuses to follow the index and says so on the room-facing screen rather than rendering a slide it cannot vouch for. Prevents: presenter and projector are two separate force-dynamic renders each calling buildSlidePlan at its own moment, so any structural change mid-service silently offsets every slide after it -- the operator sees Sermon while the room sees Special Song. Live today without Epic 20.
- (decision) AD-5 amended: the middleware.ts prohibition keeps its rule and loses its false reason. Node-runtime middleware has been stable since Next 15.5.0, so 'it compiles for the Edge runtime' is a default rather than a property; the real reason -- already stated correctly in src/proxy.ts:5-11 -- is that a proxy.ts entry ALWAYS runs on Node while a middleware.ts entry needs an explicit runtime export to. A rule defended by a refutable reason is a rule that gets reverted. Also records the deviation: Next's own docs (proxy.md:217-219) say never to rely on Proxy alone for authorization, so the nine-unguarded-routes item is a standing deviation rather than mere scheduling.
- (decision) AD-13 amended: the save mechanism is serializeCanvas over canvas.getObjects() (ArtifactEditor.tsx:257,271), projecting into the registry's CanvasElement[] shape -- not canvas.toJSON(), which appears nowhere in src/. Load-bearing rather than cosmetic: Fabric's toJSON() format is not ArtifactLayout.elements, so a second editor surface built from AD-13's literal wording (AD-22's SongSet config being next in line) would fail AD-15's validator.
- (decision) AD-23 added (next id, nothing renumbered): transition style is one service-level value that both renderers consume identically, with no per-surface default. Binds PPTX generation, web slideshow, presenter, projector. Prevents the divergence FR-7 (prd.md:305) forbids and docs/architecture.md:61 -- a declared source of this spine -- already committed by hardcoding a crossfade. src/lib/transitions.ts and use-slide-transition.ts are the shipped implementation. Added because the review found FR-7 governed by nothing and not deferred, and silence was the one unacceptable option once the corpus already held a divergence.
- (decision) AD-4 restated in the target voice with a dated anchor: production is DEPLOYED as one Docker/standalone unit on the home-PC LiveServer, and as of 2026-07-30 no deployment exists (prd.md:540, owner-corrected). Not cosmetic: AD-18's total-replacement licence ('until first deploy no production rows exist') and AD-21's released-version freeze both hinge on that fact, and AD-4's present tense had already expired the licence Epic 20's cheapest path depends on.
- (version) Node.js row corrected: Node 20 reached EOL 2026-04-30, package.json carries no engines field at all (so 'package.json is the version authority' was vacuous for that row), Next 16.2.10 requires >=20.9.0, and Dockerfile:1 plus CI already run node 22. Row is now '22.x LTS (>=22.12)' with the Next floor noted; adding engines to package.json is filed as the code-owned half. fast-xml-parser ^5.10.1 added to the table -- it is the parser inside the generator AGENTS.md names as the enforced privacy filter. A currency line added: four rows sit behind current major (TypeScript ^5 vs 7.x, better-sqlite3 12 vs 13 which needs node>=22, fabric 6 vs 7, eslint ^9 = maintenance tag), and drift-vs-package.json is a separate check from currency-vs-upstream.
- (constraint) Testing convention added to Consistency Conventions, because AD-5, AD-15 and AD-17's retirement note all delegate their enforcement to a suite the spine never described: node:test with --experimental-strip-types via tests/register-ts-resolve.mjs, over an explicit file list in package.json scripts.test; a new suite is registered in that list in the same change set (an unregistered file never runs, in CI or locally, and nothing detects it); no second runner without a decision recorded here. Verified in sync at this run -- 34 suites listed and on disk -- so the hazard is latent, not live.
- (event) Deferred gained six entries, three of them silent dimensions shaped like the existing Observability entry (current floor named + revisit trigger): data durability and recovery (losing DB_PATH loses the whole authored registry now that AD-17 removes the seed as a recovery channel; backup is a manual step at docs/deploy.md:79), secrets fail-closed as a general rule beyond WEBHOOK_SECRET (AUTH_SECRET, IMAGE_URL_ALLOWLIST, PPTX_* , AUTH_BOOTSTRAP_*), and performance envelope. Plus three shipped-vs-target gaps the old text asserted away: the registry has no ordering column and no create/delete/reorder verbs, skipTitle still ships at five sites, and the Placeholder Catalog does not exist (ALLOWED_PLACEHOLDER_KEYS is an unrelated object-key whitelist whose name makes a grep look like confirmation).
- (event) Post-amendment Reviewer Gate (4 parallel lenses: rubric walker, version/reality-check, adversarial two-units, plus an ad-hoc amendment-verification lens). It caught three defects the amendment sweep itself introduced, which is why it ran: (1) AD-10 left tagged [ADOPTED] while its new plan-identity clause ships nowhere -- fixed to [ADOPTED, partial] with the gap in Deferred; (2) a false Diagram 1 edge routing settings.slide_transition through buildSlidePlan, which has zero transition references -- rerouted to PPTX and WebShow per the real flow; (3) a Deferred instruction to 'keep the corrupt-payload fallback at :56-63' when parseRow merely returns null and the SAME loop at :85-90 substitutes for absent and corrupt rows alike, so following it literally would have deleted the fallback -- reworded, and AD-17 now decides the corrupt case explicitly (fail closed). 21 of 27 findings verified CLOSED before this pass; the remaining 6 closed in it.
- (decision) AD-23 corrected during the gate rather than after it: the absolute 'no per-surface override' was false. PresenterOperator holds a live transition override and broadcasts it over AD-10's channel, so both browser surfaces agree with each other while an already-generated PPTX keeps the settings value it was built with. The invariant is that no surface keeps a DEFAULT of its own -- not that the value is immutable. Verified against src/lib/settings.ts, pptx.ts, both page renders and PresenterOperator.
- (decision) AD-17's corrupt-row case decided: a row the database holds but cannot parse FAILS CLOSED -- no layout, logged with id and reason -- rather than being replaced by shipped seed content. Closes the loophole an adversarial lens found in the new read-door rule, which had said 'a row the database does not hold' and therefore left corrupt held rows re-materialising example content over an administrator's real layout. Consistent with AD-5 and AD-8's existing fail-closed posture, and AD-15 means a corrupt row should not exist in the first place.
- (version) SECURITY, web-verified during the gate: next@16.2.10 is pinned exact and predates the July 2026 security release (16.2.11, 2026-07-21) which patches 9 CVEs, 4 High, and Next does not back-patch earlier minors. Two of the four touch this spine's own concerns -- CVE-2026-64645 SSRF via request-controlled rewrite/redirect destinations (the class AD-8 contains, and AD-5's safeNextPath touches) and CVE-2026-64642 middleware bypass, whose stated precondition is Turbopack + single-locale i18n that this project does not have. Recorded in Deferred as pre-first-deploy code work, not housekeeping. Also: Node 22 is Maintenance LTS and 24 is Active, so the row that just retired Node 20 will need the same treatment.
- (event) Spine Update closed: 23 ADs (13 ADOPTED, 3 ADOPTED-partial, 7 TARGET), AD-23 added as the next id with nothing renumbered, lint_spine 0 findings, public-repo-guard green. updated: 2026-07-30. NOT closed and reported to the owner instead of acted on: AD-16 (784 words) and AD-19 (822 words) are now long enough that the rubric walker flagged readability -- the added clauses each close a named divergence, so the fix is restructuring into sub-bullets rather than cutting content, and that is a separate pass the owner did not ask for. Companion-owned findings C6-C9 and the code-owned trio (webhook precondition, registry-snapshot gap-fill removal, package.json engines + the next patch bump) remain open by design.
- (event) Source reconciliation for AD-23, authorized by the owner (2026-07-30): docs/architecture.md:61 and docs/component-inventory-monolith.md:94 both claimed the projector uses a fixed crossfade. Both now state the configured settings.slide_transition style resolved through src/lib/transitions.ts, with fade named as the default rather than the behaviour. The second site was not in the review findings -- it surfaced when grepping for siblings of the first. Confirmed that EXPERIENCE.md and DESIGN.md mention transitions nowhere at all, so the transition contract was never a UX-artifact gap; the pending bmad-ux work is the AD-16/AD-19/AD-22 surfaces and the C7 contradiction, which are unrelated.
- (direction by user) OWNER DIRECTIVE 2026-07-31: Update run for AD-24, the single item story 17-1 (reachable dark mode) deliberately left open. bmad-dev-story refused to edit this spine inline and was right to: AGENTS.md routes a structural-invariant amendment through a bmad-architecture Update run, and satisfying the checkbox inline would have broken the gate the checkbox exists to enforce. The finding that produced it: grep -rn localStorage/sessionStorage over src/ returned ZERO hits before that change set, and next-themes now writes localStorage.theme, so browser-side state is a storage target this spine had never named. Owner chose the wide reading of AD-24 over two narrower ones - see the constraint entry below.
- (decision) AD-24 added [ADOPTED] (next id, nothing renumbered): Operator Chrome State Is Browser-Local, and the Room-Facing Surface Is Closed to It. Binds: the root layout client boundary, browser-persisted operator preferences, settings vs localStorage as storage targets, and every full-screen room-facing surface. Three clauses. (1) THE STORAGE DIVISION, stated as three tiers because a builder adding a preference can reach for any of them and this spine named only one: persisted-shared goes to SQLite on the durable DB_PATH (AD-4), app-wide values in settings as AD-23 already does for slide_transition; persisted-local goes to this browser localStorage and nowhere else, not in DB_PATH and therefore not backed up; ephemeral-shared travels over AD-10 BroadcastChannel, which is what AD-23 presenter live override already uses. The test is who and what must agree: slide_transition is in settings because both renderers and both screens must agree on it, theme is in localStorage because nothing but this operator eyes depend on it. (2) THE ROOT CLIENT BOUNDARY: layout.tsx stays a Server Component and reaches the client through exactly one child, src/components/ThemeProvider.tsx. Passing children through a client provider does NOT make them client components - they are server-rendered and arrive as an already-rendered tree - so Server-Components-by-default survives this provider and would survive another; what must not happen is the boundary migrating UPWARD onto layout.tsx, which would convert the whole app and which no provider needs. A second root-level provider is admissible only if it must wrap every route; anything narrower mounts at the narrowest layout or page that needs it. (3) THE CLOSURE: the room-facing surfaces never read operator chrome state, in any form, under any setting. Projected render tree paints in literal colours with no theme token and no theme-coloured edge; a full-screen room-facing surface neutralizes the app shell it inherits (html/body background and scrollbar-gutter) through the ONE shared hook src/lib/use-projected-shell.ts, never its own copy; tests/theme-chrome.test.mjs is the gate and a new room-facing surface joins its PROJECTED set in the same change set, exactly as AD-5 requires of a new matcher exclusion. The PPTX is closed structurally rather than by enforcement - it is generated on the server and cannot read browser state at all. The two presenter surfaces that pin .dark on their own wrapper are OPERATOR surfaces, not room-facing, and their opt-out is deliberate.
- (constraint) Why AD-24 is ONE decision and not two or one-narrower, owner call 2026-07-31 with the alternatives shown first. DECLINED (a) splitting into AD-24 storage-target-plus-root-boundary and AD-25 projected-surface-closure: the three clauses are one divergence read from three ends, and separating them reproduces the AD-14/AD-16 cross-reference burden - a Supersedes-style pointer pair to maintain - for no gain in bindability. DECLINED (b) the minimal reading, AD-24 covering only what the review finding named (localStorage as a second storage target, plus the root client boundary), leaving the closure to AC-4, the test suite and DESIGN.md. Rejected because the closure is the half that actually earns an AD: the theme is a client-persisted value that PAINTS and that next-themes syncs across same-origin windows on the storage event, and the projected surfaces ARE same-origin windows - so without clause 3 there is a third live channel toward the congregation screen alongside buildSlidePlan (AD-7) and the BroadcastChannel (AD-10), held by no invariant at all. Story 17-1 shipped that hazard for real before review caught it: the reset lived inside ProjectorClient and the slideshow, the same fixed inset-0 pattern at an equally room-facing URL, had none, so the reserved scrollbar gutter showed body as a strip down the edge of the projected screen. Permanently white was wrong but not VARIABLE; the moment a theme existed the strip followed the operator choice live, mid-service. A minimal AD-24 would have recorded the storage target and left the mechanism that nearly reached the congregation to a story acceptance criterion.
- (version) next-themes added to the Stack table, web-verified 2026-07-31 rather than asserted: package.json pins ^0.4.6, resolved 0.4.6, and 0.4.6 IS the latest release (2025-03-11, per the GitHub releases page). No archive notice, no deprecation. So zero drift AND at head - but the row has not moved in about 16 months while React reached 19.2, so current and actively-developed are different claims for this row and Deferred says so instead of leaving the reader to infer velocity from a version match. It earns a row because it is structurally load-bearing rather than incidental: it owns the localStorage key AD-24 clause 1 names and the attribute=class contract the .dark palette is keyed on (globals.css:5, custom-variant dark). DISCARDED as unverifiable: a search-result summary claiming unresolved React 19 bugs, no server-side theme reading and broken multi-class cleanup - unattributed blog synthesis, not the changelog, and an unverified claim does not go into a spine. The one real React 19 friction is code-level and already recorded in story 17-1: react-hooks/set-state-in-effect rejects the usual useEffect mount guard, so ThemeToggle uses useSyncExternalStore with a server snapshot of false. Also confirmed sonner ^2.0.7 stays OUT of the table: Toaster is mounted nowhere and toast() is called nowhere in src/, so it is an installed dependency with no structural role yet - story 17-6 decides whether it gets one.
- (event) Reviewer Gate for the AD-24 Update (4 parallel subagents, authorized by the owner): lint_spine 0 findings, then rubric walker + version/reality-check + adversarial two-units + ad-hoc brownfield-ratification. Reports in reviews/*-2026-07-31-ad24.md. The gate earned its keep: THREE independent lenses converged on the same defect, which the parent then verified directly in the code rather than taking on report. AD-24 shipped as [ADOPTED] and had to be corrected to [ADOPTED, partial]. Consolidated: 3 CRITICAL / 7 HIGH / ~12 MED-LOW, plus 5 adversarial attacks the code already forecloses. Full suite re-run 364/365 pass 1 skipped; public-repo-guard 4/4.
- (decision) AD-24 corrected during its own gate, seven changes, each verified in code by the parent before it was applied. (1) TAG [ADOPTED] -> [ADOPTED, partial]. The shell clause was FALSE for two shipped room-facing surfaces: projector/page.tsx:71 and slideshow/page.tsx:76, the fixed inset-0 branches a buildSlidePlan throw renders, are Server Components and therefore CANNOT call useProjectedShell - a React hook does not run there - so html/body keep the themed background and the reserved gutter on exactly the screen a registry failure faces the congregation with. FULL_SCREEN in theme-chrome.test.mjs lists only the two Clients, so nothing catches it. Deliberately NOT fixed by narrowing the rule to client surfaces; the rule stays and the gap is named, the AD-6 and AD-10 shape. (2) Clause 1 quantified over persisted state, which the signed session cookie (src/lib/auth/session.ts) falsifies; now quantified over APPLICATION state, with a sub-bullet stating the cookie is AD-5 credential transport and NOT a home for a chrome preference - the point being that a cookie is the one browser-persisted home a SERVER can read, so a theme cookie, the standard first-paint-flash fix, would hand a room-facing server render operator chrome and demote the closure from structural to enforced. (3) Persisted-local now explicitly holds a view preference and no domain data: story 17.4 canvas dirty-state would otherwise park an ArtifactLayout in localStorage, outside AD-15 validation, AD-6 precondition and AD-21 counter, and pass every word of the who-must-agree test because the honest answer for an unsaved draft is nobody-yet. (4) localStorage plus the storage event is named as a same-origin cross-window channel that is NOT a licence to carry coordination state - it slips past both of AD-10 prohibitions (no second channel name, no invented message shape) while doing AD-10 job, and story 17.5 projector liveness is the live temptation. (5) Clause 2 was self-contradictory - exactly one child versus a second provider is admissible - and its bar (must wrap every route) was rhetorical; replaced with a decidable test: enumerate the consumers and mount at the narrowest layout containing all of them, root only when that enumeration IS every route. (6) The closure clause named only PROJECTED; the mechanism has TWO sets and the shell reset keys on FULL_SCREEN. Both now named. Also cites AD-12 (the Fat Payload is what makes literal-colour rendering possible) and AD-15, which the first draft omitted while citing AD-1 alone. (7) The literal-fallback parenthetical read exhaustive on #FFFFFF; ArtifactSlide has three (#FFFFFF, #000000, transparent) and the omitted one was the stage background, the largest painted area on screen.
- (event) Deferred gained five entries from the gate, and the Stack table two rows plus one correction. Deferred: (a) the Server-Component shell gap that made AD-24 partial, WITH the design call inside it stated rather than left open - the reset must reach a Server Component, so the candidates are a tiny client component the shells render, a style element they emit, or hoisting into a route-segment layout that owns both surfaces, and only the third also catches a future shell nobody annotates; (b) no per-account persisted tier exists at all - settings is the only persisted-shared home and every write path into it is requireAdminSession (api/admin/settings/route.ts:17,29), so an operator preference CANNOT be stored server-side and persisted-local is not the better choice for the theme, it is the only available one; (c) a notFound() at a projected URL renders Next default 404 inside the themed root shell, because no not-found.tsx / error.tsx / global-error.tsx exists anywhere under src/; (d) the closure gate ceiling widened - it already named runtime-composed classes and CSS-file tokens, and now also names that componentImports FILTERS OUT @/lib (:233) on the premise that @/lib is not markup, which use-projected-shell.ts and use-slide-transition.ts both strain since both reach the projected DOM, plus the unregistered-surface hole that is how SlideshowClient shipped the AC-4 defect in the first place; (e) next-themes cadence. STACK: next-themes ^0.4.6 added; shadcn ^4.13.0 added as its own row because it is a RUNTIME dependency (globals.css:3 imports shadcn/tailwind.css, feeding the very palette file AD-24 cites) and the @base-ui/react row previously named shadcn in a parenthetical while pinning a different package version, which made an auditor read it as covered; that parenthetical corrected. The engines Deferred item now also carries @types/node ^20 (resolved 20.19.43), the one Node-20 commitment that is machine-enforced rather than prose - the type-checker validates against the runtime the Stack row retired, and no doc edit reaches it.
- (event) Spine Update closed 2026-07-31: 24 ADs (13 ADOPTED, 4 ADOPTED-partial, 7 TARGET). AD-24 added as the next id with nothing renumbered - the AGENTS.md never-renumber rule held this time, no waiver needed. lint_spine 0 findings, public-repo-guard 4/4, theme-chrome 28/28. Same-change-set artifacts updated so nothing is left lying: story 17-1 twelfth patch item checked off with the AD-24 resolution AND the new high finding filed against its own change set with no owner, and sprint-status.yaml carries both. NOT done and reported to the owner instead of acted on: (1) the Server-Component shell gap needs a STORY KEY - it is a live defect on a room-facing surface, but creating an epic/story entry is bmad-create-story work and outside an architecture Update; (2) AD-24 is 1390 words against AD-19 817 and AD-16 786, so it is now the longest AD in the file by a wide margin - it is correctly structured into labelled sub-bullets, which is what the 2026-07-30 gate prescribed, and every sub-bullet closes a divergence a lens actually constructed, so trimming would reopen a hole rather than tighten prose; a restructuring pass across all three long ADs is a separate job the owner has not asked for. Also NOT acted on, out of this spine's altitude: the adversarial lens noted that adding a high-contrast mode as a third next-themes theme would silently disarm the 29 measured dark: overrides, since @custom-variant dark is keyed on a class next-themes sets one at a time - that is a DESIGN.md/palette concern, and story 17.2 owns that token surface.
- (direction) OWNER DIRECTIVE 2026-07-31 (round 3 of story 17-1's code review, decision item at story :239): the two AD-24 edits bmad-dev-story made INLINE at spine :218 and :393 route through a bmad-architecture Update run before 17.1 can go to done, and the stale @/lib / componentImports bullet at :392 is repaired by this same hand rather than a different one. The inline edits were self-certified as 'a citation repair, not an amendment' on the ground that no clause changed and no AD-n was added; round 3 disputed that, because the paragraph records a chosen design (the owner's route-group layout) and re-scopes a partial-adoption gap from one hole to four. Owner chose option (b): route it through the gate. This run's scope is therefore: ratify or restate those two edits in this file's own voice, repair :392, and accept that this gate has now twice opened new findings against the same change set.
- (decision) AD-24's Story-17.7 ownership paragraph (spine :218) is RATIFIED as an amendment of this file, not accepted as a downstream citation repair. Nothing renumbered, no AD-n added, and no clause sentence of the paragraph itself changed - what changes is its authority: the self-certification note at :220 ('Recorded by bmad-dev-story as a citation repair, not an amendment') is replaced by this run's ratification record, so the precedent is explicit rather than silent. Ratified rather than restated because every claim in it verified: Story 17.7 is registered in epics.md and sprint-status.yaml, the owner did choose the route-group layout on 2026-07-31, and the four holes are the four round 2 found. Option (a) - accept the waiver and record it - was the owner's other choice and was declined, which matters as precedent: two workflows in a row now (bmad-dev-story, then bmad-code-review) declined to substitute for this gate, and accepting the waiver would have made the third one's refusal harder to justify.
- (decision) AD-24 amended at :215 and :216, and this is the substantive half of the run rather than a citation fix. (1) MODULE NAMING: the shell reset no longer goes 'through the one shared hook src/lib/use-projected-shell.ts'. Since round 2 the mechanism is src/lib/projected-shell.ts - claimProjectedShell, REFERENCE-COUNTED, only the first claim snapshots and only the last release restores - and use-projected-shell.ts is a nineteen-line React binding over it. The invariant is one shared implementation and never a per-surface copy; naming the wrong half of the split is how a third surface ends up snapshotting the second one's black and restoring it to the operator's whole app shell. (2) THE REASON FOR THE GAP IS CORRECTED, which the AD-5 precedent says matters: :216 explained the clause's word 'client' as 'a React hook cannot run in a Server Component'. True but not the binding reason, and refutable in the direction that gets a rule reverted - a reader who learns claimProjectedShell is plain DOM with no React in it concludes the Server-Component shells can just call it. They cannot: a Server Component never executes in the browser and has no document at all, so NO browser-side mechanism closes this, hook or not. The leaked paint is the server's first paint, which is what :218 already tells the implementer and what the closure candidates (CSS, or a server-emitted class) are shaped by.
- (constraint) Deferred bullet on AD-24's closure gate REPAIRED, the patch item the owner assigned to this hand. Three factual retractions, each verified rather than taken from the review: the wholesale @/lib directory exemption is gone (round 2 of story 17-1), componentImports is gone with it (grep -c returns 0), and the cited :233 now points at an unrelated TOKEN_SHORTHAND regex - so the spine was understating AD-24's enforcement AND pointing at a dead symbol at a dead line. The walk is moduleImports at :384. What replaces the bullet is the REAL ceiling, which the review told this run not to lose: the queue enqueues .tsx only (:432), so a .ts module is scanned for tokens itself and then read as a leaf; moduleImports matches import-from and dynamic import only, so 'export … from' is invisible - live at src/lib/parsed-fields.ts:432; the walk is downward only; and the guard covers the files listed. Also corrected: the bullet claimed the suite 'guards className on SlideView/ArtifactSlide'. Both now take NO className at all, so the guarantee is a compile error rather than an assertion - a stronger property than the one the spine was crediting.
- (constraint) One PATTERN promoted from story record to spine altitude, in the same Deferred bullet: three rounds running, a finding that a rule was applied too narrowly has been closed by WIDENING THE LIST rather than by encoding the rule. Round 3 found nine further narrownesses in the rebuilt guard, all latent, every one reproduced by injecting a defect and watching 43/43 stay green. It belongs here rather than only in story 17-1 because AD-24 names that suite as its closure gate: a gate an AD depends on has its failure mode as part of the invariant, not as implementation detail. The cleanest instance is in this very bullet - the @/lib DIRECTORY exemption the last round removed came back as the .ts EXTENSION ceiling over the same directory. Deferred now says: treat the next list-widening as a signal to encode the criterion.
- (event) TWO FINDINGS ADDED to the shell-gap Deferred entry, both bearing on the mechanism Story 17.7 will build on. (1) src/lib/projected-shell.ts:34-36's own header comment claims 'a Server-Component layout can reach it without a hook'. FALSE as written - a Server Component never executes in the browser and has no document - and it sits in the module a 17.7 implementer opens first, next to the correct reasoning. Code-owned, filed not fixed: an architecture Update run does not patch production code. (2) claimProjectedShell ignores its doc argument after the first claim, so a second document gets neither the reset nor a working restore; unreachable today (both callers pass the same document, the projector window has its own module instance) and already deferred to 17.7 by round 3, recorded here because 17.7's layout is the third caller and is also what decides whether the contract goes per-document. Also sharpened in that entry: 'the reset has to move somewhere a Server Component can reach' became 'has to become something the server can emit' - the mount is the open decision, the mechanism is constrained regardless of it.
- (event) Reviewer Gate for the AD-24 ratification Update: 4 parallel subagents, authorized by the owner. lint_spine 0 findings, then rubric walker + version/reality-check + adversarial two-units + ad-hoc amendment-verification. Reports in reviews/*-2026-07-31-ad24-ratify.md. Consolidated: 3 CRITICAL, 8 HIGH, ~20 MED-LOW. THE GATE EARNED ITS KEEP FOR THE THIRD CONSECUTIVE RUN, and this time the headline findings were against the amendments THEMSELVES rather than the pre-existing text. Every claim the parent acted on was verified in the repository or in Next's shipped docs by the parent before it was applied - one lens claim (prd.md:552) checked out, another (epics.md:374 'blank') was directionally right but wrong in detail, and the truth was worse than reported: the handoff it cites is tracked NOWHERE.
- (decision) AD-24's gap clause CORRECTED DURING ITS OWN GATE - the run had replaced a refutable reason with a new refutable ABSOLUTE, one level deeper. Earlier today this run wrote 'no browser-side mechanism closes this gap - hook, direct DOM call, or useLayoutEffect alike'. Refuted from inside this repo: node_modules/next/dist/docs/01-app/02-guides/preventing-flash-before-hydration.md:15 documents a pre-paint inline script that runs synchronously as the browser parses HTML and updates the DOM BEFORE first paint - browser-side, emittable by a Server Component, and the mechanism next-themes itself uses, which is the package AD-24 already rests on for the theme key and attribute=class. So the run handed Story 17.7 a mechanism set that excluded the framework's own answer to precisely this problem. The clause now states the constraint as a TIMING one: nothing inside React's client lifecycle reaches the server's first paint, and three mechanisms take effect before it - route-segment stylesheet, server-set class, pre-paint inline script. Verified by the parent against the shipped doc, not taken on the lens's report.
- (decision) THE THREE PRE-PAINT MECHANISMS ARE SEPARATED BY RELEASE, and that is now the load-bearing distinction in the shell-gap entry rather than a candidate list. AD-24 calls the shared implementation's reference counting an invariant precisely so the operator's hub gets its shell back on Exit. A stylesheet cannot release: Next's docs state stylesheets are NOT removed as you navigate between routes (01-app/01-getting-started/11-css.md:294, verified), so the CSS closure the run's earlier text MANDATED would strand the whole hub at black with no scrollbar gutter after the operator leaves a projected route - failing on the opposite screen from the one being protected, and contradicting an invariant the same run had just named one clause earlier. A server-set class has one reachable encoding because only the ROOT layout may render html, so it needs headers() plus a marker and the only marker-setter here is src/proxy.ts - making AD-24's paint closure a consumer of AD-5's matcher, a coupling to choose deliberately. The inline script is the only candidate that is both pre-paint AND releasable. 17.7 picks one and says which; what it may not do is take a non-releasing mechanism and leave claimProjectedShell mounted as though the pair still had one release path.
- (constraint) AN INSTRUCTION THIS SPINE WAS CARRYING WAS UNSATISFIABLE, found by the rubric walker and verified in the test file: 'FULL_SCREEN gains both shells in the same change set'. FULL_SCREEN asserts every member matches /useProjectedShell()/ (tests/theme-chrome.test.mjs:511-524) and neither Server-Component route shell can call it - that IS the gap - so obeying the instruction turns 43/43 green into two failures. Today's own edit is what made that permanent by stating the timing constraint correctly. Replaced with: the closing change set rewrites the ASSERTION to whatever mechanism 17.7 lands and widens the set to the route group. With a note against the CSS candidate specifically - the gate cannot read stylesheets at all, so that mechanism would be guaranteed by nothing.
- (event) Retraction sweep and pre-existing falsehoods repaired while the spine was open. RESIDUE the run had left (found by rubric walker + amendment lens): the Structural Seed tree still named use-projected-shell.ts as 'the ONE app-shell reset every full-screen room-facing surface calls' and omitted projected-shell.ts entirely - now both, with the partial tag noted; the Consistency Conventions Client-state row and the mermaid AD-24 edge both stated the closure as complete with no partial marker - both now carry it. PRE-EXISTING AND FALSE, found by the version lens and verified by the parent: AD-23 still accused docs/architecture.md:61 of hardcoding a crossfade, though the 2026-07-30 source reconciliation this memlog itself records fixed that line, which now states the configured value and cites AD-23 - the doc was repaired and the accusation was not; and the Next security bullet asserted 'Next has stated that earlier minors are not back-patched', which is refuted (no such statement, 15.5.21 patched the same day) and was never needed, since next is pinned exact. CITATIONS: prd.md:540 -> :552 for AD-4's load-bearing 'target, not yet deployed' fact (:540 is the Roles line); slideshow/page.tsx:76 -> :88 at two sites, a citation this run had itself propagated.
- (question) AN UNACKNOWLEDGED HANDOFF was found to be worse than a wrong line number. The provenance paragraph said the EXPERIENCE.md:153 repair (a bare AD-4 cited for what belongs to AD-14) 'is owned by bmad-ux and is tracked at epics.md:374'. epics.md:374 holds an unrelated liturgical-suppression note, and grep finds NO entry for that repair anywhere in epics.md - so the handoff is tracked nowhere, which is the exact failure mode the Deferred entry on the two EXPERIENCE.md affordance questions already names. Recorded as open in the spine rather than left pointing at a line that does not carry it. Giving it a key is bmad-create-story / bmad-ux work, not this run's.
- (event) Deferred gained three entries from this gate, beyond the shell-gap rewrite. (a) THE SHARED RESET HAS ONE COLOUR AND ITS QUEUED THIRD CONSUMER NEEDS ANOTHER: claimProjectedShell pins literal #000000, correct for the room-facing surfaces and asserted - but deferred-work.md records PresenterOperator as the obvious third consumer of a route-group shell, and AD-24's own last clause classes that surface as an OPERATOR surface, so black is the wrong reset for it; the natural fix, a colour parameter, passes the guard because the guard reads only the module default. (b) CI AND RELEASE is the one dimension AD-21 binds BY NAME and no decision owns - AD-21 compacts unreleased transitions 'before release' and freezes a version that reached production, both statements about a release event, while AD-4 dates the first deployment as not yet having happened; nothing fixes what a release IS here, so AD-21's freeze rule is unenforceable. Current floor named, revisit at first deploy. (c) The notFound() closure and 17.7's closure may legitimately pick DIFFERENT encodings from the three mechanisms, closing one hazard twice with two rules - land them together or state which owns the shell.
- (event) Spine Update closed 2026-07-31 (AD-24 ratification run): 24 ADs, 13 ADOPTED / 4 ADOPTED-partial / 7 TARGET - census IDENTICAL to before the run, nothing renumbered, no AD added, no tag flipped. lint_spine 0 findings, theme-chrome 43/43, public-repo-guard 4/4, sprint-status.yaml parses. Same-change-set artifacts moved so nothing is left lying: story 17-1's two spine items checked off with the ratification AND with the three corrections the gate made to this run's own text; sprint-status.yaml line 148 no longer asserts the Update run as an unmet BLOCKING prerequisite, and last_updated leads with it; deferred-work.md carries the projected-shell.ts:34-36 false-comment finding under Story 17.7's key, which it previously lacked. NOT DONE and reported to the owner instead of acted on: (1) AD-24 is now about 2000 words, 23.5 percent of all AD text and 2.4x AD-19, and the rubric walker named a SAFE split - move the closure clause to a new AD-25 (permitted: next id, nothing renumbered) and move roughly 340 words of run-record narrative to this memlog, which is a declared companion, with no invariant lost. That restructures a heavily-cited AD across four artifact families and the owner has not asked for it. (2) EXPERIENCE.md:93 gains one narrow new obligation from this run ('before any hook runs' is now three of four holes, not the full rescope) - bmad-ux owns that file and round 3 already filed the larger item. (3) The engines floor the Stack Deferred proposes, >=22.12.0, is one patch too low for eslint 10's ^22.13.0 requirement in the same list - code-owned. (4) AD-4 says presenter.example.org where prd.md:552 says presenter.example.church; both synthetic, left alone rather than guessing which redaction is canonical.
- (direction) OWNER DIRECTIVE 2026-08-01 (round 4 of story 17-1's code review, decision item at story :349-351): the stale AD-24 closure-gate ceiling bullet is repaired by a bmad-architecture Update run, option (a), and 17.1 does not go to done until it lands. Scope as the owner stated it: repair the ceiling bullet to the shipped gate (walk reaches 27 modules with projected-shell.ts among them, 'export … from' is read, the .ts enqueue ceiling is closed); NARROW rather than delete the exportedProps clause because the composed-type spelling still opens the prop; repair sub-bullet (ii)'s citation of :511-524 and its '43/43 green into two failures' arithmetic against a file now at 47 tests; and carry forward four live ceilings - runtime-composed class, CSS-file token, downward-only walk, listed-files-only (P5 shows the last still live in the focusable guard). This run VERIFIED each of those against the shipped file rather than taking them on report, and THREE of them had gone stale between the directive being written and the round-4 patches landing. Recorded because a directive followed literally would have re-introduced two false statements into the artifact this run exists to make true.
- (constraint) THE exportedProps CLAUSE IS NOT NARROWED, IT IS RETRACTED - the owner's nuance was correct when written and was overtaken by patch P3 the same day. The directive said narrow-not-delete because the composed-type spelling (React.ComponentProps<'div'> & { … }, or interface … extends) still restored a projected className with tsc clean. Verified at tests/theme-chrome.test.mjs:1042: exportedPropsShape now resolves the inline parameter list, the WHOLE local declaration of a named props type (through its terminating ';' or its interface body, not merely the first balanced block), and the whole declaration of every locally-declared type that one composes - and anything the file does not declare fails LOUDLY, with React.ComponentProps<'div'> named in the failure message as carrying className by definition. So the spine's hedge 'only while the props stay declared inline' is now wholly false rather than partly true, and narrowing it as instructed would have left a false ceiling standing. What replaces it is the rule the guard now states, plus the one ceiling that IS live and was found by this run rather than reported to it: exportedProps at :927 matches the literal string 'export default function', so an async default export is unreadable. It FAILS LOUD (assert.ok(at !== -1)) rather than passing, so it is safe where it is applied - but it is why the two Server-Component route shells cannot simply be added to FULL_SCREEN, and that is a different reason from the one the spine gives.
- (constraint) SUB-BULLET (ii)'s ARITHMETIC WAS WRONG IN KIND, NOT ONLY STALE, AND THIS RUN MEASURED IT RATHER THAN RECOMPUTING IT. The spine said adding the two Server-Component shells to FULL_SCREEN 'turns 43/43 green into two failures', citing tests/theme-chrome.test.mjs:511-524. Both halves are wrong now: the FULL_SCREEN const is at :1100 and the useProjectedShell assertion at :1105-1119, and the file is at 48 tests. The count was ALSO wrong when written - FULL_SCREEN feeds TWO loops (:1105 and :1121), so adding two members adds four tests, not two. Measured by temporarily adding both route shells to FULL_SCREEN and running the suite (probe reverted, git status clean, 48/48 restored): 52 tests, 4 failures. Two are the shell gap itself (useProjectedShell unreachable from a Server Component). The other two are a DIFFERENT mechanism the spine never named: the branch-root text-colour guard calls jsxReturnBranches -> exportedFunctionBody -> exportedProps, which asserts on the literal string 'export default function' and both route shells are 'export default async function' - so it fails with 'expected a default-exported function' before it ever reaches a class name. Recorded as measured-not-derived because this bullet has now been wrong twice in the same place, both times by carrying an arithmetic nobody re-ran.
- (decision) THE PATTERN THIS SPINE PROMOTED TO ITS OWN ALTITUDE BROKE IN ROUND 4, AND THE SPINE MUST SAY SO RATHER THAN KEEP COUNTING. The 2026-07-31 run recorded: 'three rounds running, a finding that a rule was applied too narrowly has been closed by WIDENING THE LIST rather than by encoding the rule', and instructed that the next list-widening be treated as a signal to encode the criterion. Round 4 is the first round that did it, on both axes it touched. (1) exportedPropsShape states the RULE - these props are composed only of inline object literals and types declared in this same file, anything else fails loudly - instead of adding '&' and 'extends' to a list of spellings; the guard's own comment names this ('following the name and then reading only its first { … } moved the wall by exactly one spelling'). (2) projectedTree() is ONE shared answer to 'what is projected', consumed by the token guard at :867 and the focusable sweep at :718, replacing two answers of different widths - which retires the listed-files narrowing the owner's directive expected to still be live in the focusable guard (patch P5, closed the same day). Keeping the 'three rounds running' sentence would make the spine wrong in the direction that matters most: it would teach the next reader that the criterion still has not been encoded, when the evidence that it can be is now in the file. What survives is narrower and sharper - the ROOTS are still a list. PROJECTED (six entries) and FULL_SCREEN (two) are hardcoded; the walk widened the REACH, not the roots, and an unregistered room-facing surface is still invisible, which is how SlideshowClient shipped the AC-4 defect. That is the pattern's last instance in this gate and it is now stated as such.
- (constraint) CITATION DRIFT found by this run's own sweep, outside the owner's stated scope and inside the same AD: 'projector/page.tsx:71' is stale at TWO sites (AD-24's gap clause and the shell-gap Deferred entry). Round 3 rewrote that failure branch to 'fixed inset-0 overflow-y-auto bg-black text-white' with a min-h-full inner column, matching the slideshow's shape, and it now sits at :85. The twin citation 'slideshow/page.tsx:88' is still correct - verified. Worth recording rather than fixing silently, because the spine cites :71 as the evidence for a clause that is still TRUE (both shells are Server Components and reset neither html nor body) - so the claim survives and only its pointer rotted, which is the failure mode hardest to catch by reading. Also swept and CONFIRMED STILL ACCURATE: the Deferred claim that a colour parameter on claimProjectedShell 'passes tests/theme-chrome.test.mjs, which reads only the module's default' - :1164 asserts the literal '#000000' in the module source, so a parameterised default would still satisfy it.
- (event) REVIEWER GATE FOR THE 2026-08-01 GATE-REPAIR UPDATE: 4 parallel subagents authorized by the owner; the host process died mid-run and three of four reports had already landed, the version/reality-check lens was relaunched. lint_spine 0 findings. Reports in reviews/*-2026-08-01-gate-repair.md. THE GATE EARNED ITS KEEP FOR THE FOURTH CONSECUTIVE RUN, and for the second run running its headline findings were against the amendment ITSELF rather than pre-existing text. Consolidated: 1 CRITICAL/HIGH, 7 HIGH, ~10 MEDIUM, 4 LOW, plus foreclosed attacks. THREE lenses converged independently on the same contradiction (rubric F1, adversarial F2, amendment F1 approaching it from a third direction), which is the same convergence signal the 2026-07-31 gate produced. Every finding acted on was verified in the repository by the parent before it was applied - including two reproduced at the keyboard: the LITERAL_OUTLINE_COLOUR arbitrary-value bypass (run against the shipped regex) and the edge-guard scope (EDGE_UTILITY consumed at :631 and nowhere else).
- (decision) THE PATTERN CLAIM THIS RUN WROTE THIS MORNING IS RETRACTED BEFORE IT SHIPPED - the gate refuted it, and the refutation is the most valuable thing in this run. This run had written: round 4 encoded the criterion 'on both axes it touched', therefore 'the roots are the last instance in this gate, and widening that list is the widening to refuse'. FALSE on three counts, each verified by the parent. (1) Round 4 widened a THIRD list in the same file: LITERAL_OUTLINE_COLOUR (:697) went from a four-spelling subtraction list to a NINE-spelling one, after the round-3 finding had explicitly offered 'match a positive colour vocabulary rather than a subtraction list' as the alternative. Grouping a list into two named buckets is not encoding a criterion. (2) That widened list is ALREADY DEFEATED in the same direction, reproduced against the shipped regex: focus-visible:outline-[transparent], outline-[inherit] and outline-[color:inherit] are ACCEPTED while their bare spellings are rejected - and none carries a token name, so no token guard sees them either; the arbitrary-value form is house idiom on these very surfaces and EDGE_WIDTH already models it. (3) Even the axis this run credited with encoding still asserts the ABSENCE OF ONE WORD rather than the closedness of a shape: an inline index signature ({ slide: SlidePlanItem; [key: string]: unknown }) short-circuits exportedPropsShape at :1051 and contains no 'className', while the JSX call-site belt iterates allTsxFiles() only (:340), so a React.createElement(SlideView, {className}) from a .ts module is invisible to both. So the className clause is NOT simply 'retracted' as this run wrote - the composed-type spelling is closed and two adjacent spellings are live. The standing instruction is restored to its general form and the tally is stated honestly: two axes encoded, one list widened, and the encoded axes still assert words rather than shapes.
- (constraint) THE SPINE WAS FORBIDDING AND MANDATING THE SAME CHANGE, and this run created the contradiction. Three lenses found it. This morning's text said 'widening that list is the widening to refuse' about FULL_SCREEN; AD-24 :218, ratified 2026-07-31 and therefore carrying this file's authority, records the owner's chosen design as 'one layout owning every room-facing URL, WITH FULL_SCREEN WIDENED TO IT'. A reviewer of Story 17.7 could not satisfy both and neither clause was subordinated. RESOLVED by stating the distinction the rubric lens supplied and the run had left implicit: a route-group layout is the structural ENCODING, not a list-widening - every page under the segment inherits it and there is no registration step to forget, which is exactly the property a list lacks - so what must be refused is adding another LEAF FILE to a list. Recorded with the second-order half the lens also found: nothing in the spine assigns the PROJECTED criterion to anyone, and AD-24 states in its own voice that 'a partial tag with no owner is a dead end'. Also corrected: AD-24's rule clause said the gate has 'TWO sets that must both be maintained'. It has FOUR hardcoded lists - PROJECTED (:546), ROUTE_SHELLS (:740, which hand-duplicates two PROJECTED entries and feeds the scroll guard), FULL_SCREEN (:1100), and an inline pair at :1087 feeding the className props guard. An implementer obeying AD-24 to the letter registers a new room-facing branch in the two NAMED sets and still misses the scroll guard. And AD-24's claim that 'both ceilings are recorded in Deferred' pointed at a record that did not exist: the 'never its own copy' ceiling was in no Deferred entry, which mattered more after this run turned Deferred's open-ended ceiling list into a definitive enumeration.
- (constraint) VERSION LENS: this run's own two passages came back CITATION-CLEAN (all 11 line refs exact, the 48-test baseline independently reproduced) and the rot was everywhere else - four HIGH, two of them claims about SIBLING ARTIFACTS THAT HAVE SINCE BEEN REPAIRED, which is the citation-rot failure running in the direction that WASTES WORK rather than hides it. All verified by the parent before repair. (1) The paragraph at :42 said the EXPERIENCE.md bare-AD-4 repair 'is tracked nowhere' and cited EXPERIENCE.md:153 - which is a BLANK LINE. bmad-ux had already made the repair in the file itself: the bullet is at :185 and states in its own voice that it supersedes AD-14 and that a previous version cited AD-4. The memlog's own open (question) entry from 2026-07-31 about an unacknowledged handoff is therefore CLOSED. (2) The Deferred entry claiming 'two affordance questions this spine hands to EXPERIENCE.md have not been received there, whose Open Items carries neither' is FALSE - both received WITH OWNERS: Open Item 6 stale-snapshot / Story 20.8 (:304), Open Item 7 Reset-reverts-a-rename / Story 20.3 (:306). Open Item 7 also found a SECOND FACE this spine never named: an administrator-authored row has no seed to restore under AD-17 and so exposes no Reset at all, leaving two rows side by side with different verbs. (3) AD-23 cited prd.md:305 for FR-7; :305 is an offline-cache assumption. FR-7 is at :179-180 and the cross-surface obligation is at :304, verbatim - both now cited. (4) NFR-3's binding cited prd.md:550, which is the Privacy paragraph; NFR-3 is at :563. Also SPEC.md:88 -> :89 (the no-clone-yet services line), and projector/page.tsx:71 -> :85 which this run had already fixed at both sites before the lens reported it.
- (constraint) THE NEXT SECURITY BULLET'S NON-APPLICABILITY ARGUMENT WAS HALF WRONG, and this is the THIRD time this file has been bitten by a refutable reason - the failure mode AD-5's precedent exists to name. The bullet said CVE-2026-64642's precondition is 'a Turbopack + single-locale i18n configuration this project does not have'. Turbopack is the DEFAULT for next dev and next build in Next 16 and next.config.ts carries no opt-out (verified: the file sets only output: standalone), so this project DOES have the Turbopack half. Only the config.i18n.locales half is absent, so the whole conclusion now rests on one leg instead of two. The conclusion survives and the stated reason does not - corrected in place, because a reader who checks the Turbopack half and finds it present has been handed a reason to dismiss the entire bullet, including the SSRF (CVE-2026-64645) that bears directly on AD-8 and AD-5's safeNextPath. Everything else in that bullet re-verified exactly: nine CVEs, four High, both CVSS 8.3 scores, 15.5.21 patched the same day, 16.2.12 still current. STACK DRIFT vs package.json: ZERO, row by row. One undercount corrected: the Deferred header said four rows sit 'a major behind' - TypeScript resolves 5.9.3 against a latest of 7.0.2, which is TWO majors, so the header now reads 'at least a major'.
- (event) SPINE UPDATE CLOSED 2026-08-01 (AD-24 closure-gate ceiling repair). Census IDENTICAL to before the run: 24 ADs, 13 ADOPTED / 4 ADOPTED-partial / 7 TARGET - nothing renumbered, no AD added, no tag flipped. lint_spine 0 findings; theme-chrome 48/48; public-repo-guard 4/4; full suite 387 tests / 386 pass / 0 fail / 1 skipped, identical to the pre-run baseline as it must be for a run that touched no code; working tree carries only the intended artifact edits (one measurement probe on tests/theme-chrome.test.mjs applied and reverted, verified clean twice). CHANGED: the AD-24 ceiling bullet rewritten twice - once against the shipped gate, then again by its own Reviewer Gate which refuted three of the first rewrite's claims; sub-bullet (ii) re-measured and given the conclusion the measurement supports; AD-24's rule clause amended IN PLACE from 'two sets' to the four hardcoded lists; six citation repairs from the version lens; the CVE-2026-64642 Turbopack reason corrected. SAME-CHANGE-SET ARTIFACTS moved so nothing is left lying: story 17-1's decision item checked off with the full resolution INCLUDING the three ways the owner's own scope note had gone stale, its File List note rewritten from 'not this workflow's to touch' to name who touched it, a Change Log entry added, and Status -> review; sprint-status.yaml 17-1 -> review with both the amendment findings and the version lens's six repairs; epics.md story heading -> review; deferred-work.md gained a 2026-08-01 heading with five code-owned findings. NOT DONE and reported to the owner rather than acted on: (1) the DONE call on 17.1 - three of the five filed findings are live narrowings of AC-4's own guard, which is the material argument for a round 5 and is the owner's decision, not this run's; (2) the adversarial lens's proposal that AD-24 assert the PROPERTY (every room-facing URL resolves through a segment emitting the reset before first paint) rather than a mechanism spelling - a new invariant reaching into Story 17.7's design, surfaced in the spine text and left for the owner; (3) no story owns PROJECTED's criterion, named in Deferred rather than assigned, because creating a key is bmad-create-story work; (4) the AD-25 split the 2026-07-31 rubric walker proposed is still unmade and AD-24 grew again today; (5) F10's point that 'room-facing' has no definition anywhere, which 17.7's route segment is the first chance to create.

- (event) EXTRACTED 2026-08-01, owner directive: `../archived/architecture-epic-16/` deleted. The fold-in of 2026-07-30 moved the nine decisions here as AD-11..AD-19 but left their REASONING in that folder, and the spine said so at `ARCHITECTURE-SPINE.md:58` — "a resume that reads only this memlog will not find them". The bullets below close that. Everything carried over is a rationale, a declined alternative, an operational trap, or a dated assumption with an expiry; 13 process events ("spine finalized", "Reviewer Gate run", the folder relocation, the retired `INIT AD-n` citation convention) were deliberately NOT carried. `CASE-STUDY.md` was not carried either: its three decision write-ups restate AD-11/AD-12/AD-13, its data-flow diagram predates AD-16's per-service snapshot, and it still named Vercel in AD-11's rationale — the exact claim the epic-16 gate refuted and fixed in the spine, left standing in the case study because nobody re-ran that fix there.
- (rationale AD-11) SQLite (WAL) chosen for the live registry over the JSON file: concurrent operator saves, and no loss on an ephemeral filesystem. `data/default-registry.json` is a startup seed only. An early draft named **Vercel** in its Prevents; the epic-16 gate raised that as its single HIGH because it contradicts the LiveServer/Docker target AND is impossible for a `better-sqlite3` native addon. Replaced with platform-neutral "ephemeral container filesystems". Recorded as a reality-check failure rather than a deliberate divergence.
- (rationale AD-11) The seed is TWO-LAYERED, and that is a privacy mechanism rather than a convenience: `data/local/default-registry.json` (git-ignored) takes precedence over the shipped `data/default-registry.json` example. A seeder reading only the shipped example breaks the rule that keeps congregation data out of a public repository.
- (rationale AD-13) Uncontrolled Wrapper chosen for the Fabric.js/React boundary: Fabric owns canvas state exclusively, React supplies only the mount point and a Save button and reads state via `canvas.toJSON()`. Declined alternative: two-way binding — the notorious source of drag/drop lag and redundant re-renders when an imperative library is driven declaratively.
- (rationale AD-14) It must be split by clause, never reversed wholesale. Clause 1 (global across services, no per-service override) is REVERSED by AD-16. Clause 2 (management UI and APIs are admin-only and re-check role from SQLite) SURVIVES untouched. Clause 3 (downstream planners read through server-side modules, not the management API) SURVIVES, but now reads the service snapshot.
- (rationale AD-15) Widened during the gate from "canvas serialization" to EVERY write path — canvas save API, startup seeder, import/asset-extraction scripts. The adversarial lens found that a fully compliant SECOND writer could persist unvalidated templates, and an asset-extraction step was already planned, so the second writer was real rather than hypothetical.
- (decision AD-16, owner) Sync Artifact is always permitted, including on a service already presented. Chosen over restricting Sync to the review window.
- (correction AD-16, owner, 2026-07-30) Supersedes an earlier "accepted cost" framing this run had logged. The parent convention *services are immutable once presented* MISLOCATES THE FREEZE. The freeze event is service **CREATION** — a service becomes self-contained against the registry the moment it adopts it — and what is frozen is the **SUPPORTING DATA ENTRY** (sermon speaker, sermon flyer, hymnal number per SongSet slot), never the rendered deck. A past service is worth keeping for the data entered into it, not for regenerating its slides. So no historical-fidelity cost is being accepted at all; three deliberate NON-GOALS replace it: Sync unrestricted on any service, Announcement membership stays live from the master list and is not frozen, and a later structural change (new predefined placeholder, retired kind) need not keep an old snapshot renderable. The design target is the service being prepared now.
- (evidence AD-16) The announcement-not-frozen clause was verified against shipped code rather than asserted: `resolveAnnouncementUrls` (`src/lib/announcements.ts`) selects `announcement_items WHERE service_id IS NULL OR service_id = ?` at render time, so a recurring master item already reaches an existing service today. AD-16 therefore RATIFIES current behaviour; the invariant is that the snapshot must not clone membership into itself and close that door.
- (rationale AD-17) The concrete bug it resolves: a deleted registry entry left no evidence behind, so the seeder read it as never-existed and RESURRECTED IT ON EVERY BOOT. Gating seeding behind a settings marker is what makes an administrator's delete, rename or reorder survive an unrelated restart.
- (trap AD-17) The trap it must therefore name: with bootstrap-once, `data/local/default-registry.json` is read only at FIRST boot. A deployment that boots once and only later gains the private override never gets seeded from it. **Recovery has to be an explicit action, never a restart.**
- (waiver AD-18, expires) Pre-first-deploy, the seven-to-three `base_type` collapse is a TOTAL REPLACEMENT: no backward compatibility, no incremental migration, developer databases rebuilt. The waiver rests entirely on an owner-confirmed assumption — no production deployment existed as of 2026-07-30. **Revisit trigger: first deploy.**
- (correction AD-18) Narrowed by the same owner correction: a post-first-deploy vocabulary migration covers live `artifact_templates` rows and must preserve ENTERED DATA, but is explicitly NOT obliged to keep existing service snapshots renderable. Earlier text demanded a backfill over every snapshot, which the correction shows was never the requirement.
- (decision AD-19, owner mechanism) Strictly simpler than the first draft: the SongSet binding key IS the `base_type` value, so the predefined slots become distinct enumerated types and slot identity is immutable by construction — no separate identity mechanism exists. Reorder changes presented sequence (CAP-8 requires that) and cannot touch a binding. Two clauses keep it unambiguous: `base_type` is never administrator-editable for a slot-carrying type, and at most one row per slot type. A binding whose slot row was deleted is INERT, not an error — the entered hymnal number survives in service data per AD-16.
- (constraint AD-19) Kind is not type. The kind vocabulary is exactly the three the SPEC fixes (`general`, `song-set`, `announcement`); `text-placeholder` / `image-placeholder` / `mix-placeholder` / `fullscreen-image` are GONE rather than renamed, since a placeholder is an element inserted onto a General from the catalog. Only `song-set` expands, into **FOUR** slots — not three. Recorded because SPEC Constraints says slide kinds are exactly three, and a later reader would otherwise read AD-19 as contradicting it.
- (deferred AD-19) An earlier draft asserted the four slots live as distinct `base_type` enum values; that picked a SCHEMA SHAPE the spine has no business fixing, and it was withdrawn. Column value or discriminator beside it — either satisfies the invariant. Deferred to Story 20.2 / 20.7. Naming, however, is load-bearing where it used to be free: slot identities must be SEMANTIC (`songset-bt-open`) never ORDINAL (`songset1`), because an ordinal re-imports the positional reading AD-19 exists to remove, and because the value is a persisted binding key so renaming it later is an AD-18 value migration. The placeholder-catalog key spelling is the same class of decision and is deferred to Story 20.5.
- (constraint) Gate HIGH worth keeping as a pattern: AD-6 (optimistic concurrency) was missing from the epic's inherited-invariant table while the registry had ALREADY shipped `expectedUpdatedAt` / `RegistryStaleError`, and AD-16 made that omission load-bearing — Sync Artifact is a service mutation, so two builders could implement it as a 409 or as an unconditional overwrite and both would read as compliant. An inherited invariant left off the list is not neutral once a new decision depends on it.
- (direction) OWNER DIRECTIVE 2026-08-01 (third run of the day, after the locale Correct Course shipped as PR 14): settle the TWO open bmad-architecture action items in sprint-status.yaml in ONE run. (1) epic-22, open since the morning: no AD governs a SHIPPED REFERENCE CORPUS at all; does the every-boot upsert survive, or does AD-21 counter arrive here? Plus the stale Structural Seed tree and two Deferred bullets. (2) epic-21, WIDENED the same evening by the second Correct Course: two corpus registries carrying locale, per-translation book names, a canonical book identity stable across translations, and three renames. Never renumber an existing AD-n. NOT in scope and NOT an open item: per-locale template identity - rejecting projection_locale makes one global template set an accepted trade (PRD 4.12).
- (constraint) MEASURED 2026-08-01, not inherited: THE TWO SHIPPED CORPORA ALREADY USE TWO DIFFERENT CHANNELS, and no AD names either. upsertHymns (db/index.ts:65-83) runs on EVERY boot and rewrites title and lyrics from the committed file via ON CONFLICT(book_code, number) DO UPDATE. seedBibleCorpus (db/index.ts:91-139) seeds FROM ZERO ONLY - it early-returns when the default translation already holds one verse, so nothing persisted is ever overwritten. Epic 21 and Epic 22 are cut into separate worktrees precisely so they do not touch one table; what the cut did not prevent is the two of them having ALREADY answered the same architectural question two different ways. That is the two-units-choose-incompatibly test failing on shipped code, which is what makes this an invariant rather than a story detail.
- (constraint) MEASURED 2026-08-01: THE CORPUS TABLES HAVE EXACTLY ONE WRITER EACH, and that is the fact the whole reconcile question turns on. Grepped INSERT/UPDATE/DELETE against hymns, bible_verses and bible_books across src/ and scripts/: zero hits outside src/lib/db/index.ts. No hub surface, no API route, no admin screen writes a corpus row. So a corpus table is not authored data in AD-17 sense - there is no administrator edit for a re-seed to silently undo. This is the load-bearing difference from the registry, and it is a property of the code TODAY rather than a promise; the moment a write path appears the reconcile becomes exactly the resurrection AD-17 forbids.
- (evidence) MEASURED 2026-08-01: THE CORPUS REGISTRY ATTRIBUTES ALREADY SHIP INSIDE THE FILES - the registries are a persistence gap, not a data gap. data/bible/kjv.json declares translation.code, .name, .language (en), .licence and .provenance; data/song-book/sdah.json declares book.code, .name, .language (en), .attribution and .licence. src/lib/corpus.ts reads only code and name (corpus.ts:86-95, :171-180); licence, provenance and attribution reach no table. scripts/verify-corpora.mjs:49-54 and :133-138 assert licence/provenance/attribution exist, and tests/corpus.test.mjs:77-104 pins them. THE ONE ATTRIBUTE THE LOCALE AXIS DEPENDS ON IS THE ONLY ONE NOTHING GUARDS: language is asserted by no verifier, no test and no loader. It is also spelled language in the files while the PRD, the paths and the settings keys all say locale.
- (constraint) MEASURED 2026-08-01: bible_books IS ONE TABLE WITH TWO OWNERS, and the collision is armed rather than hypothetical. The table is (id, name, short_name) - one global row per book - while name and short_name are per-TRANSLATION values. seedBibleCorpus insertBook does ON CONFLICT(id) DO UPDATE SET name = excluded.name, short_name = excluded.short_name (db/index.ts:99-105), so whichever translation seeds last owns every book name globally. It cannot fire today only because the from-zero guard checks the DEFAULT translation; Story 21.2 parameterises exactly that guard, at which point installing an Indonesian translation renames Genesis to Kejadian for the KJV reader too. Book ids 1..66 are supplied BY THE CORPUS FILE (corpus.ts:106, r.id), so a second file could also renumber the identity that bible_verses.book_id points at.
- (decision) OWNER DECISION 2026-08-01, and it is a THIRD answer rather than either of the two the action item offered. The item asked: does the every-boot upsert survive, or does AD-21 counter arrive here? Neither. A SHIPPED REFERENCE CORPUS IS A DERIVED PROJECTION OF ITS COMMITTED FILE - the file is authoritative, the table is reconciled to it, and that is its own channel with its own closure. The premise both offered answers share is false: AD-17 exists because an administrator owns registry rows, and AD-21 counts transitions over values somebody persisted deliberately. Neither is true of a corpus table, which has exactly one writer and no editor. Recorded as a departure from the framing rather than an answer inside it, because a later reader will otherwise assume the binary was resolved rather than dissolved. Becomes AD-25.
- (decision) OWNER DECISION 2026-08-01: THE RECONCILE IS COMPLETE, bounded to the corpus code it names. Rows carrying that code and absent from the file are REMOVED, so the table is genuinely the file rather than the file plus history. What it prevents concretely: a hymn withdrawn from a corpus still resolving and still reaching a deck on a Sabbath. The bound is what keeps it safe - a reconcile may never touch a row belonging to a sibling corpus. CONSEQUENCE STATED RATHER THAN SLIPPED IN: rows stamped SDAH by the Story 22.1 boot migration that are not among the corpus 695 disappear at the next boot; the migration deliberately preserved them. Cost is zero today because AD-4 records no deployment exists, which is the same pre-first-deploy licence AD-18 already carries and it expires on the same event.
- (decision) OWNER DECISION 2026-08-01: THE SPELLING IS locale, EVERYWHERE. The two committed corpus files declare language; the PRD, the data/<locale>/ path segment, the four settings keys and all prose say locale. One concept, two spellings, and it is a persisted cross-boundary key - the class AD-19 says is fixed once and changed only by a versioned migration afterwards. The files are the source of record and no deployment exists, so the field is renamed in both files now rather than mapped in the loader forever.
- (evidence) A LIVE DEFECT FOUND WHILE SIZING AD-27, measured against the shipped functions rather than read off the regex. BOOK_ALIASES (scripture.ts:65) maps song of solomon, songofsolomon and sos onto the name Song of Solomon. THE SHIPPED CORPUS DOES NOT HOLD THAT NAME: data/bible/kjv.json book 22 is Song of Songs, shortName Song. So all three entries resolve to nothing. Two more of the six entries (psalms, ps) are redundant - they already match the corpus name and shortName - leaving exactly ONE (psalm, singular) doing work. AND THE PARSER IS THE HARDER HALF: parseScriptureRef caps a book name at TWO words and ASCII letters only, so Song of Songs 2:1 - the corpus own name for that book - is REJECTED BEFORE resolution is reached. Probed directly: Song of Songs 2:1 and Song of Solomon 2:1 both rejected; Song 2:1 parses. Book 22 of the default translation is reachable only by abbreviation. The same cap rejects Kisah Para Rasul 1:8, which is what the locale axis walks into.
- (constraint) WHY THAT DEFECT IS AN INVARIANT AND NOT JUST A BUG: an alias list maintained BESIDE a corpus rots against it, because nothing binds the two. Under AD-27 names are corpus-owned, so the matcher may not hold a name list of its own at all. But the two jobs BOOK_ALIASES does are not the same job and Story 21.4 will collapse them if nobody separates them here: cross-language naming (Kejadian vs Genesis), which per-translation names subsume, and intra-language input tolerance (psalm for Psalms, abbreviations, punctuation), which they do NOT. Retiring the map wholesale, as epics.md:519 instructs, drops the tolerance. The split: a translation owns its NAMES; the matcher owns how forgivingly it COMPARES. Without that line the next translation corpus file becomes the place people dump aliases, and then names are corpus-owned in name only.
- (question) FOUND IN SCOPE OF AD-24 RATHER THAN OF EITHER ACTION ITEM, and it is a live divergence: WHICH TIER DOES ui_locale LAND IN? AD-24 decides by asking who must agree on it, and for a UI language the naive answer is nobody but this operator - which routes it to persisted-local, the same home its closest analogue the theme took. The PRD answers the other way: FR-25 says an ADMIN sets ui_locale and names it as one of four SETTINGS keys, and Story 24.1 puts it in src/lib/settings.ts on the RETENTION_KEY / SLIDE_TRANSITION_KEY pattern. Both are faithful readings and they pick different storage targets, which the AGENTS.md gate calls a structural invariant. Consequence of the PRD reading, worth stating because it is not obvious: every settings write path is requireAdminSession (spine :405), so ui_locale is one language for the whole hub and an Operator cannot change it - which is what FR-25 says, so the reading holds. Recorded as an amendment to AD-24 rather than a new AD.
- (decision) AD-25 [ADOPTED, partial] A Shipped Reference Corpus Is a Projection of Its Committed File. Binds hymns, bible_books, bible_verses, corpus.ts, the db/index.ts boot path, every corpus added later. Prevents two corpus families answering one question two ways - and the two failures that question has: a corrected corpus that cannot reach the table, or one that reaches it and leaves withdrawn rows standing. Rule: developer-owned, exactly one writer, file authoritative; the table is a projection reconciled to the file, COMPLETE within the corpus code it names and never touching a sibling. A third channel: not AD-17 bootstrap (a bootstrap runs once because an administrator owns what follows, and nobody owns a corpus row), not AD-18/AD-21 value migration (nothing here was persisted deliberately). Detection mechanism left to the story, bounded by two properties: a correction reaches the table with no operator step, and the table does not outlive the file. Closure: no UI write path into a corpus table exists today, and adding one reopens the decision BEFORE it ships. Runs after data migrations on the getDb path; order vs the AD-17 bootstrap is free because the tables are disjoint, stated so AD-21 fixed sequence is not read as forbidding a fourth step. Consequence taken: a rename inside a projected table is a rebuild, not a migration.
- (decision) AD-26 [TARGET] A Corpus Is a Registered Entity: Its Code Is the Key, Its Locale Is an Attribute. Binds the bible_translations and song_books registries, bible_verses.translation_code, hymns.song_book_code, the loaders and paths, the three default_* keys, every corpus listing endpoint. Prevents one corpus code meaning two books depending on which locale directory held it - and the mirror, a locale reaching a WHERE clause and turning a view default into a data filter. THE STRUCTURAL PAYOFF: because no key contains a locale, no read path can NEED one, so FR-24 never-filter rule holds structurally rather than by discipline. The file declares and the path merely locates - the loader already refuses a declared code that disagrees with the loaded code (corpus.ts:91, :176) and refuses a declared locale disagreeing with its directory on the same terms. Three renames belong to this AD because book_code means song book in one table while bible_books makes book mean a Bible book in another. isKjvCorpusEmpty -> isBibleTranslationEmpty(code) is the same rule reaching a signature: no corpus code survives as a literal. Terminology stops deliberately at hymn as the entry term, because resolvedHymns / failedHymnNumbers is an external contract (AD-3).
- (decision) AD-27 [TARGET] Book Identity Is Canonical and Translation-Independent; Every Name Belongs to a Translation. Binds bible_books, bible_verses.book_id, name resolution and display, the bible loader, Story 21.4. Prevents one table with two owners - the last translation seeded renaming every book for every reader - and same passage another translation ceasing to be one query. Rule: one canonical identity carrying NO display text; every name and shortName belongs to a translation. A corpus file MAPS onto the identity, it does not define it, and a book outside the canon is refused WITH THE BOOK NAMED (fail-closed, per AD-5/AD-8/AD-17) rather than silently partially imported. Two consequences stated: the shipped canon is the 66-book Protestant one, so a deuterocanonical translation is refused rather than truncated and admitting one is code-plus-migration; and INPUT TOLERANCE IS THE MATCHER CONCERN, NEVER THE CORPUS - a translation owns its names, one shared server-side matcher owns how forgivingly it compares. The second is the non-obvious half: the collapse is one-way, and once a corpus file is where an alias goes, every translation added later inherits the job of listing every way somebody might type its books.
- (decision) AD-24 AMENDED, no new AD and nothing renumbered: ui_locale is PERSISTED-SHARED. AD-24 own tier test returns the wrong answer for it - who must agree on the interface language answers nobody but this operator, which routes to persisted-local, the same home the theme took, and a builder applying AD-24 faithfully would reach for localStorage. FR-25 and PRD 4.12 decide otherwise: an ADMIN sets it, it is one of four settings keys, so the hub has ONE language rather than one per browser. Recorded so it is not re-derived, with the consequence named: every settings write path is requireAdminSession, so an Operator cannot change the interface language - which is what FR-25 intends, and which is the no-per-account-tier Deferred item arriving from a second direction. The room-facing closure is unchanged and restated: no projection_locale, so a language setting reaching the deck would be a fourth structural channel to the congregation screen.
- (event) REVIEWER GATE for the 2026-08-01 corpus+locale Update. Subagents NOT used - the host forbids the Agent tool unless the user asks for it, so the four lenses ran SEQUENTIALLY inline, each written to reviews/ before moving on. Same posture as the 2026-07-29 run recorded as subagents unauthorized; noted because three runs in a row used parallel subagents and a reader would otherwise take the change as an oversight. lint_spine 0 findings, twice - before the semantic lenses and again after every fix. Lenses: rubric walker, version/reality-check, adversarial two-units (both configured finalize_reviewers, never cherry-picked), plus an ad-hoc DATA-INTEGRITY lens the gate reference recommends for a heavy data model - earned here by a change that adds two registries, a names table, three renames and a channel that DELETES rows. Reports in reviews/*-2026-08-01-corpus-locale.md. SEVEN findings, all fixed in-run, none deferred to the owner.
- (constraint) THE MOST SERIOUS GATE FINDING WAS A DELETE PATH THE DRAFT AUTHORISED BY ACCIDENT. AD-25 said rows absent from the file are removed. A file that fails to open contributes zero rows, so a literal implementation deletes all 695 hymns on boot from one corrupted download or bad merge - and a second implementer reading skip this corpus is equally faithful. Closed: a missing, unparseable or refused corpus does NOT reconcile at all, the table is left as it stands, and the failure is loud with corpus and reason named. Removal is what the reconcile does having READ A GOOD FILE that lacks the row; never what it does having read nothing. The shipped loader already throws (corpus.ts:64-79), so the fix ratifies behaviour rather than inventing it. THE PATTERN BEHIND THREE OF THE FOUR ADVERSARIAL FINDINGS: the draft wrote every Prevents from its success case and was silent on its failure case - no file, duplicate key, dangling default_* reference. Worth watching alongside the list-versus-criterion pattern this file already tracks.
- (constraint) RUBRIC FINDING WORTH KEEPING AS PRECEDENT: AD-25 does, word for word, what AD-17 unqualified sentence forbids - a gap in a database that already holds data travels as a versioned data migration, never as a re-seed. Not an actual conflict, because AD-17 Binds scopes it to the registry and every clause of it is about rows an administrator owns. But the disambiguation lived ONLY in a Binds list three screens away while the prohibition reads as a general principle, so a reader arriving at either decision first concludes the other is wrong. Fixed with an explicit SUPERSEDES NOTHING opening line that states the scope AND says why the line exists. The house style had the positive precedent (AD-16 supersedes this clause of AD-14 and nothing else); the NEGATIVE case - a new decision that touches an old one and changes none of it - had never been written down.
- (event) SPINE UPDATE CLOSED 2026-08-01 (corpus + locale). Census 24 -> 27 ADs: 13 ADOPTED / 5 ADOPTED-partial / 9 TARGET. Added AD-25 [ADOPTED, partial], AD-26 [TARGET], AD-27 [TARGET] as the next three ids. NOTHING RENUMBERED, no id reused, no existing tag flipped - the AGENTS.md never-renumber rule held with no waiver, as it did on 2026-07-31. AD-24 amended in place with a ui_locale tier clause (no new id). Two Deferred bullets struck as closed with their record kept (FR-19 corpus ops, SDAH licence), the AD-21 counter bullet amended to say AD-25 is not what introduces it, and four Deferred entries added. Structural Seed tree and the first mermaid repaired. Capability map gained an Epics 21/22/24 block. lint_spine 0 findings; public-repo-guard 5/5; docs-only, zero code files touched.
- (correction) CORRECTED 2026-08-01, same day, after the owner asked what the Song of Solomon finding actually was - AND THE FIRST VERSION HAD THE BLAME THE WRONG WAY ROUND. This run had recorded three BOOK_ALIASES entries as BROKEN because they target Song of Solomon while data/bible/kjv.json names book 22 Song of Songs. True, and framed as though the map had rotted. WEB-VERIFIED against Project Gutenberg KJV text: the book HEADING reads Song of Solomon, and the song of songs is a phrase INSIDE VERSE 1 (The song of songs, which is Solomon s), not the book title. Song of Songs is the Hebrew-derived title other translations use and it reached the file through the SQL export the corpus was normalised from - the corpus provenance note is accurate about verse text and silent about book names. SO THE CORPUS IS THE WRONG SIDE, the alias map was right, and this is a live AD-27 output-is-exact violation on the SHIPPED DEFAULT TRANSLATION rather than a stale list. It is a one-word data fix, after which two of the three entries are merely redundant and sos starts working. SWEPT ALL 66 rather than stopping at the one that surfaced: exactly one mismatch against conventional KJV titles. The direction matters because Story 21.4 would otherwise have deleted three correct aliases to match wrong data.
- (constraint) AND THE CORPUS FIX DOES NOT REACH THE PARSER, which is the part worth carrying into Story 21.4. parseScriptureRef caps a book name at two words and ASCII, so Song of Solomon 2:1 is rejected exactly as Song of Songs 2:1 is - correcting the data makes the RIGHT name untypeable in precisely the way the wrong one was. Measured: book 22 is the ONLY book in the 66 whose name runs past two words, which is why a cap this coarse has survived unnoticed since the parser was written. The two fixes are independent and both are needed: correct the corpus name, and widen the parser. Ordering note - correcting the corpus alone still buys something real, because sos is one word, parses, and would then resolve.
- (evidence) THE PARSER CAP IS TWO LIMITS, NOT ONE, AND THE SECOND ONLY SHOWS IN A SECOND LANGUAGE. Measured after the owner asked what the Kisah Para Rasul remark meant. Indonesian Terjemahan Baru names verified at sabda.org, then probed against the shipped parseScriptureRef: FOUR OF EIGHT cannot be typed. Kisah Para Rasul 1:8 fails the two-word cap, which this run had already named. Hakim-hakim 2:16, 1 Raja-raja 3:5 and 2 Raja-raja 2:11 fail for a reason nobody had named - THE HYPHEN. The book group is [A-Za-z]+ which does not match -, so the pattern stops at Hakim and then requires whitespace it does not find. Reduplication is ordinary Indonesian morphology and recurs across that book list, so it is a standing fraction of every translation in the language FR-24 exists to serve rather than a long-tail case. Kidung Agung 2:1, 1 Tawarikh 4:10, 2 Tawarikh 7:14 and Wahyu 3:20 pass. SIZING NOTE CARRIED TO STORY 21.4: the fix is a matcher comparing against corpus-supplied names (AD-27), NOT a widened regex - admitting hyphens and three words is exactly the list-widening this file keeps refusing, and the next language brings a fourth shape.
