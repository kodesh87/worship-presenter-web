---
scope: Epic 16: Slide Artifact Model
purpose: Refactor slide planning and introduce Canvas editor
altitude: epic
updated: 2026-07-23T07:04
---

- (direction) Mode: Coaching path. Purpose: Publish as a case study (needs an extended human-facing document with diagrams alongside the terse spine).
- (decision) AD-1 (Registry Storage): The live Artifact Registry will be stored in SQLite to unify storage targets and handle concurrency. data/default-registry.json serves only as an initial seed. Binds: Database schema and Canvas API. Prevents: Managing concurrent file writes or losing data in ephemeral environments.
- (decision) AD-2 (Data Flow): buildSlidePlan outputs a fully hydrated AST (Fat Payload) with exact rendering coordinates, fonts, and content. Binds: The output shape of buildSlidePlan. Prevents: Web and PPTX renderers from performing independent DB lookups or managing layout logic.
- (decision) AD-3 (Canvas State Boundary): The Canvas Editor uses an Uncontrolled Wrapper pattern. Fabric.js owns the canvas state exclusively; React only reads state via canvas.toJSON() on save. Binds: React-Fabric integration boundary. Prevents: Two-way binding lag and unnecessary React re-renders during drag/drop.
- (event) spine finalized
