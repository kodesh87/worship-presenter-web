# The committed shell script has to reach macOS and Linux with LF endings even
# when the repository was cloned on Windows with core.autocrlf=true, which is
# what this checkout uses. Without this, bash sees a trailing CR on every line.
*.sh text eol=lf
