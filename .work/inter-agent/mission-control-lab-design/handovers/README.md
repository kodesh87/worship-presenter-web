# Handover Directory

No immutable handover exists yet for `mission-control-lab-design`.

The design is intentionally held before `spek-to-coding` because the user plans to move `labs/mission-control/` into a dedicated repository. Creating a `READY` coding handover in the current BIC repository would leave Cursor to guess the implementation repository, story, base branch, package versions, and protocol-v2 transition-skill changes.

After those prerequisites exist, the first immutable file in this directory must be `001-spek-to-coding.md` with parent `none`.
